---
id: self-docs/engine/document-map
canonical_question: 'Technical guide and specification: Document Map — Core System-backend'
aliases:
- Document Map — Core System-backend
- document map
entity_type: how_to
domain: self-docs > engine
last_verified: 2026-07-14
---

# Document Map — Core System-backend

Generated: 2026-07-08 · updated 2026-07-14 (added `docs/routes-permissions.md`)

This file maps every document in the repository so you can find what you need without reading everything.

---

## Root-level docs

| File | Lines | Purpose |
|---|---|---|
| `README.md` | 166 | Tech stack, how to run, env vars, quick start |
| `ARCHITECTURE.md` | 229 | Go package layout, request flow, auth, DB migration system |
| `MIGRATION.md` | 159 | Clean-Architecture migration playbook — current progress, rules, slice strategy |
| `TESTING.md` | 61 | Test file taxonomy, how to run, conventions |
| `AGENTS.md` | 177 | Agent-oriented summary — entrypoint, env, key packages |
| `BUG_REVIEW_FINDINGS.md` | 22 | Code review findings from June 2026 (formula engine / attendance) |
| `REVIEW_PLAN.md` | 59 | Review scope for develop vs main (202 files, ~25k LOC diff) |
| `docs/routes-permissions.md` | — | **New 2026-07-14.** Role gate + company/department scope gate for every route in `router.go`, kept in sync with `Core System-frontend/docs/routes-permissions.md`. Read this first for "who can call what." |
| `docs/formulas-name-manager-backend.md` | — | **New 2026-07-23.** Technical reference for the frontend team (S004 "Tab Công thức" — T0022 Chèn hàm, T0023 Trình quản lý tên, T0024–T0028). Documents what backend actually exists: `internal/service/engine.go` formula parser/evaluator, `salary_components` (per-column formula storage) + `salary_component_overrides` (scoped override) + `payroll_formula_snapshots` (finalize-time freeze), full CRUD/validate/history stack for T0022/T0026/T0027 (confirms T0026 IS wired to a real API, not sample data), the `SetCell`/`UndoCell` manual cell-pin layer, and the `AllowanceOverrides` module (separate concern). Flags T0023 (Name Manager) as having **no dedicated backend** — "names" today are just `salary_components.code`; a real named-constant manager would be new backend work, needs a design decision first. |

---

## Security / RBAC docs (this `self-docs/` folder)

| File | Purpose |
|---|---|
| **`00-START-HERE.md`** | **ĐẦU TÀI LIỆU — mở file này trước.** Trang trạng thái + điều hướng: 5 sự thật kỹ thuật phải biết trước khi sửa gì (bảng `salary_components` phù du, backend không chạy Atlas, `migrations/vNN.sql` sống 1 boot, nhánh `feature_v2`=`origin/giatbh`, test fail sẵn), **bảng 10 lỗi đã biết chưa sửa xếp theo mức gây hại** (L1 override mất dữ liệu mỗi restart, L2 phiếu lương xếp 21 mã sai…), 3 luồng việc đang mở + việc kế tiếp, 4 quyết định đang chờ người dùng, và mục "đọc gì theo chủ đề". Cố ý KHÔNG chứa chi tiết — chi tiết ở file canonical; file canonical thắng nếu mâu thuẫn. Phân vai: file này = *đang ở đâu, làm gì tiếp*; `document-map.md` = *file X là gì*; nhật ký `CLAUDE.md` = *hôm đó làm gì*. |
| `files/prompt-F1-Task0-040826.md` (mới) | **Prompt dispatch tự chứa cho F1/Task 0**, dán làm tin nhắn đầu tiên của một session mới. Gồm: mục tiêu + vì sao gấp, 3 chỗ phải đọc trước, **11 sự thật đã xác minh kèm `file:line`** (để agent không đo lại/đoán), phương án RE-LINK đã chốt + lý do LOẠI re-key, **7 ràng buộc tuyệt đối**, **6 bẫy đã biết** (quên đường GHI, không idempotent, test đè DB dev, import cycle, chép baseline, `gofmt -w` cả thư mục), điều kiện DỪNG-và-hỏi, định nghĩa "xong" 6 điểm (có nghiệm thu restart thật), và checklist ghi tài liệu khi xong. Nguồn: PLAN `040826-...` Task 0. |
| `files/prompt-F2-Task1-050826.md` (mới) | **Prompt dispatch tự chứa cho F2/Task 1** (2 nạn nhân cùng gốc "bootstrap đặt trong Atlas"): Phần A `employee_payroll_templates` (L4), Phần B seed `salary_component_versions` (L11 — nặng hơn, làm versioning của `giatbh` vô hiệu). Nêu rõ **Phần A rẻ vì DDL Atlas vốn đã idempotent** còn **Phần B là bẫy vì backfill Atlas không có guard + dùng `created_at` nói dối**. Gồm 6 bẫy, quyết định `effective_from=now()` đã chốt (không hỏi lại), ràng buộc "KHÔNG đổi giá trị lương" kèm **nghiệm thu md5 số trước/sau seed**, và nghiệm thu Phần A ở tầng thật (rename bảng → status phải trả `healthy:false`). Baseline đo thật: **7 case fail, sau task còn 6** vì `TestIntegrationCalculateOnePointInTimeFormula` chuyển xanh. |
| `files/prompt-L12-L13-050826.md` (mới) | Prompt dispatch cho bản hoà giải `salary_component_versions` ↔ `salary_components` (L12+L13) — migration 2 câu, **thứ tự không được đảo**. Đã thi hành, commit `bf42175`. |
| `files/prompt-Task2-050826.md` (mới) | **Prompt dispatch cho Task 2** — dựng lại lớp bảo vệ mask theo quy tắc **"chỉ mask cột NGUỒN"** + `format<>'days'` + `source_field=''` + blocklist tối thiểu (phương án **A**) → đúng **66 mã**. Brief nêu tường minh **2 quy tắc ĐÃ BỊ BÁC BỎ để agent không dựng lại**: (a) "chỉ mask cột lá" (chặn đúng những cột cần mask, chỉ còn 24/140), (b) "nới cho cột formula chỉ tham chiếu cột nguồn" (lọt thêm 13 mã, chỉ 1 là `MEAL_ALLOW`, 12 còn lại gồm `BONUS_TOTAL`/`CONTRACT_TOTAL`/`PROB_SAL`). Bẫy số 1: **6 biến bơm runtime** (`CONTRACT_TYPE`…) không phải component, phải loại khỏi đồ thị. Nghiệm thu định lượng: phải ra đúng 66 mã, có `PHONE_ALLOW`/`FUEL_ALLOW`/`HEALTH_INS`, không có `GROSS`/`NET_PAY`/`BASIC_SAL`/`MEAL_ALLOW`. |
| `files/prompt-Task4-050826.md` (mới) | **Prompt dispatch cho Task 4** — sửa cổng duyệt shadow-mode `ComputeTemplateImpact`. Điểm cốt lõi: **có HAI lỗ, không phải một** — (1) `salaryOverrides` rỗng (`payroll_service.go:802`, `:809`), (2) **`empManual` truyền `nil`** (`:824`, `:825`). Lỗ 2 chỉ lộ ra SAU Task 2 vì 66 mã maskable đều có `source_field=''` nên giá trị đến từ `payroll_manual_inputs`; `Calculate` nạp ở `:253-254` còn 2 hàm impact truyền `nil` ⇒ mọi phụ cấp = 0 ở cả 2 lần mô phỏng ⇒ delta 0 tuyệt đối. **Sửa lỗ 1 một mình là "sửa mà không sửa"** và test sẽ xanh giả. Vì vậy brief bắt test **neo vào một PHỤ CẤP bị mask**, KHÔNG neo vào `BASIC_SAL` (neo vào BASIC_SAL thì xanh ngay khi sửa lỗ 1) và bắt **chứng minh test FAIL nếu revert riêng lỗ 2**. Kèm nợ ghi nhận: `ComputeFormulaImpact` cũng `nil`, không sửa kèm. |
| `files/prompt-Task3-050826.md` (mới) | **Prompt dispatch cho Task 3** (đóng L6 — picker cột không lọc gì). Chạm CẢ backend lẫn frontend. Ba yêu cầu, trong đó **yêu cầu 2 là chỗ dễ sai nhất**: cờ `maskable` phải phản ánh tập mask THỰC TẾ **`66 ∪ 6` = 72**, không phải 66 của `maskableCodes()` — lấy 66 một mình sẽ disable 6 cột tách và **cắt luôn đường bỏ phụ cấp cơm**. Yêu cầu 3: `maskReason` của `MEAL_ALLOW` phải chỉ đường qua `MEAL_TAX`/`MEAL_NONTAX`, **KHÔNG** được nói "không bỏ được phụ cấp cơm" (câu đó sai, xem mục 16.6) — có test bắt. BE: rút helper `templateMaskEligible` dùng chung cho cả guard lẫn API (một nguồn sự thật) + `templateMaskReason` + 2 field `db:"-"` điền ở `SalaryComponentService.List:116`. FE: disable + tooltip, **không ẩn cột**, cảnh báo dữ liệu cũ, tách logic thuần ra `lib/` để test được. Nêu rõ thứ ĐÃ CÓ đừng làm lại (`componentCount` `:118`, bộ đếm `:132`), FE không có script `typecheck` (dùng `pnpm build`), và bắt **kiểm tay trình duyệt** (tạm mở feature flag rồi đóng lại trước commit). |
| `files/prompt-Report-Task1-060826.md` (mới) | **Prompt dispatch cho Task 1 của PLAN `060826-report-template-config-v1-PLAN.md`** — hạng mục MỚI, KHÁC PLAN `040826` (cấu trúc lương). Migration 2 bảng `report_templates`/`report_template_lines`, khoá theo `component_code` (không FK `salary_components.id` — G1). PLAN đã qua cổng harness R7/R18 (`exit 0`, kiểm chứng độc lập), có sẵn code+lệnh+output từng bước. Brief chỉ bổ sung phần PLAN không biết trước: **mốc chữ cái `BY` phải tự kiểm lại** trước khi ghi (đã bị chiếm 2 lần liên tiếp ở PLAN kia, `BO→BV`, `BP→BW`, `BQ→BX`) — xác nhận dòng cuối slice thật vẫn là `BX` trước khi dùng `BY`. |
| `../llmwiki/wiki/sources/draft/060826-report-template-config-v1.md` (mới) | **SPEC báo cáo cấu hình-hoá v1 (Nhóm 1)** — HR tự định nghĩa template báo cáo qua UI (chọn cột `component_code`, vai trò earning/deduction/employer_cost/total/info), backend đọc thẳng `payroll_records.computed_values`, không qua engine. 13 FR (TASK-REF→013). |
| `../llmwiki/wiki/sources/draft/060826-report-template-config-v1-PLAN.md` (mới) | **PLAN thi hành SPEC trên, 7 task — ĐÃ THI HÀNH XONG cả 7/7 (060826).** Kiểm chứng độc lập trước khi chạy (harness gate-clean, 2 lỗi đường dẫn tự sửa trong PLAN đã xác nhận đúng, 30/30 mã payslip role xác nhận thật bằng SELECT). Khi thi hành bắt thêm **2 lỗi PLAN không lộ ra lúc kiểm tĩnh** (xem `Report-Template-Config-Analysis-040826.md` mục 10.2): bug scan bool→int trong test Task 1, và route Task 3 định đặt ngoài `r.Use(requireAdmin)` (lỗ hổng role — bắt bởi `TestG1AllProtectedRoutesRequireRoleGateExceptAllowlist`). Kết quả đầy đủ + bảng commit: `Report-Template-Config-Analysis-040826.md` mục 10. |
| `../llmwiki/wiki/sources/draft/110826-Core System-excel-import-vlookup.md` (mới) | **SPEC "Nhập Excel" ghi VLOOKUP theo mã NV.** Trước đó tính năng chỉ đọc-xem-trước (`applyImportedRows` là stub luôn lỗi, `VERSION_API_READY=false`), không có mapping cột/tra mã NV/validate nào. 10 FR: bắt buộc cột mã NV, khớp header cả key lẫn label, chỉ ghi cột input/manual, NV lạ bỏ qua+cảnh báo (không all-or-nothing), ô trống giữ nguyên, preview đầy đủ trước khi ghi, ghi qua API bulk mới (không lặp cell-set — sẽ hàng nghìn request), company-scope 403 toàn bộ nếu 1 mã NV ngoài phạm vi. |
| `../llmwiki/wiki/sources/draft/110826-Core System-excel-import-vlookup-PLAN.md` (mới) | **PLAN thi hành SPEC trên, 4 task — ĐÃ THI HÀNH XONG cả 4/4 (110826).** Task 1 (BE): tách `writeOverrideCell` dùng chung từ `SetCell`, thêm `BulkSetCells` (ghi override từng ô, `CalculateOne` đúng 1 lần/NV — không phải 1 lần/ô), middleware mới `RequireBulkEmpCompanyScope` (kiểm TỪNG mã NV trong `items[]`, khác `RequireCompanyScopeMatch` chỉ hỗ trợ 1 mã), route `POST /Core System/cells/bulk-set/{periodId}`. Task 2 (FE): `matchImportToGrid` hàm thuần (3 test vitest PASS). Task 3 (FE): nối UI dialog "Nhập Excel" + gọi API thật. Đã kiểm chứng end-to-end bằng `curl` trên dữ liệu dev thật (đổi `PHONE_ALLOW` 1 NV, xác nhận `GROSS` tính lại đúng + `payroll_cell_edits` ghi đúng reason, rồi khôi phục lại giá trị cũ) — **chưa click-through UI trong trình duyệt** (không có công cụ điều khiển trình duyệt trong phiên này). `go test ./...` sau: 922 passed/7 failed, khớp baseline (7 fail cũ không liên quan). Commit: `Core System-backend@9d53e6a` + `Core System-frontend@4d34e6b`, cả hai trên `feature_v2` — chưa push. |
| `../llmwiki/wiki/sources/draft/120826-super-admin-bootstrap-security.md` + `-PLAN.md` (mới) | **SPEC+PLAN "Bootstrap super-admin an toàn hơn" — ĐÃ THI HÀNH XONG cả 3/3 task (120826).** Phát hiện khi người dùng hỏi "trên prod thì ai là admin": `EnsureSuperAdmins` (`database.go`) seed cứng email `user@company.test` trong source code, chạy mỗi lần boot ở mọi môi trường kể cả prod (rủi ro TASK-REF — danh tính đặc quyền hardcode trong git). Sửa: bỏ hardcode, chỉ seed từ biến môi trường mới `INITIAL_SUPER_ADMIN_EMAIL` khi bảng `super_admins` đang RỖNG (không phải mỗi boot vô điều kiện) — bảng đã có dòng thì bỏ qua hoàn toàn dù biến có set gì, tránh set nhầm biến ở lần deploy sau vô tình cấp thêm quyền; nhánh `DEV_USER_EMAIL` giữ nguyên không đổi. Đổi chữ ký `EnsureSuperAdmins(db, devEmail, initialSuperAdminEmail)` — phải sửa kèm 4 call site test có sẵn (`pipeline_route_integration_test.go`, `admin_read_route_integration_test.go`). 4 test tích hợp mới tự snapshot/khôi phục `super_admins` (bảng toàn cục dùng chung nhiều test khác) — xác nhận bằng `psql` thủ công dữ liệu thật (3 dòng `user@company.test`/`user@company.test`/`user@company.test`) nguyên vẹn trước/sau. `go test ./...`: 913→917 passed, đúng 6 fail baseline nhánh `develop` (khác baseline `feature_v2` cũ), 0 hồi quy. Không tạo `.env.example` (chưa tồn tại trong repo, ngoài phạm vi). Runbook: `Super-Admin-Bootstrap-Runbook-120826.md`. Nhánh `fix/admin-bootstrap-security` (từ `origin/develop`, KHÔNG phải `feature_v2` — để merge sạch, không kéo theo Nhập Excel). Commit: `Core System-backend@f0dc6f3`. |
| `Super-Admin-Bootstrap-Runbook-120826.md` (mới) | Runbook vận hành: cách set `INITIAL_SUPER_ADMIN_EMAIL` khi dựng môi trường mới, SQL thu hồi email bootstrap sau go-live, SQL thêm/xoá super-admin thủ công. |
| **`Formula-Versioning-PointInTime-050826.md`** (mới, canonical) | **Hạng mục "versioning công thức theo thời điểm".** Giải thích cơ chế thật của tính năng lớn nhất nhánh `giatbh`: bảng `salary_component_versions` (khoá theo `component_code`, không theo `id`), `InsertVersion` đóng version đang mở rồi chèn version mới, và `ListActiveAsOf` là **COALESCE 3 tầng** (đúng-ngày → sớm-nhất → `sc.formula` hiện tại). **Khuyết tật L11**: bảng tồn tại nhưng **0 dòng** vì backfill chỉ nằm ở `atlas/migrations/` mà backend không chạy Atlas ⇒ luôn rơi tầng 3 ⇒ mọi kỳ tính bằng công thức HÔM NAY, đúng bug tính năng được xây để diệt. **Quyết định đã chốt (050826): `effective_from = now()`** — khái niệm "chân trời lịch sử", kèm **bảng diễn biến 6 ca** (A-F) cho thấy seed biến tầng 2 từ cái bẫy thành cái chốt. Ghi rõ 3 điều nó KHÔNG giải quyết (không tái tạo lịch sử trước chân trời; chân trời khác nhau giữa dev/staging/prod nên cùng một kỳ có thể ra số khác nhau; không thay `payroll_formula_snapshots`) và **4 bẫy thi hành** (phải `WHERE NOT EXISTS` vì bảng KHÔNG có unique trên `component_code`; **không chép file Atlas** vì nó thiếu guard + dùng `created_at` nói dối; `effective_until` phải NULL; đặt cuối slice sau `BG→BO`). Đọc file này trước khi chạm `salary_component_versions` hoặc `ListActiveAsOf`. |
| `Structure-Security-001.md` | Theory note: Hierarchical RBAC (NIST maturity levels) + ABAC refinement layer + OPA/Rego policy example. Background reading, not implementation-specific. |
| `Core System-Architecture-Roadmap.md` | Full security architecture roadmap (RBAC vs ABAC vs Hybrid, DB security options, 3-phase plan). The source roadmap that `architecture-synthesis.html` and the RBAC work below verify against real code. |
| `architecture-synthesis.html` | Gap analysis comparing the roadmap's targets against what's actually built — open in a browser. Updated 2026-07-14 to mark the department/company scoping gap **resolved**; several other gaps (token encryption, RLS, data masking, token revocation) remain open (Phase 2). |
| `RBAC-Hybrid-Scoping-Implementation-140726.md` | **New 2026-07-14, single canonical doc for this work.** Full analysis grounded in real code (schema, middleware, router) + what was actually built: schema migration, `internal/middleware/scope.go` (company + department scoping), Hierarchical RBAC, the FE/BE role reconciliation (`pm`/`site_admin`/`search_profile`), the `site_admin` role definition, the side-fix for attendance/timesheet routes with no role gate, and Phase-2/ABAC risk notes — with test evidence. An earlier `RBAC-Hybrid-Scoping-Analysis-140726.md` (pre-code draft) existed briefly and was merged into this file then deleted, once it went stale relative to what was actually implemented. |
| `RBAC-Improvement-Analysis-150726.md` | **New 2026-07-15, single canonical doc for this work.** WorkOS/IBM RBAC reference comparison, 10 gaps (G1–G10), and full analysis + implementation of 4 business security requirements (company-per-legal-entity data hiding, HR/C&B-only access, audit log old→new + mandatory reason, fine-grained View/Edit/Export/Lock/Approve-on-behalf permissions) — with test evidence for each. Section 0 is a fast-scan track note of the day's code changes. |
| `RBAC-Bao-Cao-Bao-Mat-150726.md` | **New 2026-07-15, summary report (not a working doc).** Condensed report across both 140726 and 150726: what's done, what gaps remain (referencing G1–G10), what's not yet implemented, and prioritized improvement recommendations — for quick status tracking without reading the two detailed docs above. |
| `RBAC-Improvement-Analysis-160726.md` | **New 2026-07-16, the canonical DAILY REPORT for this date (convention from today, see `CLAUDE.md`).** Part I: G1 (route-gate test-guard via `chi.Walk()` — found 2 more real high-risk gaps: Employee routes incl. bank-accounts, HRIS connectors, departments), G4 (access-review report endpoint), G9 (JML checklist), G10 (scoped agent DB role, prepared but not applied). Part II: A1 (employee upsert → `hris_id`), A2 (company-scope → FK), B1 (report-generator company-scope) — full narrative of what was implemented, cross-referencing `RBAC-Workday-Golive-Impact-160726.md` and `RBAC-Backlog-Tracklist-160726.md` for the underlying analysis/backlog rather than repeating it. Combined test evidence: 623 passed, 12 failed (same pre-existing baseline). |
| `RBAC-JML-Checklist-Van-Hanh-150726.md` | **New 2026-07-16 (implements the G9 gap analyzed on 2026-07-15, hence the file name).** Operational (not code) Joiner-Mover-Leaver checklist for HR/IT staff — 3 sections plus a periodic-review tie-in with `GET /roles/access-review`. |
| `RBAC-Backlog-Tracklist-160726.md` | **New 2026-07-16, single canonical tracklist doc, updated continuously.** Consolidates every open RBAC/security item from all 4 prior docs into one prioritized backlog, grouped by what's blocking it (ready to implement now / needs business decision first / blocked on data cleanup / pure business decision with a safe shadow-mode path / out of RBAC scope, handed to another team). **Updated same day:** A1 (employee upsert key → `hris_id`), A2 (company-scope → FK `scope_company_id`), and B1 (company-scope for report generators) implemented with migrations, code, and passing tests (zero regressions, verified against the dev DB `payroll_engine`). B2 (RequirePermission for 16 remaining modules) surveyed but deferred to a follow-up session — much larger in scope than B1 (16 separate route packages vs. one central dispatch point). Read this first when picking up the next piece of RBAC work. |
| `RBAC-Improvement-Analysis-200726.md` | **New 2026-07-20, the canonical DAILY REPORT for this date.** Bốn màn hình RBAC mới trong bản Excel (`Core System-frontend/components-page/tinh-luong`): Phân quyền (CRUD role), Gán quyền NV (gộp theo nhân viên + tìm kiếm), Ma trận theo User (đọc, OR-combine đúng ngữ nghĩa nhiều-role), Ma trận theo Role (sửa được) — cộng ẩn/hiện 4 nút này trên ribbon theo role (`hr_admin`/`config_admin` mới thấy). Phân tích ngữ nghĩa nhiều-role phát hiện 2 lỗi thật: lỗi giao diện (`roleMatrixChecked` ghi đè sai default khi Lưu ma trận theo role khác `hr_admin`) và lỗi thiết kế backend (`resolveModuleActionPermission` gộp chung dòng permission của mọi role rồi mới xét mặc định 1 lần, khiến role đã cấu hình kéo tụt role chưa cấu hình dù mạnh hơn) — cả hai đã sửa. Backend: thuật toán OR theo từng role (Hướng A) + cột `roles.priority` làm nền cho rank/priority sau này (Hướng C, chưa có UI, mặc định trung lập) + role hệ thống mới `config_admin` (IT thuần cấu hình RBAC, tách khỏi nghiệp vụ HR). Ghi chú thêm: cơ chế super-admin bypass (`super_admins` table) không phải bug, có thể gây nhầm lẫn khi test ẩn/hiện theo role. |
| `RBAC-Architecture-Reassessment-200726.md` | **New 2026-07-20, đánh giá kiến trúc tổng quan (không phải bug fix, không phải plan test).** Lùi lại nhìn toàn hệ thống permission/role sau nhiều lần vá liên tiếp (140726–200726), chỉ ra 7 rủi ro mang tính cấu trúc mà cách sửa-từng-điểm không tự lộ ra: (1) 4 cơ chế quyết định độc lập (role-hierarchy, permission opt-out+priority, company/department scope, super-admin bypass) không hợp nhất thành một authorization decision — mỗi request tối thiểu 3-4 lượt query DB rời rạc, không nơi nào trả lời đầy đủ "vì sao bị từ chối"; (2) mô hình mặc định-cho-phép (opt-out) là một lựa chọn thế trận bảo mật đang mặc nhiên giữ nguyên, ngược nguyên tắc least-privilege, gây "quyền mở rộng ngầm" cho mọi module mới; (3) `RequirePermission` (lớp mịn) chỉ che 5/~137+ route, phần lớn công sức priority/OR dồn vào đúng phần diện tích nhỏ đó, trong khi 3 nhóm route (ESS, read-only, dashboard) không có RequireRole ở tầng router — dựa hoàn toàn vào tự-lọc trong handler, đúng dạng rủi ro G1 đã bắt được 2 lần; (4) bề mặt CẤU HÌNH RBAC chính nó không có kiểm soát nội bộ — hr_admin/config_admin tự gán role/tự sửa quyền cho bản thân không bị chặn, AccessReview chỉ detective không preventive — một khoảng trống SoD mới, khác G7 (SoD nghiệp vụ Core System); (5) audit trail cho thay đổi RBAC không tồn tại và có dead code gây hiểu lầm (`RoleService.audit` field không bao giờ được gọi); deny-audit-log (G2, 150726) chỉ ghi stdout, không persist DB; (6) frontend chỉ đồng bộ role 1 lần lúc mount — độ trễ UI không riêng super-admin mà là hành vi mặc định cho MỌI người dùng bị thu hẹp quyền giữa session. Không đề xuất sửa cụ thể, thuần đánh giá để người dùng quyết định lộ trình. **Cập nhật cùng ngày (mục 9-10):** độ phức tạp 2 phương án A/B cho ưu tiên #1 (A rẻ và nên làm trước; B lợi ích nhỏ hơn ước lượng ban đầu vì role list đã cache trong context, hoãn lại); triển khai thật Bước 1 (`internal/app/permission_gate_coverage_test.go`, báo cáo coverage 10%/81%/9% tự động mỗi lần test) và Bước 2 (`internal/handler/ess_self_scope_integration_test.go`, xác nhận self-scoping thật + phát hiện 3 route ESS leave-details/timesheet-raw/attendance-calendar không hề tự lọc theo JWT như comment cũ khẳng định — chỉ sửa comment cho đúng, chưa đổi hành vi, cần quyết định thiết kế riêng). **Cập nhật cùng ngày (mục 12):** triển khai thật Ưu tiên #1 Phương án A — `logAuthzAllowed`/`logSuperAdminBypass` (`internal/middleware/access_audit.go`) ghi quyết định CHO PHÉP ở mức Debug (không đổi hành vi log mặc định) kèm `request_id` để nối các dòng log rời rạc của 4 cơ chế; bypass super-admin ghi ở mức Info (luôn hiện, đóng khoảng trống mục 5); `LOG_LEVEL` env var (`cmd/Core System/main.go`) làm công tắc bật/tắt không cần build lại. 650 passed, 9 failed (baseline cũ, không hồi quy). **Cập nhật 2026-07-21 (mục 13):** xử lý mục 4 (SoD bề mặt cấu hình RBAC) theo hướng "audit-only trước" (người dùng chọn, dữ liệu thật cho thấy có 12 hr_admin nên rủi ro khoá nhầm thấp nhưng vẫn ưu tiên đo trước khi khoá) — nối audit trail thật cho `Create`/`Update`/`UpdatePermissions`/`AssignRole`/`RemoveRole` role (`RoleService.audit` trước đây là dead code), cờ `selfReferential` đánh dấu không chặn, `UpdatePermissions` ghi cả old lẫn new toàn bộ ma trận. Phát hiện phụ: `AuditLogRepo.List` chưa trả cột `details` (old/new) ra API — giới hạn tiền tồn tại. **Cập nhật cùng ngày (mục 14):** đã sửa `AuditLogRepo.List` (giờ trả đúng old/new). Bắt đầu Ưu tiên #3 mục 3 (Phương án A) với module Employees — 30 route wire `RequirePermission`, xác nhận dữ liệu `permissions` có sẵn (cb_staff/config_admin) và cho enforce ngay theo xác nhận người dùng; coverage tăng 19/184 (10%) → 49/184 (27%). Task-list module còn lại: `RBAC-Priority3-Module-Rollout-Tasklist-210726.md`. 660 passed, 9 failed (baseline cũ, không hồi quy). **Cập nhật cùng ngày (mục 15):** module thứ 2, Roles/\* (`Settings.Roles`) — kiểm tra dữ liệu trước cho thấy KHÔNG đổi hành vi thật nào (config_admin đã full quyền, hr_admin mặc định allow-all, cb_staff/employee không chạm được route này). 9 route wire xong, coverage 49/184 (27%) → 58/184 (32%). 665 passed, 9 failed (baseline cũ, không hồi quy). **Cập nhật cùng ngày (mục 16):** module thứ 3, Departments/Companies — Departments không đổi hành vi (hr_admin allow-all); Companies chưa từng có tên module trong UI, đã thêm `"Companies"` vào `Core System-frontend/lib/rbac-constants.ts` trước khi wire backend. 9 route (4+5) wire xong, coverage 58/184 (32%) → 67/184 (36%). 671 passed, 9 failed (baseline cũ, không hồi quy). **Cập nhật 2026-07-21 (mục 17):** module thứ 4 (Config/\*, tách 5 sub-module mới `Settings.SalaryComponents`/`PITBrackets`/`TransportAllowances`/`PeriodConfig`/`EmployeeLevels`) và module thứ 5 (AttendanceDaily/AttendanceSummary/AttendanceComputed/Timesheets/AllowanceOverrides, tách `AttendanceSummary`/`AttendanceComputed`/`AllowanceOverrides` thành module mới) — cả hai đều xác nhận KHÔNG đổi hành vi thật nào trước khi wire (Config: hr_admin allow-all mọi Settings.\*; Attendance/Timesheets: cb_staff có dữ liệu cấu hình sẵn nhưng chỉ đúng action thật dùng đã = true). Route `/attendance/items`,`/attendance/records` (stub rỗng, tính năng đã gỡ) — theo quyết định người dùng, bỏ qua không wire. Coverage 67/184 (36%) → 137/184 (74%), 0 route "unknown". 689 passed, 9 failed (baseline cũ, không hồi quy). |
| `RBAC-Priority-Hierarchy-Collision-Fix-220726.md` | **New 2026-07-22, phân tích sâu + phương án khắc phục + triển khai thật cho 1 finding cụ thể** (mở rộng mục 1.2 của `RBAC-Technical-Code-Audit-210726.md`). Luồng dữ liệu đầy đủ, ràng buộc phải giữ, Phương án A (context key mới `UserDirectRolesKey`) đã chọn và triển khai. Mục 6: phát hiện phụ khi viết test — thiết kế ban đầu (gắn nhãn priority theo "sponsor") vẫn sai vì để tổ tiên không dòng nào đồng bỏ phiếu như role ngang hàng, vô hiệu hoá deny tường minh của role trực tiếp; sửa lại "role trực tiếp có dòng riêng → tuyệt đối, chỉ fallback lên tổ tiên khi im lặng hoàn toàn". `go test ./...`: 703 passed, 9 failed (baseline cũ, không hồi quy). Chưa commit. |
| `RBAC-Improvement-Analysis-110826.md` | **New 2026-08-11, canonical DAILY REPORT.** Yêu cầu: gán quyền chỉ cần điền email, không bắt buộc trỏ tới `employees`. Phát hiện mâu thuẫn với quyết định 160726 (đổi `scope_company_code`→`scope_company_id` FK vì email/chuỗi dễ đổi = mất quyền âm thầm) — chốt Phương án C (`employee_roles.email` là khoá match quyền NOT NULL, `employee_id` nullable chỉ enrich + phát hiện lệch, có báo cáo `GET /roles/email-drift`, không mất quyền âm thầm). SPEC+PLAN: `llmwiki/wiki/sources/draft/110826-rbac-email-based-role-assignment(.md/-PLAN.md)`. 7 task: migration (backfill 26/26 dòng + trigger tự điền email từ `employee_id` giữ nguyên ~15 file test insert trực tiếp), 3 điểm resolve quyền đổi JOIN→so khớp trực tiếp, API `AssignRole/RemoveRole/ListEmployeeRoles/AccessReview` theo email (lưu ý `uuid.NullUUID` không phải `*uuid.UUID` — tránh panic gọi `.Value()` qua pointer nil), endpoint báo cáo lệch, FE cho gõ email tự do + banner cảnh báo lệch. `go test ./...` 904 passed/10 failed (baseline đo lại tại chỗ, 0 lệch qua mọi Task). Kiểm tay trình duyệt thật qua Playwright (script tạm, đã xoá) xác nhận toàn bộ luồng gán/hiển thị/xoá đúng. `Core System-backend@feature_v2` 4 commit, `Core System-frontend@feature_v2` 1 commit — chưa push. |
| `RBAC-Technical-Code-Audit-210726.md` | **New 2026-07-21, code review kỹ thuật (KHÁC tầng với mọi tài liệu RBAC trước — logic/policy).** Đọc trực tiếp code thật (auth.go, permission.go, scope.go, role_repo.go, role_service.go) tìm bug/nợ kỹ thuật, không phải chính sách. 6 finding đã xác nhận bằng đọc code trực tiếp (không suy luận): `RoleRepo.Update` no-op thầm lặng trên system role kèm audit log sai (HIGH, **✅ đã sửa 220726**), hierarchy expansion (`expandRoleHierarchy`) và priority-tier vô tình dùng chung 1 danh sách role (HIGH, **✅ đã sửa 220726**, xem `RBAC-Priority-Hierarchy-Collision-Fix-220726.md`), cycle-safety của recursive CTE dựa vào `UNION` ngẫu nhiên không tài liệu hoá (MEDIUM, chưa sửa), lỗi DB trên auth path bị nuốt không log (MEDIUM, **✅ đã sửa 220726** — `slog.Error` phân biệt lỗi DB thật với `sql.ErrNoRows`/0-role hợp lệ), Assign/RemoveRole ghi audit dù no-op (LOW/MEDIUM, chưa sửa), `fmt.Printf` PII không qua slog (LOW, **✅ đã sửa 220726** — chuyển `slog.Warn`/`slog.Debug`). Đã kiểm và loại các nghi vấn khác (SQL injection, transaction UpdatePermissions, fail-open scope) — không phải bug. |
| `RBAC-Tri-Party-Comparison-210726.md` | **New 2026-07-21, so sánh 3 nguồn (nội bộ + Gemini + GPT second opinion).** Ma trận đồng thuận theo 7 rủi ro từ `RBAC-Architecture-Reassessment-200726.md`; điểm 3/3 đồng thuận mạnh (RLS BYPASSRLS, 3 route ESS `X-HRIS-Cookie` IDOR-shaped, SoD self-escalation cần chuyển từ audit sang chặn, default-allow không nên vĩnh viễn, audit log deny cần persist); điểm bất đồng cần quyết định (Gemini muốn hợp nhất 4 cơ chế NGAY qua Single Gateway Middleware, khác nội bộ+GPT muốn hoãn); hạng mục mới do 2 bên ngoài nêu (precedence permission chưa thành văn bản, route coverage ≠ authorization coverage, route classification taxonomy, CI guardrail seed row, metric `AuthzDefaultAllowFallbackTriggered`). Kết thúc bằng backlog tổng hợp P0-P3, chưa triển khai — đề xuất gộp vào `RBAC-Backlog-Tracklist-160726.md` khi bắt đầu làm. **Cập nhật 2026-07-22 (mục 7):** triển khai P0#2 (3 route ESS — TOFU self-scope cho `attendance-calendar` qua cột mới `employees.hris_portal_employee_code`, audit-log-only cho `leave-details`/`timesheet-raw` vì cookie opaque không có định danh đối chiếu), P1#1 (audit log deny persist vào `audit_logs` qua context `db` stash ở `AuthJWT`), P3#1 (tài liệu `RBAC-Permission-Precedence-220726.md`). 695 passed, 9 failed (baseline cũ, không hồi quy). |
| `RBAC-Permission-Precedence-220726.md` | **New 2026-07-22, đặc tả tường minh (specification), không phải báo cáo công việc.** Trả lời "2 quy tắc mâu thuẫn thì ai thắng" cho 4 cơ chế authorization độc lập: super-admin bypass thay thế hoàn toàn role-hierarchy (không cộng dồn); role-hierarchy là union thuần, không có precedence; permission opt-out+priority là cơ chế duy nhất có precedence phức tạp (bảng quyết định đầy đủ mọi tổ hợp priority/granted cho 2 role) — priority CẮT BỚT tập role được xét, không "cộng thêm quyền"; company/department scope là OR phẳng theo assignment, không dùng priority; giữa CÁC CƠ CHẾ xếp chồng trên 1 route luôn là AND tuyệt đối (không merge, không ưu tiên cơ chế nào). Kết thúc bằng quy trình gỡ lỗi theo thứ tự tra cứu khi debug 1 ca cụ thể. |
| `RBAC-Priority3-Module-Rollout-Tasklist-210726.md` | **New 2026-07-21, task-list riêng cho Ưu tiên #3 mục 3.** Bảng trạng thái từng module (Employees ✅ xong, Roles/\*, Departments/Companies, Config/\*, Attendance/Timesheets/AllowanceOverrides, HRIS Connector/Sync/Pipeline ⬜ chưa làm) + quy trình chung 6 bước cho mỗi module + lưu ý phải kiểm tra dữ liệu `permissions` có sẵn và tên module đã tồn tại trong `Core System-frontend/lib/rbac-constants.ts` chưa trước khi wire. Cập nhật liên tục, không tạo file mới khi làm module tiếp theo. |
| `RBAC-Phase1A-Test-Plan-200726.md` | **New 2026-07-20, artefact tham khảo (không phải báo cáo ngày).** Đối chiếu 2 file kế hoạch test do Claude web tạo (`files/RBAC-Phase1-Improvement-TestStrategy.md`, `files/RBAC-Test-Templates-and-Samples.md`, không có quyền đọc code thật) với thực tế codebase — bảng gap chi tiết (pgx vs sqlx, testify vắng mặt, uuid vs int id, `employee_id` không phải `user_id`, không có `PermissionService`/`models.User`, `roleMatrixChecked` là closure không phải pure function, endpoint thật `PUT /roles/{id}/permissions`, GitLab không phải GitHub Actions, FE chưa có hạ tầng test nào). Đề xuất Phase 1A điều chỉnh: BE tiếp tục thêm test vào `permission_integration_test.go` có sẵn (không tạo cây `tests/` mới), cần thêm test priority+scope kết hợp (khoảng trống thật); FE cần dựng Vitest từ 0 + bắt buộc tách `roleMatrixChecked` khỏi closure thành hàm thuần trước khi unit-test được; hoãn full-flow component test sang Playwright sau. Có vài quyết định còn mở (CI GitLab có thêm không, test DB dùng dev DB hay docker riêng) — chưa code. |
| `RBAC-Workday-Golive-Impact-160726.md` | **New 2026-07-16, single canonical doc for this work.** Analysis of whether switching employee/company/department data source to Workday (post-golive API sync) breaks the RBAC/company-scoping work already built. Key finding: department-scope is already architecturally sound (`scope_department_id` is a real FK, `org_structures` upserts by `hris_id`), but company-scope is NOT built the same way — `employee_roles.scope_company_code` is a string snapshot, `companies` has no `hris_id` column and upserts by `code` with `DO NOTHING`, so a Workday company-code reassignment orphans every `employee_roles` row scoped to that company at once. Same-family risk on employee upsert (`(employee_code, company_code)` key instead of the already-present `hris_id` column). User confirmed 2026-07-16 that Workday does supply a stable id (separate from the business code) for employees, companies, and departments alike — decided direction: move both employee upsert and company-scope to `hris_id`/FK, matching department-scope's already-correct pattern. Plus a real schema gap in `hris_attendance_daily` (no `org_structure_id`), flagged for the data/connector team, out of RBAC scope. Decisions recorded, no code changed yet. |
| `../llmwiki/wiki/sources/draft/230726-Core System-approve-reject-flow.md` (+ `-PLAN.md`) | **New 2026-07-23, SPEC + PLAN cho luồng Phê duyệt/Từ chối bảng lương thật.** Hai nút UI trước đây chỉ đổi state React tạm thời (không có route BE nào) — thêm `approved_at`/`approved_by`/`rejected_reason` vào `payroll_records` (độc lập với `status`/finalize), 2 route mới `POST /Core System/approve/{id}`, `POST /Core System/reject/{id}` dùng lại `RequirePermission`/`RequireCompanyScopeMatch`, audit log atomic. Seed tường minh bảng `permissions` cho `module=Core System action=approve` (tra DB dev xác nhận `hr_admin`/`site_admin`/`search_profile` chưa có dòng nào — đang "được duyệt" chỉ vì default allow-until-configured). KHÔNG đóng gap D3 (uỷ quyền/delegation) ở `RBAC-Backlog-Tracklist-160726.md` — 2 tính năng khác nhau, chỉ trùng tên gọi bề mặt. Đã triển khai xong cả 2 task code (backend `Core System-backend@409c1d3`, frontend `Core System-frontend@b784711`). |
| `../llmwiki/wiki/sources/draft/240726-formulas-tab-fe.md` | **New 2026-07-24, SPEC cho việc nối UI thật 6 nút ribbon "Công thức" (S004 T0022-T0028).** Ribbon hoàn toàn vô tri trước đó dù backend (engine công thức, CRUD `salary_components`, override, snapshot, lịch sử) đã chạy thật từ trước. 7/7 task đã code xong: `FormulasRibbon.tsx` (click wiring), `SalaryComponentModal.tsx` (Thêm/Sửa dùng chung form + validate + chèn `[MÃ_CỘT]`), `DeleteColumnDialog.tsx` (xử lý 409 `usedBy`), `FormulaHistoryList.tsx` (lịch sử đổi công thức, endpoint đã có sẵn — phản bác dự đoán "cần thêm endpoint" của `docs/formulas-name-manager-backend.md`), Task 6 ban đầu là `NameManagerModal.tsx` (Approach A3-a — "tên" = alias cho `salary_components.code`). **Cập nhật 2026-07-26 (phản hồi "nên giống UI hiện tại, không phải modal riêng"):** `NameManagerModal.tsx` đã bị xoá, thay bằng sheet nhúng "Danh sách cột lương" trong lưới chính (đọc sống `data.allComponents`, tự cập nhật sau Thêm/Sửa/Xoá) — xem chi tiết trạng thái mới nhất trực tiếp trong code (`TinhLuongExcel.tsx`: `FORMULAS_LIST_SHEET_ID`, `openFormulasListSheet`), tài liệu này không lặp lại toàn bộ diễn biến trung gian. Cùng đợt: sửa bug thật phát hiện khi merge `origin/develop` (nhãn ribbon "Trình quản lý<br>tên" khiến `textContent` không có khoảng trắng, khớp label cũ luôn `false`), thêm "Chèn hàm nhanh" (IF/IFS/SUM/MIN/MAX/ABS/ROUND) + tooltip giải thích cho toàn bộ nút riêng của tab. |
| `../llmwiki/wiki/sources/draft/240726-salary-component-db-hardening.md` | **New 2026-07-24/25, đánh giá 5 bảng liên quan `salary_components` (đóng vai trò gì, chuẩn hoá DB ra sao) + SPEC 4 hạng mục chọn triển khai ngay.** Phát hiện: `salary_components`/`salary_component_history` wiring đầy đủ; `salary_component_overrides` có backend + API client FE đầy đủ nhưng 0 dòng dữ liệu vì chưa có UI; `payroll_templates`/`template_components` hoàn toàn chưa có code Go nào (không repo/service/route/FE), không bảng nghiệp vụ nào khác tham chiếu tới. Đã triển khai: migration `Core System-backend/atlas/migrations/20260725000000_salary_components_db_hardening.sql` (3 index + CHECK constraint `config_table` + `COMMENT ON TABLE` đánh dấu 2 bảng "RESERVED") đã áp dụng thật vào `payroll_engine`; FE `SalaryComponentOverridesPanel.tsx` mới (CRUD override theo phòng ban/nhân viên, nhúng vào `SalaryComponentModal.tsx` mode edit qua cờ `canManage`). |
| `Salary-Structure-Template-Analysis-260726.md` (file này, `self-docs/`) | **Cập nhật 2026-08-04 (mục 14 + 15) — tái mở sau HOLD, tổng hợp lại kiến trúc + đối chiếu engine `giatbh`. Mục 14: 6 phát hiện mới, trong đó 3 phát hiện BÁC BỎ chính tài liệu này — (1) 'phòng thủ 3 lớp' của mask thực chất chỉ còn 1 lớp (`statutoryOverrideBlocklist` có 4 mã nhưng chỉ `PIT` tồn tại trong DB; guard `component_type='system'` khớp 0 dòng sau bản fix PIT), (2) biện pháp 'UI chỉ cho chọn cột phụ cấp' mà mục 7 tuyên bố đã có **chưa bao giờ được xây** (`allComponents.map` không lọc gì), (3) `ComputeTemplateImpact` truyền `salaryOverrides` rỗng nên cổng duyệt shadow-mode luôn báo 'không có tác động'. Mục 15: đối chiếu 15 nhóm thay đổi engine của `giatbh` — tương thích ở versioning as-of, nhưng lộ 2 khuyết tật hạ tầng chí tử (15.2, 15.3), 1 lệch nguyên tắc (15.5 — thành viên template không có ngày hiệu lực trong khi công thức và assignment đều có), và phân tích sâu 2 phương án mô hình gán (15.7) theo yêu cầu người dùng. PLAN thi hành: `040826-Core System-template-on-giatbh-engine-PLAN.md`.** Cập nhật 2026-07-27 (mục 7/9/10/11/12/13) — vẫn là phân tích kiến trúc canonical, KHÁC RBAC, cho chủ đề "cấu trúc lương theo cấp bậc". TOÀN BỘ PLAN (Task 3.5→7) đã xong + commit/push (mục 11). Mục 12 mới: review UX sau khi lên UI thật phát hiện 3 gap (không xem được danh sách assignment, banner rò rỉ nội bộ + ngụ ý sai hành động, endpoint shadow-mode Task 5.2 chưa có UI nào gọi) — PLAN riêng đóng gap: `270726-Core System-template-audit-and-preview-ui-PLAN.md` (chưa code, để phiên sau).** Mục 3-6 (gốc): đối chiếu 3 hệ thống Core System thật (Frappe/ERPNext, Odoo, Oracle HCM — mô hình 3 tầng Component→Structure→Structure Assignment) với engine dự án; xác minh `employee_level_id` đủ tin cậy làm khoá (Approach A2). Mục 7: Approach A1 chốt = mask-về-0, quy tắc chọn cột mask đã qua **3 vòng thiết kế khi thi hành** (v1 closure-only — lỗi zero-hoá cột TỔNG GROSS/PIT/NET, phát hiện trước khi chạy thật → v2 hardcode universe — an toàn nhưng sai kiến trúc → **v3 union-of-templates data-driven, bản đang chạy**). Mục 9: Task 3.5→4.6 (mask-về-0 core), 5.1 (kill-switch `PAYROLL_TEMPLATE_ENFORCE`, phát hiện phụ: enforce đã LIVE không có công tắc tắt trước khi làm task này), 5.2 (shadow-mode `GET /Core System/template-impact/{periodId}`, phát hiện phụ: draft đầu đọc env global sẽ làm bản mô phỏng vô tác dụng — sửa bằng field `calcShared.templateEnforce`), 6 (FE — phát hiện phụ: PLAN giả định sai hợp đồng API `request<T>`) đều đã code/test/verify xanh trên `feature_1` (`Core System-backend`+`Core System-frontend`). Mục 10: phát hiện phụ ĐỘC LẬP — bug PIT luôn = 0 toàn hệ thống, **đã sửa cùng ngày** (xem `PIT-Formula-Fix-270726.md`). |
| `PIT-Formula-Fix-270726.md` (mới, `self-docs/`) | **Tài liệu canonical riêng cho bug PIT luôn = 0 (KHÁC chủ đề với file trên — không phải tính năng cấu trúc lương). Nay gồm cả việc cập nhật `pit_brackets` (mục 5).** Nguyên nhân: `CalculateInputs.PeriodStart`/`PeriodEnd` chưa từng được gán → `filterActivePITBrackets` luôn lọc rỗng bracket thật (`effective_from=2009-01-01` ≠ zero-value) → PIT luôn 0. Không phải hồi quy (nguyên văn từ commit đầu tiên `47753b4`). Quyết định: chuyển PIT từ `component_type='system'` sang `formula` (không chỉ vá `PeriodEnd`) — dùng bảng bậc thuế 5 bậc (0-10/10-30/30-60/60-100/>100tr @ 5/10/20/30/35%) THEO DỮ LIỆU HR CUNG CẤP, đã xác nhận khớp Luật Thuế TNCN 2025 Điều 9 qua tìm kiếm thật (KHÁC bảng `pit_brackets` 7 bậc cũ trong DB theo luật cũ). Nhánh expat/intern trong công thức HR hoãn lại (thiếu dữ liệu + cú pháp engine không hỗ trợ `=TRUE`/so sánh chuỗi). Migration `20260727000000_pit_system_to_formula.sql` đã áp dụng DB dev; dọn sạch code chết; test cập nhật. Không recompute 128 record `finalized` kỳ 05/2026 (theo yêu cầu). **Mục 5 (270726, mới):** migration `20260727010000_pit_brackets_2026_law.sql` — additive INSERT 5 bậc mới `effective_from=2026-01-01` vào `pit_brackets`, giữ nguyên 7 bậc cũ, đã áp dụng DB dev (0 màn hình FE đang đọc bảng này, xác nhận bằng grep). |
| `../llmwiki/wiki/sources/draft/260726-salary-structure-usage-and-grading.md` | **Cập nhật 2026-07-27 — Task 3/4/5 đều chuyển `[x]` (toàn bộ SPEC đã xong, kể cả FE).** Phần A (Task 1, hướng dẫn override) và Task 2 (xác minh `employee_level_id`) đã xong từ 260726. Task 3 (CRUD backend + FE UI — `PayrollTemplateModal.tsx`/`EmployeeTemplateAssignPanel.tsx`) xong. Task 4 (Approach A1 = mask-về-0 + kill-switch + shadow-mode) xong. Task 5 (tài liệu) xong — chính là đợt cập nhật này. Xem phân tích đầy đủ ở `Salary-Structure-Template-Analysis-260726.md`, kế hoạch thi hành ở PLAN cùng tên. |
| `../llmwiki/wiki/sources/draft/270726-Core System-template-audit-and-preview-ui-PLAN.md` (mới) | PLAN nối tiếp file trên, phát sinh từ 1 phiên review UX sau khi Task 6 lên UI thật. 4 task, KHÔNG có migration DB: A (backend — `GET /config/Core System-templates/{id}/assignments` liệt kê NV theo template + `GET /Core System/template-enforce-status` đọc trạng thái kill-switch thật), B (FE — hiện danh sách NV đang gán trong `PayrollTemplateModal`), C (FE — màn hình mới "Xem trước tác động" nối `GET /Core System/template-impact` lần đầu, endpoint này trước đó 0 UI nào gọi), D (FE — sửa 2 banner dùng trạng thái enforce thật thay vì text tĩnh, bỏ tham chiếu nội bộ "Task 5/PLAN 260726"). Chưa code — để phiên chat mới thực hiện. |
| `../llmwiki/wiki/sources/draft/040826-Core System-template-on-giatbh-engine-PLAN.md` (mới) | **PLAN tái mở tính năng sau khi HOLD từ 270727, trên nền engine của `giatbh` (người dùng chốt: engine họ là chuẩn chính).** 10 task; Task 0–6 làm được ngay, Task 7–8 chờ người dùng chốt. Hai task GATE là **khuyết tật hạ tầng mới phát hiện, không có trong bất kỳ tài liệu cũ nào**: (0) `template_components` FK CASCADE vào `salary_components.id` nên bị `migrationSalaryComponentsV4` xoá sạch **mỗi lần backend boot** — chính comment của V4 ghi `-- STEP 1: XÓA TOÀN BỘ (cascade xóa template_components)`; (1) `employee_payroll_templates` chỉ có trong `atlas/migrations/` mà **backend không bao giờ chạy Atlas** → môi trường sạch thiếu bảng, `loadTemplateMaskData` nuốt lỗi nên enforce âm thầm tắt trong khi status vẫn báo `enabled:true`. Task 2–4 vá 3 lỗ an toàn của mục 14.6; Task 5 bật `PAYROLL_TEMPLATE_ENFORCE=on` **chỉ dev local** (quyết định người dùng); Task 6 mở lại UI + đóng 3 lỗ UX; Task 7 (mô hình gán A/B) và Task 8 (ngày hiệu lực cho thành viên template) chờ quyết định. Chưa code dòng nào.
| `Report-Template-Config-Analysis-040826.md` (mới, `self-docs/`) | **Canonical cho hạng mục "bảng cấu hình template báo cáo" + tái dùng rule cho payslip (bắt đầu 040826).** Mục 0: **phát hiện nền** — `salary_components` là bảng PHÙ DU, `migrationSalaryComponentsV4` chạy `DELETE FROM salary_components` mỗi lần backend boot; đo được: 110 dòng cùng `created_at` trong 4 ms, 11/11 dòng history mồ côi, 13 mã của `v51_*.sql` đã mất sạch, 15/18 dòng `salary_formula_configs` trỏ mã không tồn tại; **2 bảng CASCADE bị xoá mỗi restart** gồm `salary_component_overrides` (tính năng ĐANG CHẠY THẬT, đọc ở 4 điểm tính lương) và `template_components`. Mục 1: báo cáo hiện có **2 thế hệ / 20 báo cáo**, 17 template `.xlsx` nhúng `go:embed`, mã component hardcode rải rác 15 chỗ, **không có bảng cấu hình báo cáo nào**, và **không có endpoint preview** (UI export `.xlsx` rồi parse lại bằng `exceljs`). Phân loại Nhóm 1 (13/20, một dòng mỗi NV — cấu hình hoá được) vs Nhóm 2 (5/20, pivot/`AllocRatio`/đa sheet — không). Mục 3-4: khuyến nghị **bảng dòng-bố-cục riêng** có `doc_type` + `role`, khoá theo `component_code`; đã soi và loại 3 ứng viên tái dùng (`template_components` bị CASCADE, `salary_formula_configs` là bảng chết 0 code đọc, `hris_payroll_reports` không liên quan). Mục 8: trả lời "dùng được code như payslip không" — ĐƯỢC, hai bên vốn cùng đọc `payroll_records.computed_values` theo code; kèm **bug thật chưa sửa**: `PayslipSummaryModal` phân loại thu nhập/khấu trừ bằng đoán chuỗi con, xếp **21 mã sai** vào cột thu nhập (gồm 11 mã chi phí công ty `_CTY` và `ADVANCE_1` tạm ứng). Mục 9: quan hệ với PLAN template — phân tích này đã được đưa vào PLAN đó ở 3 chỗ (mở rộng Task 0 sang 2 bảng, bổ sung bằng chứng cho G1, thêm G5 cấm đổi semantics V4), báo cáo sẽ có PLAN riêng. |
| `Task7-DB-Source-BanGiao-DoiDB-310726.md` (mới, `self-docs/`) | **Cập nhật 2026-07-31 sau khi restore dump DB thật (mục 14 của PLAN):** DB `payroll_adapter` thật có **46 bảng + 9 VIEW**, trong đó **9 view + 3 bảng KHÔNG nằm trong 29 migration nào của repo** — `ts_attendance_daily_pivot` (một dòng mỗi (NV,ngày), một cột numeric mỗi tag → đúng hình dạng cho lưới `Workdays1..31`), `ts_time_off_plan_balances_pivot`, `ts_ot_reconciliation` (tính bằng GIỜ), `ts_worker_job_change_log/_diff`, `ts_worker_organization_change_diff`, `ts_worker_compensation_change_log/_diff`, `ts_attendance_daily`, cộng `raas_ot_actual_blocks`/`_tags`. Chúng trả lời gần hết mục V1–V3 của tài liệu bàn giao. Đã tự chạy 3 truy vấn V1: **19 tag thật**, `calculated_quantity` tính bằng **NGÀY** (0.5/1.0) trong khi hậu tố mã hiển thị (`L-8`/`TASK-REF`/`x8-4`) trông như GIỜ → V1 thu hẹp nhưng chưa đóng. **Hai ràng buộc mới:** chỉ có ĐÚNG 1 kỳ `2026-06-21→2026-07-20` (3.768 NV) nên Task 7 phải bỏ kỳ 03/2026 mà test đang dùng; và lưới ngày chỉ phủ **227/3.768 NV (~6%)**. **Tài liệu BÀN GIAO cho người phụ trách DB của `Core System-adapter` (2026-07-31) — viết cho NGƯỜI đọc, văn xuôi đầy đủ, không phải brief cho agent.** Artefact đi kèm `BangCong-Xlsx-Template-Golang-310726.md`, dùng khi cần đội DB cấp nguồn cho Task 7. Nội dung: (1) ranh giới hợp đồng — đội DB chỉ cần trả `Row map[string]any` khoá bằng **mã field** (116 mã nằm sẵn ở dòng 7 của template, không phải tên cột Excel), không cần biết gì về Excel, và đổi cấu trúc DB chỉ ảnh hưởng đúng 1 file Go; (2) **V1 = hạng mục CHẶN TOÀN BỘ: bảng quy đổi tag Workday → mã chấm công** (`x8-4`, `L-8`, `TASK-REF`, `TS/2-2`…) chưa ai định nghĩa — kèm **3 truy vấn SQL sẵn để chạy**, trong đó truy vấn (3) `GROUP BY (tag, calculated_quantity)` là chìa khoá vì mã hiển thị có vẻ mã hoá cả số giờ; (3) V2 = 19 mã nhóm A đã map từ `raas_monthly_attendance` (3 cột số + 35 field trong `raw` JSONB) cần xác nhận, có nêu 2 cặp field dễ lẫn (`Sick_Leave___30_Days_` vs `Sick_Leave_____30_Days_` chỉ khác số dấu gạch dưới); (4) V3 = bảng ứng viên nguồn cho nhóm C; (5) V4 = 6 câu hỏi kỹ thuật (chiến lược delete+reinsert của `raas_monthly_attendance` và rủi ro đọc trúng lúc sync, độ ổn định của key JSONB kiểu `Off_in_Lieu_83246575`, `hire_date` đang là TEXT, phạm vi kỳ/công ty/nhân viên); (6) yêu cầu đội DB thông báo khi đổi schema để cập nhật test coverage mã field; (7) **cảnh báo F2** kèm yêu cầu phân biệt 3 trạng thái cho mỗi mã: có nguồn ngay / sẽ có (cho mốc thời gian) / **vĩnh viễn không có** (để ẩn hoặc đánh dấu cột thay vì chờ). |
| `BangCong-Xlsx-Template-Golang-310726.md` (mới, `self-docs/`) | **PLAN canonical: build template Excel "Bảng chấm công CBNV" bằng Go + excelize trong `Core System-adapter` (2026-07-31). Chưa code dòng nào.** Đặt ở `self-docs/` (không ở `sources/draft/`) theo tiền lệ `Salary-Column-Sync-Impact-280726.md` — quy ước local cấm companion `.html` mà R7 lại đòi. 4 quyết định đã chốt: sheet `202603_Bang_Cong,_Phu_Cap_Repor` (tab **nhìn thấy** thứ 2 — file có 4/7 sheet ẩn nên "sheet thứ 2" nhập nhằng), phạm vi khung + đổ dữ liệu từ DB, **giữ công thức Excel sống**, đặt ở `Core System-adapter`. Số liệu đo thật bằng openpyxl (đính chính con số "~60 công thức" tôi đưa lúc hỏi): **271 cột, 76 merge, 1.081 ô công thức / 51 dạng, 123 công thức mỗi dòng nhân viên**, 22 tổ hợp fill **trong đó 15 dùng màu theme+tint**, 13 định dạng số. **Phát hiện quyết định kiến trúc:** (a) lưới ngày là **4 dải 31 cột** nối nhau bằng công thức (`EU–FY` đầu vào → `FZ–HD` → `HE–II` → `M–AQ` hiển thị) nên chỉ cần đổ dữ liệu vào **31 cột**, 93 cột còn lại tự chạy; (b) **dòng 12 đã sẵn là dòng mầm** (đủ 123 công thức, không dữ liệu, không `#REF!`) → phương án = nhúng template đã làm sạch bằng `go:embed` + nhân bản dòng mầm, **không dựng lại style bằng code** (15 fill theme+tint không có API excelize và sai màu thì không test nào bắt được); (c) **không tin `DuplicateRow` của excelize** mà tự viết `ShiftRows` có test — rủi ro số một là regex dịch số dòng quét mù làm hỏng chuỗi literal `"L-10"`/`"x8-4"`/`"TASK-REF"` (mã chấm công nằm trong nháy kép). **Lỗi có sẵn trong file gốc đã phát hiện:** 10 ô `#REF!` (`E13:E17`, `DS13:DS17` — nằm hết ở 5 dòng mẫu sẽ xoá), 4 mã field hỏng vì find/replace (`Workdays{r}`, `Workdays1{r}`, `Workdays2{r}`, `Seniority{r}` — chữ `9` bị thay bằng `{r}`, quên xử lý là trượt 4 cột), và file chứa **PII thật** (5 họ tên + mã NV) nên template nhúng vào repo phải làm sạch. Bản đồ dữ liệu chia 3 nhóm theo dòng 4 của chính file (`WD` 103 cột / `Core System` 6 / `Core System tính thực tế` 4 / `??? Not available` 2): nhóm A có nguồn ngay từ `raas_monthly_attendance` (38 field trong `raw` JSONB), nhóm B phải tính (lưới ngày từ `wd_calculated_time_blocks` + tags — **bảng quy đổi tag → mã `x8-4`/`L-10` CHƯA ai định nghĩa**), nhóm C chưa có nguồn → **để trống, cấm điền 0** (điền 0 làm công thức tổng ra số sai mà trông hợp lệ). 10 task (Task 0 là cổng spike kiểm excelize có giữ được màu theme không) + **mốc M1 = Task 0+1** ("dựng xong template, chưa nhân dòng, chưa nối DB" — có ghi rõ vì sao KHÔNG tách được mốc "template không có công thức": nhúng file gốc thì 1.081 công thức đi kèm sẵn), 9 ràng buộc bao trùm, 5 câu hỏi còn mở. **Mục 6.1 ghi lại một lần sửa brief giữa đường:** bản đầu định làm sạch template bằng `sanitize.py` (openpyxl) — tự phá phép kiểm của chính mình, vì Task 0 chứng minh *excelize* không phá file mà template thật lại do *openpyxl* ghi ra thì chứng minh xong không nói được gì về file thật. Đã thành ràng buộc **G9: một công cụ GHI (excelize/Go), một công cụ KIỂM (openpyxl, chỉ đọc), không đảo vai** — cũng khớp thực tế Python không tồn tại trong runtime image distroless của adapter. **Mục 8 = kết quả Task 0+1 (mốc M1, đã kiểm chứng chéo). Mục 9 = kết quả Task 2+3+4+4.5 (2026-07-31, 31 test pass):** `ShiftRows` (13 case gồm 3 bẫy nháy kép) · `ExpandRows` (mầm 12 giữ nguyên, dữ liệu 13…12+n, footer giãn `JB2:JB<12+n>`) · `SetPeriod` (neo 1 ô, kỳ 21→20) · `RecalcAll`. **Chuỗi `input → formula → số` đã chạy thật, xác nhận bởi HAI engine độc lập** (excelize + LibreOffice headless, `soffice` có trên máy nên phép kiểm chéo không bị skip) khớp số tính tay kể cả ca âm. **Hai lỗi phiên chủ tìm ra khi kiểm độc lập, CHƯA SỬA:** (D1, nặng) `RecalcAll` **phá 295 ô công thức** ở dòng 6–9 vì **416/464 công thức template là SHARED formula** — `SetCellFloat` lên ô chủ xoá cả nhóm, ô thành viên sau đó `GetCellFormula` trả rỗng nên bị `continue` bỏ qua → **lưới ngày đóng băng**, phá cam kết "neo 1 ô cả lưới tự đúng"; sửa bằng cách đọc hai lượt (đọc hết công thức trước khi mutate). (D2, TB) `SetCellFormula` ép `t="str"` nên **mọi giá trị cache là text** (`AS13` đọc ra `'22'` chuỗi, không phải số). Hạn chế có chủ đích: công thức trả text/`""` không cache được do **rò chỉ số shared-string** (tái hiện: cache `"22"` hiện ra `684` = index bảng chuỗi) → **cột lưới ngày `M–AQ` sẽ không bao giờ có cache**. **Mục 11 = phiên 3: sửa D1/D2 + Task 5 + Task 6 (43 test pass).** D1 đã sửa (đọc hai lượt), kiểm chứng mạnh hơn test của agent: **so nội dung 314 ô lưới ngày, 0 lệch** (`O9=DAY(O6)` offset đúng từng ô — test của agent chỉ khẳng định "còn chứa DAY", chưa loại trừ rủi ro excelize chép nguyên văn thân ô chủ sang ô thành viên). D2 chấp nhận (không có API công khai vừa giữ `<f>` vừa đúng kiểu số), có test khoá hành vi. Thêm `Diagnostics` cho quirk `#DIV/0!`-kèm-giá-trị-đúng (xảy ra ở MỌI dòng thiếu mẫu số, không riêng dòng mầm — `EK`/`EO` chia cho `StdWorkDay`). Task 5 (`Render`+`FixtureSource`+`Source` interface) và Task 6 (116 mã field, 4 mã hỏng `{r}` chuẩn hoá, 0 clash) xong. **Mục 11.8 — hai phát hiện mới:** (F1) **kỳ tháng 2 rò 2 cột hiển thị ngày ngoài kỳ** (`AP`/`AQ` = 22-23/3) vì nhánh guard `IF(DAY(AQ6)=21,…)` chỉ đúng cho tháng 30 ngày — hạn chế template gốc; kèm việc **verify 3 kỳ của Task 4 chưa từng chạy** và chính PLAN ghi sai giả định đó (đã đính chính). (F2, ảnh hưởng cam kết sản phẩm) **G2 không bảo vệ cột DẪN XUẤT**: để trống vẫn ra 0 vì Excel coerce blank→0, nên `BJ=+22` vô nghĩa mà trông hợp lệ và nhóm C làm `BW/CF/DR/EG/EK=0` cho mọi dòng — báo cáo nói "Thành tiền tất cả PC = 0" như câu trả lời thật; cần **quyết định người dùng**, không tự quyết. Task 7 (DB) chặn bởi Docker + bảng quy đổi tag; Task 8/9 chưa làm. |
| `Core System-Engine-Rule-Flow-290726.md` (mới, `self-docs/`) | **Explainer canonical: "engine tính lương theo rule và bảng lương hoạt động thế nào" (2026-07-29).** Không phải báo cáo triển khai — đọc trực tiếp `engine.go`/`payroll_service.go`/`payroll_scoped_resolver.go`/`payroll_template_mask.go`/`payroll_record_repo.go` + truy vấn DB dev thật, không dựa vào tài liệu cũ. Nội dung: rule là DỮ LIỆU (`salary_components.formula`), engine Go chỉ thông dịch — topological sort theo `[MÃ_CỘT]` (KHÔNG theo `seq`), fail-soft (công thức lỗi → log + gán 0, không dừng lượt tính), mã cột lạ → 0 im lặng; cú pháp đầy đủ được hỗ trợ (7 hàm, so sánh, chuỗi) và những gì KHÔNG có (vòng lặp, biến tạm, so sánh chuỗi, hàm ngày); pipeline `Calculate` 8 bước + point-in-time tại ngày 20 (level/phòng ban/người phụ thuộc/template); thứ tự ghi đè map giá trị 5 tầng, `BASIC_SAL` không đọc từ `employees` mà từ bảng HR upload; override phạm vi (nhân viên > phòng ban) + cache engine khoá `tplSig\|sig`; mask-về-0 theo cấu trúc lương (universe data-driven, kill-switch mặc định OFF). Có bảng chuỗi rule THẬT 4 tầng trích từ DB (`PAID_DAYS`→`EARNED_SAL`→`GROSS`/`TAXABLE_GROSS`→`TAXABLE_INC`→`PIT`→`NET_PAY`), trong đó `PIT` là `MAX` của 5 đường thẳng (không đọc `pit_brackets`). Phần bảng lương: `computed_values`/`base_values` JSONB (thêm cột không cần migration), lưới lấy DÒNG từ `attendance_summary` đã chốt chứ không từ `payroll_records`, `SetCell` ghi override `cell_pin` priority 1000 rồi `CalculateOne` chứ không ghi thẳng kết quả, `Finalize` snapshot công thức. Kết thúc bằng bảng 11 ngộ nhận thường gặp ↔ thực tế. Dùng làm điểm khởi đầu cho người mới đọc phần tính lương. |
| `Salary-Component-Modal-Type-Gating-290726.md` (mới, `self-docs/`) | **File canonical cho hạng mục "điều kiện lưu của `SalaryComponentModal` theo loại cột" (2026-07-29) — hạng mục RIÊNG, không phải phần tiếp của `Salary-Column-Sync-Impact-280726.md`.** Modal "Thêm cột lương" chặn **4/5 loại cột**: `canSubmit` đòi `formula.trim() !== "" && validation.checked` vô điều kiện cho mọi `componentType`, trong khi `input`/`config`/`manual`/`system` không có công thức nào để nhập; tệ hơn `runValidate()` thoát sớm khi công thức trống nên `validation.checked` vĩnh viễn false — **ngõ cụt kín, không thao tác nào gỡ được**, và nút Lưu chỉ xám câm không nói lý do. Backend `SalaryComponentService.Create` không validate formula → rào thuần frontend. Xác nhận **không phải hồi quy của đợt 280726**: dòng đó có từ `64f90a5` (24/07, đợt S004 gốc), commit `e81bb22` chỉ thêm import/prop/panel. **Sự thật kỹ thuật đáng nhớ (đính chính một nhận định sai giữa chừng):** engine **CÓ** dùng `Formula` cho `input`/`config`/`manual` làm giá trị mặc định khi ô chưa có dữ liệu, nhưng `buildOrder` **chỉ** trích phụ thuộc cho loại `formula` (`engine.go:86`) → công thức trên cột input chạy không đảm bảo thứ tự topo. Vì vậy khi ẩn ô công thức phải gửi `formula: ""`, không được giữ state ẩn. Engine **không có** `case "system"` lẫn `default:` (gỡ từ 27/07 khi chuyển PIT sang formula) → cột `system` không bao giờ có giá trị; người dùng chọn GIỮ loại này, đã thêm cảnh báo nền vàng nói thẳng. Dữ liệu thật lúc sửa: 41 cột `formula` (đều có công thức), 69 cột `input` (**0** có công thức), 0 cột `config`/`manual`/`system` → thay đổi không phá gì. 3 thay đổi: gắn yêu cầu công thức theo loại · ẩn khối công thức + ghi chú nguồn giá trị theo loại + footer liệt kê điều kiện còn thiếu · không gửi công thức còn sót. Commit `Core System-frontend@b4216c4` + `@fd01c9e`, chưa push. Còn nợ: chưa kiểm tay trình duyệt, không có test tự động (vitest repo chỉ có `environment: "node"`, không mount được component). |
| `Salary-Column-Sync-Impact-280726.md` (`self-docs/`) | **SPEC + file canonical — ĐÃ THI HÀNH XONG 9/9 task ngày 2026-07-28 (mục "Kết quả thi hành": bảng 7 commit thật, bằng chứng test, 4 chỗ PLAN ghi sai đã sửa, việc còn nợ)**. **Cùng ngày có thêm mục "Vòng kiểm chứng độc lập ở tầng HTTP thật" — gọi 3 route mới bằng `Authorization: Bearer dev` (`auth.go:205`, không cần trình duyệt/Azure) và tìm ra 2 lỗi mà `go test` + `go vet` + `tsc` + `eslint` + `next build` ĐỀU XANH không bắt được: (1) `usage` trả `null` thay vì `[]` cho `overrideRefs`/`templates` → 6 chỗ FE gọi `.length`/`.map` crash render ở đúng trường hợp phổ biến nhất (cột không override, chưa có template nào) — kiểu TypeScript khai mảng không-nullable nên type nói dối về API thật; (2) `ComputeFormulaImpact` quét mù mọi key bằng `asFloat()` → ~100k dòng log cho 1 lần bấm nút ở kỳ 12k người, kèm nội dung hardcode `[PIT] TAXABLE_INC` vu oan cho PIT. Đã sửa cả hai (2 lớp cho lỗi 1), 3 test mới khoá bất biến ở tầng JSON thật và tầng log. Task 8 Step 3 đã đóng: md5 toàn bộ `computed_values` + count + max(updated_at) y nguyên trước/sau khi gọi thật. Commit `Core System-backend@9fe9147`, `Core System-frontend@f8b9bb0`.** Ban đầu là SPEC status proposed. File canonical cho hạng mục "đồng bộ cột lương giữa 2 sheet + kiểm tra tác động khi sửa rule" — KHÁC chủ đề với nhóm template đang hold.** Nằm ở `self-docs/` chứ không ở `sources/draft/` vì quy ước máy local cấm tạo companion `.html` mà rule R7 lại đòi (ghi rõ lý do trong file). Nội dung: 12 sự thật kỹ thuật đã xác minh trực tiếp từ code — trong đó (a) hai sheet vốn đã dùng chung 1 lần fetch nên phần "thêm/xoá rule thì cột bên kia theo" ĐÃ chạy sẵn, (b) `ON DELETE CASCADE` làm xoá cột lương xoá sạch override + gỡ khỏi mọi template im lặng (`baseline.sql:2490`/`:2498`), (c) `seq` là thứ tự chung của 2 sheet nhưng không sửa được ở đâu, kéo-thả chỉ sống trong phiên, (d) `BASIC_SAL` chỉ có trong sessionStorage nên gọi `Calculate` body rỗng sẽ zero-out cả kỳ, (e) `ComputeTemplateImpact` truyền `salaryOverrides` rỗng → báo cáo tác động nói "không có tác động" sai (bug tiền tồn tại, KHÔNG sửa vì nhóm template đang hold). 9 task, 3 route mới (`GET /config/salary-components/{id}/usage`, `PUT /config/salary-components/reorder`, `POST /Core System/formula-impact/{periodId}`), không migration. 5 quyết định đã chốt với người dùng qua `AskUserQuestion`, gồm quyết định KHÔNG thêm nút tính lương/upload lương vào UI Excel. |
| `../llmwiki/wiki/sources/draft/280726-salary-column-sync-and-edit-impact-PLAN.md` (mới) | PLAN thi hành cho SPEC `Salary-Column-Sync-Impact-280726.md` (SPEC nằm ở `self-docs/`, không ở `sources/draft/` — lý do ghi trong SPEC). 9 task chia thành 4 lát cắt dọc độc lập: A xoá-cột-nói-hết (Task 1 endpoint `usage` → Task 3 dialog), B thứ-tự-dùng-chung (Task 2 endpoint `reorder` ghi `seq` → Task 5 kéo-thả lưu thật), C nhãn-chưa-tính (Task 4, FE-only), D xem-trước-tác-động (Task 6 endpoint `formula-impact` → Task 7 panel). Task 8 kiểm chứng tổng hợp, Task 9 tài liệu. Không migration DB. **Ba ràng buộc bắt buộc phát sinh từ phân tích `BASIC_SAL`** được ghi thành mục riêng trong `## Global constraints` (Task 4 không được thêm nút tính lương; Task 6 phải truyền `salaries` vào cả hai lần mô phỏng + trả 422 khi rỗng; Task 7 rỗng thì chỉ hiện phần tĩnh) — chúng là hệ quả kỹ thuật, không phải gợi ý. Chủ động KHÔNG sửa bug `ComputeTemplateImpact` (`payroll_service.go:713`/`:720`) vì nhóm template đang hold, chỉ comment chỉ thẳng vào dòng gây lỗi. **Đã thi hành xong toàn bộ 9 task ngày 2026-07-28** — kết quả, commit và các chỗ PLAN ghi sai so với code thật nằm ở mục "Kết quả thi hành" của SPEC `Salary-Column-Sync-Impact-280726.md`, không lặp lại ở đây. Ba chỗ PLAN dặn "grep xác minh đừng đoán" đều đúng là sai nếu đoán (`toFloat`→`asFloat`, `uuidParse`→không tồn tại, `PayrollPeriod.status`→có thật). |
| `../llmwiki/wiki/sources/draft/260726-salary-structure-usage-and-grading-PLAN.md` | **Cập nhật 2026-07-27 — TOÀN BỘ Task 3.5→7 ĐÃ XONG (code+test+verify).** ⚠️ Nội dung Task 4.2 (thuật toán mask v1, closure-only), Task 4.5 (snapshot bằng SQL thô), và Task 5 (route `/periods/{id}/template-impact` + gate `requireSalaryComponentsView`) trong file **đã bị thay khi thi hành thật** — cả ba có lỗi/hạn chế phát hiện lúc code (Task 5: route/gate không khớp code thật của nhóm `/Core System`), xem ghi chú "Cập nhật 270726" ở đầu mục Task 5 + `Salary-Structure-Template-Analysis-260726.md` mục 7/9 để lấy bản cuối. Task 4.3/4.4/4.6 và Task 6 giữ nguyên đúng thiết kế đã viết. Phát hiện phụ khi làm Task 4.6: bug PIT luôn = 0 toàn hệ thống, độc lập, **đã sửa cùng ngày** (`PIT-Formula-Fix-270726.md`). |

---

## Testing docs

| File | Purpose |
|---|---|
| `Core System-Test-Suite-Handoff-100826.md` (mới, canonical) | **Bàn giao bộ test 7 phân hệ Core System (Formula Engine, Core System Template, Salary Override, Core System Period Flow, RBAC, Bảng chấm công, Report Template Config).** 74 kịch bản test tay (2 file Excel Blind/Answer mỗi phân hệ, `files/*-TestCase-{Blind,Answer}-100826.xlsx`) + 33 test Playwright tự động (`Core System-frontend/e2e/*.spec.ts`). Gồm: hướng dẫn chạy lại từ đầu, quy trình đặc biệt cho RBAC (phải restart backend + xoá dòng auto-seed `super_admins` — `DEV_USER_EMAIL` không đổi được theo request), 4 phát hiện lệch giữa giả định cũ và code thật (override panel mất đường vào UI, lệch mã lỗi 400/500 giữa CellSet/CalculateOne, ReportTemplateConfig TC04 chặn lúc Chạy không phải lúc Lưu, Bảng chấm công không có UI/HTTP ở nhánh hiện tại), và danh sách việc cần test tay/còn thiếu. Viết vì người phụ trách tạm rút khỏi dự án — giả định người đọc không có context gì từ phiên làm việc trước. |
| `../llmwiki/html/100826-Core System-test-summary.html` (mới) | Trang tổng kết trực quan cùng nội dung trên (mind map, bảng số liệu theo phân hệ, sơ đồ luồng thiết kế kịch bản) — bổ sung cho file `.md` canonical ở trên, không lặp chi tiết kỹ thuật. |

---

## DB Cleanup docs

| File | Purpose |
|---|---|
| `DB-Cleanup-Legacy-Tables-170826.md` (mới, canonical) | Xoá 10 bảng DB không còn code nào (Go/TS ở cả 3 repo) đọc/ghi (`hris_oauth_tokens`, `hris_payroll_reports`, `ref_hospitals`, `hris_payroll_snapshots`, `hris_payroll_detail_snapshots`, `hris_allowance_override`, `allowance_overrides_bak_20260624`, `allowance_items`, `allowance_types`, `employee_change_requests`) trên `Core System-backend@chore/db-cleanup-legacy-tables` (từ `origin/develop`, chưa merge). `hris_districts` chủ động giữ lại dù cũng không có code dùng, theo quyết định người dùng. Kèm SPEC/PLAN nguồn (`llmwiki/wiki/sources/draft/170826-db-cleanup-legacy-tables(.md/-PLAN.md)`), backup dữ liệu 7 bảng có dòng (`files/db-cleanup-170826-*.sql`), và ghi chú lỗi tự bắt được khi rà soát (quét nhầm nhánh `develop` thay vì `feature_v2` làm dương tính giả 3 bảng report-template). |
| `DB-Cleanup-Education-Ref-Tables-170826.md` (mới, canonical) | Xoá 6 bảng `ref_*` dư thừa trong cụm học vấn (`ref_majors`, `ref_graduate_schools`, `ref_academic_degrees`, `ref_graduation_ranks`, `ref_degree_types`, `ref_training_forms`) + 11 cột FK chết trên `employee_educations`/`employees` — 0% dữ liệu, xác nhận qua code cả 3 repo + số liệu `psql` thật. Ghi nhận rủi ro đã hỏi: dữ liệu HRIS sắp chuyển sang Workday, các bảng `ref_*` này *có thể* cần lại để map học vấn — người dùng xác nhận xoá ngay. Cùng nhánh `chore/db-cleanup-legacy-tables` với đợt 1. |
| `DB-Cleanup-Standalone-Tables-FK-Columns-170826.md` (mới, canonical) | Đợt 4: xoá 16 bảng chết (9 độc lập không FK + 7 có FK phụ thuộc lẫn nhau, đúng thứ tự) + 9 cột FK chết trên `employees`/`org_structures` trỏ `hris_provinces`/`hris_districts`/`work_locations` — xác nhận qua grep code cả 3 repo + đối chiếu dump `payroll_dev` thật (146 bảng, 12.304 NV). Loại trừ `salary_formula_configs` (user tự phân tích riêng), `work_locations`/`hris_provinces`/`hris_districts`/`sync_jobs` (đang sống hoặc giữ theo quyết định). Phát hiện thêm lúc áp dụng: `ref_banks`/`ref_relation_types` có FK chết bị bỏ sót ở vòng quét đầu, xử lý bằng `CASCADE`. Sự cố kỹ thuật: script Python làm mất line ending CRLF của `database.go`, đã sửa bằng 1 commit riêng. Cùng nhánh `chore/db-cleanup-legacy-tables`. |

---

## Salary structure docs

| File | Purpose |
|---|---|
| `Salary-Structure-Shared-Columns-200826.md` (mới, canonical) | Quyết định business 200826: không tạo payslip template riêng theo NV/nhóm NV — toàn bộ NV dùng chung 1 bộ cột rule. Xử lý 3 cơ chế riêng biệt: (1) `salary_formula_configs` — xoá hẳn (mồ côi, 0 code đọc); (2) `salary_component_overrides` (`source='formula'`) — tắt đường tạo mới ở 2 UI entry point (`SalaryComponentOverridesPanel.tsx` + `ComponentDrawer.tsx` route `/v1/Core System`), giữ nguyên `source='cell_pin'` (hạ tầng Nhập Excel/pin ô lương, không đụng); (3) `employee_payroll_templates`/`payroll_templates` — chặn 403 mọi đường ghi + ẩn UI (`SALARY_STRUCTURE_FEATURE_VISIBLE=false`), giữ đường đọc để audit. Nhánh `chore/salary-structure-shared-columns` (BE từ `release/uat-180826`, FE từ `feature_v2`), chưa push/merge. 0 hồi quy (3 fail còn lại xác nhận tiền tồn tại qua `git stash`). |

---

## Attendance/Core System approval + period schedule docs

| File | Purpose |
|---|---|
| `Attendance-Core System-Approval-Gate-240826.md` (mới, canonical) | Nút "Duyệt tất cả" bảng lương (dùng nhánh `scope="all"` có sẵn, không route BE mới) + gate `PayrollService.Calculate` lọc `is_finalized` từ kỳ 09/2026 + chu kỳ lương 21-20 (khoá 4 route trong khoảng ngày mở kỳ mới + scheduler tự động tạo kỳ/chốt công/tính lương). Nối dây bảng `payroll_period_config` từng mồ côi. Nhánh `feature/Core System-period-schedule-gate` (từ `origin/develop`, cả 2 repo), chưa push/merge. `go test`: 795/0 (từ 794/0). Xem SPEC/PLAN `llmwiki/wiki/sources/draft/240826-attendance-Core System-approval-gate(.md/-PLAN.md)`. |

---

## Workday bridge docs

| File | Purpose |
|---|---|
| `Workday-Bridge-Contracts-WorkHistories-170826.md` (mới, canonical) | Mở rộng `/v1/changes` (đã chạy thật cho entity `worker`) thành đa-entity: (A) `employee_contracts` từ field hợp đồng có sẵn trong payload `worker`, không cần đổi adapter; (B) `employee_work_histories` từ entity mới `worker_transfer_event` (adapter ghi thêm `wd_change_log`). Nhánh `Core System-backend@feature/workday-bridge-contracts-workhistories` + `Core System-adapter@feature/workday-bridge-transfer-changelog`, cả 2 từ `origin/develop`. Chưa xác minh Workday thật (môi trường không có kết nối) — verify bằng unit test fixture + dry-run trên 8429 dòng `wd_change_log` thật tại local. |
| `Review-DB-Cleanup-Workday-Bridge-180826.md` (mới, canonical review) | **Review độc lập trước merge (180826)** cho cả 4 nhánh của 170826 — mọi số liệu tự đo lại, không lấy từ self-docs cũ. Xác nhận: 0 hồi quy test (BE cleanup 917/9 ≡ develop; BE workday 924/9; adapter 163/0), replay 6 migration sạch trên dump prod (146→114 bảng, 0 dòng mất, đúng 4 NOTICE CASCADE), `schema.sql` không thêm drift, 5 đối tượng giữ lại nguyên vẹn, CRLF `database.go` đã đúng. Tìm 8 vấn đề luồng 1 (nặng nhất: 3 endpoint `/ref/*` chết → 500; thiếu runbook vì backend KHÔNG chạy Atlas) + 10 vấn đề luồng 2 (chặn merge: lỗi upsert `employee_contracts` làm kẹt vĩnh viễn con trỏ sync employees; `RefreshWorkerTransferEvent`/dòng cũ không sinh `wd_change_log`). Xung đột `atlas.sum` giữa 2 luồng đã thử thật + cách giải. |
| `DB-Cleanup-Deploy-Runbook-180826.md` (mới) | **Runbook triển khai** cho 7 migration của đợt dọn DB (4 đợt 170826 + F1/F3 của review 180826). Sự thật nền: backend KHÔNG chạy Atlas (`database.go:2912`) — `atlas migrate apply` phải chạy tay. **Thứ tự bắt buộc: deploy code TRƯỚC, apply SAU** — đảo lại thì `RunMigrations` tái sinh 15 bảng ở lần restart kế (đã liệt kê đủ tên). Kèm: 4 NOTICE hợp lệ duy nhất được phép xuất hiện lúc apply, số liệu kiểm sau apply (146→114 bảng, employees 228→211 cột, org_structures 61→58, employee_educations 30→24, số dòng employees không đổi), đường rollback (backup ở đâu, DDL lấy từ commit nào, 9 cột nhóm C là thứ duy nhất không tái tạo được ngoài dump), và nhắc merge `develop`→`feature_v2`. |
| `files/payroll_engine-full-180826.dump` + `-schema-180826.sql` + `payroll_engine-180826-README.md` (mới) | **Dump SCHEMA-ONLY của DB backend dev** (`payroll_engine`, PG 14.18, 79 bảng / 242 index / 63 FK) tại 18/08/2026 — tức trạng thái ĐÍCH sau đợt dọn DB + cầu nối Workday. **Không chứa một dòng dữ liệu nào** (0 `COPY`, 0 `INSERT`; DB gốc có 12.044 NV thật nhưng không thứ gì trong đó lọt vào file). Đã quét: 0 email, 0 token thật. Đã kiểm chứng restore vào DB trống với `ON_ERROR_STOP=1`: 0 lỗi, danh sách bảng `diff` với nguồn ra rỗng. **Kèm thêm bản `-full-` 42 MB có TOÀN BỘ DỮ LIỆU THẬT** (12.044 NV, 76.856 dòng lịch sử công tác, 24.530 người phụ thuộc, 10.700 tài khoản ngân hàng) — phát hành theo yêu cầu trực tiếp của user 180826 với lý do đồng nghiệp đã có quyền truy cập dữ liệu tương đương; README ghi rõ cảnh báo xử-lý-như-dữ-liệu-nội-bộ và chỉ ra bản schema-only cho ai không có quyền đó. Dùng để gửi đồng nghiệp tham khảo. Lưu ý ghi trong README: file này là ảnh chụp ĐÚNG DB, còn `internal/db/schema.sql` của repo thiếu 10 bảng + 10 cột — lệch nhau thì tin file dump. |
| `files/payroll_adapter-full-190826.dump` + `-schema-190826.sql` + `payroll_adapter-190826-README.md` (mới) | **Dump DB adapter** (`payroll_adapter`, PG 14.18, 55 bảng / 511 MB / ~208.500 dòng) — DB trung gian Workday→backend. Bản full 81 MB có toàn bộ payload SOAP Workday thật (`wd_change_log` 47.508 dòng/258 MB, `wd_workers` 4.211); bản schema-only 116 KB, 0 dòng dữ liệu. **Đã quét xác nhận KHÔNG chứa credential**: không cột `password`/`secret`/`token`/`api_key`, không bảng config, 0 dòng change_log có chuỗi kiểu Bearer — thông tin ISU Workday chỉ nằm ở `.env`. README kèm 4 câu SQL để đội adapter TỰ KIỂM 3 con số trong tài liệu bàn giao (Business_Title 8.427/8.429 · Employee_Contracts_Data 0/8.429 · transfer event 0 và 0) và nêu rõ DB local KHÔNG dùng để đo quy mô backfill được, việc V0 phải đếm trên production. |
| `Adapter-Workday-Handover-180826.md` (mới) | **Điểm vào cho đội `Core System-adapter`** — đọc trước tài liệu hợp đồng. Chia việc thành V0 (đo quy mô backfill, gửi số về backend) → V1 (đường ghi cơ bản `wd_change_log`) → V2 (múi giờ `effectiveDate`) → V3 (đường refresh sinh change log `update`) → V4 (backfill), giải thích vì sao thứ tự đó (V1 dựng hàm dựng JSON dùng chung, V2-V4 tái dùng), điều kiện xong từng việc, 3 điều KHÔNG được làm, 3 thứ backend cần nhận lại từ họ, và bước xác minh chung sau khi xong. |
| `Adapter-Workday-Implementation-And-Local-Test-180826.md` (mới) | **Hướng dẫn thi hành + kiểm thử local** cho đội `Core System-adapter`. Chỉ đích danh những gì ĐÃ CÓ SẴN trên `origin/develop` (vòng lặp `SyncWorkerTransferEvents` ở `internal/sync/sync_transfer_events.go:64`/`:108`, hàm mẫu `insertChangeLog` ở `internal/store/raw.go:1791`) và việc thiếu duy nhất là 2 chỗ ghi `wd_change_log`. Mỗi việc V1–V4 có: file/dòng cần sửa, cách kiểm bằng Go test + SQL **không cần kết nối Workday**, và bẫy cụ thể (FK `log_wid`→`wd_worker_transaction_logs` khi dựng fixture; chọn mốc 17:00–23:59 UTC mới lộ được lỗi múi giờ; `applied=0` thường là do `Worker_ID` không khớp `employees.employee_code` nào). Kèm quy trình test end-to-end với backend local và checklist trước khi báo xong. |
| `Adapter-Workday-ChangeLog-Mapping-Template-180826.md` + bản `.html` rời cùng tên (mới) | **Hợp đồng dữ liệu Adapter → Backend**, phát hành cho đội `Core System-adapter`. Ranh giới kênh duy nhất (`GET /v1/changes`, không nối thẳng DB), giao thức con trỏ, bảng field đầy đủ cho entity `worker` (kể cả nhánh hợp đồng `Employee_Contracts_Data`) và entity mới `worker_transfer_event` kèm giới hạn độ dài từng cột đích. Chứa **3 yêu cầu bắt buộc phía Adapter** mà Backend không tự khắc phục được: `RefreshWorkerTransferEvent` phải sinh change log `update`, backfill change log cho `log_wid` đã tồn tại, `effectiveDate` phải theo `Asia/Ho_Chi_Minh`. Kèm bảng "Adapter đổi X → Backend hỏng Y" và 5 câu SQL tự kiểm. |

---

## Internal technical docs

| File | Purpose |
|---|---|
| `internal/README.md` | Package-level guide for `internal/` directory |
| `internal/db/tables_code.md` | DB tables as seen by Go code (sqlc models) |
| `internal/db/tables_db.md` | DB tables as seen in the actual schema |
| `pgp-to-sftp/ADAPT.md` | Adaptation notes for the PGP→SFTP utility |

---

## HTML visual docs (`llmwiki/html/`)

Open in browser.

| File | Size | Topic |
|---|---|---|
| `onboarding-Core System-backend.html` | 48 KB | Visual onboarding overview for the whole backend |
| `overstack.html` | 141 KB | Large stack / dependency diagram |
| `wiki-graph.html` | 97 KB | Wiki link graph |
| `problem-tree.html` | 10 KB | Known problems / issue tree |
| `design-pattern-v3.html` | 58 KB | Latest design pattern reference (v1/v2 are older, skip) |
| `180626-orca-framework-overview.html` | 46 KB | Orca framework overview (June 18) |
| `230626-harness-docs-gate-orca-guard.html` | 35 KB | Harness + docs-gate + orca-guard explainer |
| `210626-design-pattern-infographic-seq.html` | 29 KB | Design pattern sequence diagram |
| `220626-design-pattern-html-refactor-seq.html` | 32 KB | HTML refactor sequence diagram |
| `230626-*-seq.html` (×5) | 5–7 KB each | Sequence diagrams: sync, guard, hook, docs-gate, skill-okf |

---

## Harness / framework scaffolding (not project docs)

`harness/poc-vendor-neutral/out/` — agent adapter snippets generated by the harness framework. Not Core System-specific; safe to skip unless working on harness tooling.

| File | Notes |
|---|---|
| `out/kiro/.kiro/steering/harness.md` | Kiro steering file |
| `out/codex/AGENTS.snippet.md` | Codex agent snippet |
| `out/antigravity/permissions.snippet.txt` | Antigravity permissions |

---

## Recommended reading order

1. `README.md` — orient yourself
2. `ARCHITECTURE.md` — understand the structure
3. `AGENTS.md` — how to interact with the project as an agent
4. `MIGRATION.md` — understand the ongoing refactor direction
5. `onboarding-Core System-backend.html` — visual onboarding
6. `internal/db/tables_db.md` + `tables_code.md` — DB surface area
