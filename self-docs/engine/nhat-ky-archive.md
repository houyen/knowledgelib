---
id: self-docs/engine/nhat-ky-archive
canonical_question: 'Technical guide and specification: Nhật ký công việc — Archive'
aliases:
- Nhật ký công việc — Archive
- Nhat Ky Archive 170826
entity_type: how_to
domain: self-docs > engine
last_verified: 2026-09-17
---

# Nhật ký công việc — Archive (đến 150726)

Các dòng nhật ký ngày từ `self-docs/CLAUDE.md` (mục "Nhật ký công việc theo ngày"), chuyển ra đây ngày 170826 để giảm token của CLAUDE.md chính (mục đó chỉ còn giữ 2 entry gần nhất). Đây là bản lưu trữ nguyên văn, không phải tài liệu canonical — chi tiết kỹ thuật đầy đủ vẫn nằm ở các file canonical tương ứng trong `self-docs/` mà mỗi dòng dưới đây trỏ tới. Thứ tự: mới nhất lên đầu (100826), cũ nhất ở cuối (150726).

- **100826** — Thiết kế **74 kịch bản test** cho 7 phân hệ Core System (Formula Engine, Core System Template, Salary Override, Core System Period Flow, RBAC, Bảng chấm công, Report Template Config), xuất mỗi phân hệ 2 file Excel Blind/Answer (`self-docs/files/*-TestCase-{Blind,Answer}-100826.xlsx`), dựng lại hạ tầng Playwright e2e cho máy local (bỏ phụ thuộc Docker trong `psql()` helper, vá fixture period/attendance Tháng 06/2026 thiếu, **tắt `trace: "retain-on-failure"`** — nguyên nhân thật của timeout giả 30s+ trước đó bị nghi sai là "dev-mode nguội", sửa fixture `contract_type`/`is_foreigner` ưu tiên đúng cột theo thay đổi 040826), viết 5 spec Playwright mới (`Core System-template`/`Core System-override`/`Core System-period-flow`/`Core System-rbac`/`report-template-config`, cộng `Core System-formula`+`smoke` đã sửa = **33 test PASS**, xác nhận sạch DB sau mỗi lần chạy). Phát hiện 4 lệch giữa giả định/tài liệu cũ và code thật: `SalaryComponentOverridesPanel` không còn đường vào từ UI chính (mode "edit" của `SalaryComponentModal` không được dispatch ở đâu); lệch mã lỗi 400 (CellSet) vs 500 (CalculateOne) cho cùng lỗi nghiệp vụ "bản ghi đã Finalize"; `ReportTemplateConfig` TC04 chặn alias trùng mã thật lúc **Chạy báo cáo**, không phải lúc **Lưu** như tôi viết ban đầu trong Excel — đã tự sửa lại file gốc + regenerate; `Core System-adapter` (Bảng chấm công) không có UI/HTTP endpoint ở nhánh hiện tại (chỉ CLI), dùng bộ Go test có sẵn (58/58 PASS) thay Playwright. RBAC cần quy trình đặc biệt (dev-bypass gắn cố định 1 email qua `DEV_USER_EMAIL`, và `EnsureSuperAdmins` tự seed lại email đó vào `super_admins` mỗi lần khởi động) — đã restart backend tạm thời với email test, xoá dòng auto-seed, chạy 3 test, rồi khôi phục sạch (.env + DB + restart lại), xác nhận lại bằng smoke test. **Chưa commit gì** — toàn bộ ở `Core System-frontend/e2e/*.spec.ts` + `playwright.config.ts`, `Core System-backend` không đổi code (chỉ DB/config tạm, đã khôi phục). Tài liệu bàn giao đầy đủ (vì rút khỏi dự án tạm thời): `self-docs/Core System-Test-Suite-Handoff-100826.md` (canonical), trang tổng kết `llmwiki/html/100826-Core System-test-summary.html`.
- **060826** — Đóng **L10** (RLS trên `employees` vô hiệu vì app chạy bằng DB role `postgres` superuser/BYPASSRLS) theo brief `self-docs/files/prompt-L10-060826.md`, **chỉ ở dev local**. Backup `pg_dump` trước khi làm gì (44MB, xoá sau khi Bước 5+6 pass). Tạo role `payroll_app_role` (`LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOBYPASSRLS`), chuyển OWNER 106 bảng + 8 sequence trong `payroll_engine` sang role này (kiến trúc 1 connection duy nhất, dựa vào `FORCE ROW LEVEL SECURITY` đã bật sẵn trên `employees` — RLS áp dụng cả cho owner khi có FORCE). Verify RLS thật bằng `psql` tay với role mới TRƯỚC khi đổi `.env`: `company_scope_codes` không khớp → `0` dòng; `unlimited=true` → đủ `12044` dòng — đúng kỳ vọng cả 2 chiều. Đổi `Core System-backend/.env` (`DB_USER`/`DB_PASSWORD`, gitignored, không commit). Backend khởi động 2 lần liên tiếp không lỗi migration/permission; smoke test `GET /me` + `GET /Core System/template-enforce-status` qua role mới đều thành công. Full suite `TEST_DATABASE_URL` giữ nguyên `postgres` (đúng brief — test tích hợp cần toàn quyền dựng fixture): 891 passed/7 failed khớp baseline tuyệt đối, 0 hồi quy. **Chưa đẩy staging/production** — brief chỉ định phạm vi dev local, việc nhân rộng là quyết định vận hành riêng. Tài liệu: `RBAC-Backlog-Tracklist-160726.md` mục A3 (số đo thật đầy đủ), `00-START-HERE.md` (xoá L10 — không còn lỗi nào trong bảng mục 2, L1-L11 đã đóng hết; cập nhật 3 chỗ tham chiếu "Task 9"/"L10" còn mở đã lỗi thời). **Không có commit code** — chỉ đổi DB (đã áp trực tiếp) + `.env` (gitignored) + tài liệu (`self-docs/` ngoài git). Commit tài liệu: chờ xác nhận.
- **060826** — Thi hành **Task 9** (tài liệu tổng kết, task cuối PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md`) theo brief `self-docs/files/prompt-Task9-060826.md`. **Phát hiện thêm 1 lỗ ngoài 5 lỗ brief liệt kê**: `Core System-backend/docs/routes-permissions.md` thiếu hoàn toàn dòng cho `GET /Core System/template-enforce-status` (route có thật, FE đã dùng từ Task 5.1/6, chưa từng ghi tài liệu) — bắt được nhờ đúng bước kiểm `grep` đối chiếu 2 phía mà brief yêu cầu. Sửa 6 lỗ: (1) `routes-permissions.md` BE dòng `template-impact` GET→POST + thêm dòng `template-enforce-status` thiếu; (2) thêm dòng `/bulk` (Task 7); (3) đồng bộ toàn bộ nhóm route `Core System-templates`/`employee-Core System-templates` sang `routes-permissions.md` FE (trước đó 0 dòng); (4) `database-schema.md` thêm 2 bảng `employee_payroll_templates`+`template_component_versions` (ERD lấy từ `psql \d` thật, không chép brief); (5) mục 13 `Salary-Structure-Template-Analysis-260726.md` đánh dấu HOLD đã kết thúc (giữ nguyên nội dung lịch sử); (6) mục 7 thêm cross-reference đính chính "3 lớp bảo vệ" (2/3 lớp đã chết) trỏ sang mục 14.6(1) — brief ghi sai số mục (16.6), đã tự sửa theo nội dung thật. **Phát hiện phụ, không phải việc của mình**: khi đọc lại log để lấy commit hash, thấy 1 phiên khác đã kiểm chứng độc lập Task 8 và tìm ra lỗ thứ 2 — `snapshotTemplateMasksForPeriod` (gọi bởi `Finalize`) vẫn dùng `ListAllTemplateComponentCodes` không lọc ngày, khiến snapshot ghi thành phần template HÔM NAY dù `computed_values` đã tính đúng theo lịch sử — đã sửa + test chứng minh bằng revert-khôi-phục, commit `09f68ae` (đã có sẵn trong repo trước khi mình chạm vào, không phải việc mình làm, ghi lại để không ai tưởng nhầm). `document-map.md` đã có đủ 2 entry SPEC/PLAN 060826 từ trước, không cần thêm. Full suite sau khi xác nhận: 891 passed/7 failed khớp baseline. Commit: `Core System-backend@bdf689f` + `Core System-frontend@3ae70bd`, cả hai trên `feature_v2` — đã push.
- **060826** — Thi hành **Task 8** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (Hướng 1 — effective-dating cho `template_components`, khuôn `salary_component_versions`) theo brief `self-docs/files/prompt-Task8-060826.md`, ngay sau Task 7. **TOÀN BỘ PLAN (Task 0-8) hoàn tất.** Re-grep lại số dòng thật trong `payroll_template_repo.go` trước khi sửa (đúng cảnh báo của brief — Task 7 đã chèn code, lệch dòng nhưng không lệch nội dung). Bảng mới `template_component_versions` (mốc BZ) + `ListAllTemplateComponentCodesAsOf` (COALESCE-style, fallback về `template_components` hiện tại cho template chưa từng qua `ReplaceComponents` sau Task 8) + `ReplaceComponents` ghi version mới CÙNG transaction + `loadTemplateMaskData` đổi sang gọi bản AS-OF (không đổi chữ ký hàm). **Nghiệm thu tầng thật đáng chú ý nhất trong ngày**: thiết kế lại cách dựng kỳ test của Task 5 (kỳ tương lai 2099) không đủ để chứng minh point-in-time — phải dùng kỳ THẬT-SỰ-TRONG-QUÁ-KHỨ (2020-01, periodEnd nằm giữa 2 lần đổi thành phần template theo thời gian thực) mới bẫy được đúng bug. Kết quả: `Calculate` kỳ 2020-01 lần 1 → `PHONE_ALLOW=500000` (giữ)/`FUEL_ALLOW=0` (mask); đổi thành phần template A sang `FUEL_ALLOW` NGAY BÂY GIỜ qua API thật; `Calculate` LẠI cùng kỳ 2020-01 → **kết quả giữ nguyên tuyệt đối** — chứng minh trực tiếp: đổi cấu trúc hôm nay không làm sai lệch kỳ lịch sử đã tính. 2 test tích hợp PASS (repo-level + test cũ Task 2/4 không hồi quy), full suite 890 passed/7 failed khớp baseline. Dọn sạch dữ liệu test, `payroll_records` thật y nguyên (6435 dòng, md5 `202a20ec...`). Tài liệu: PLAN mục Tiến độ (Task 8 → xong, Task 0-8 hoàn tất, còn Task 9 — tài liệu tổng kết, brief riêng sau). Commit: `Core System-backend@82a2562` trên `feature_v2` — đã push (backend only, Task 8 không sửa FE).
- **060826** — Thi hành **Task 7** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (nhánh B — gán hàng loạt theo `level_code`) theo brief `self-docs/files/prompt-Task7-060826.md`, ngay sau Task 6. **Bắt 2 lỗi thật khi thi hành** (không chạy mù): (1) brief viết `ON CONFLICT ON CONSTRAINT employee_payroll_templates_emp_from_key` — xác nhận bằng `BEGIN/ROLLBACK` trực tiếp trên `payroll_engine` rằng đây là UNIQUE INDEX chứ không phải constraint có tên (0 dòng trong `pg_constraint`), Postgres báo lỗi thật "constraint does not exist" — sửa bằng `ON CONFLICT (employee_id, effective_from)` (cùng ngữ nghĩa DO UPDATE, chỉ đổi cách chỉ định target); (2) test tích hợp brief viết `defer` xoá 2 template + 1 employee_payroll_templates theo thứ tự khai báo LIFO sai — template thứ 2 (`tpl2`) bị xoá TRƯỚC khi dòng `employee_payroll_templates` còn tham chiếu tới nó (FK `ON DELETE RESTRICT`), lỗi bị `defer db.ExecContext(...)` nuốt im lặng, để lại `TEST_T7_060826_v2` mồ côi mỗi lần chạy — xác nhận bằng query DB thấy dòng mồ côi thật, sửa bằng dời defer xoá `employee_payroll_templates` xuống SAU khi tạo `tpl2` (chạy trước cả 2 defer xoá template theo LIFO). `BulkAssignByLevelCode` (repo/service/handler/route `POST /config/employee-Core System-templates/bulk`, khai trước `POST "/"`) + khối FE "Gán hàng loạt theo cấp bậc" trong `EmployeeTemplateAssignPanel.tsx` (tách biệt hoàn toàn state với luồng gán-từng-người). Verify: test tích hợp PASS cả 2 nhánh (tạo mới `created=2,updated=0` → ghi đè `created=0,updated=2`, xác nhận cả `template_id` thật đã đổi); `curl` thật qua route/gate khớp đúng test; full suite 889 passed/7 failed khớp baseline; FE `tsc`/`eslint` sạch, `vitest` 30/30. **Kiểm tay trình duyệt thật** (Playwright dev bypass): điền form, bấm 2 lần xác nhận đúng `created`/`updated`, luồng gán-từng-người không bị ảnh hưởng. Dữ liệu test (DB test + curl + browser, 3 lượt riêng) đã dọn sạch. Tài liệu: PLAN mục Tiến độ (Task 7 → xong, Task 0-7 hoàn tất, còn Task 8 chờ dispatch — phụ thuộc Task 7 đã "land"). Commit: `Core System-backend@008ad0e` + `Core System-frontend@ed3b9c5`, cả hai trên `feature_v2` — chưa push.
- **060826** — Thi hành **Task 6** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (mở lại UI, đóng L8/L9) theo brief `self-docs/files/prompt-Task6-060826.md`, ngay sau khi xác nhận Task 5 đã xong. `Core System-frontend`: bật `SALARY_STRUCTURE_FEATURE_VISIBLE=true`; tạo `lib/enforce-banner.ts` (hàm thuần `resolveEnforceBanner`, 3 trạng thái on/off/unknown, KHÔNG coi API lỗi là "TẮT" như banner cũ fail-unsafe) + 4 test PASS; sửa `PayrollTemplateModal.tsx` dùng banner mới (bỏ câu nhắc nút "Xem trước tác động" đã bị xoá 270727 — quyết định đã chốt KHÔNG dựng lại modal đó), thêm checkbox `isDefault` (xác nhận trước: BE không có guard "chỉ 1 default", để nguyên theo brief); thêm banner cho `AllAssignmentsModal.tsx` (trước đó bất đối xứng, không có); `EmployeeTemplateAssignPanel.tsx` thêm Sửa/Xoá mỗi dòng lịch sử gán (2 hàm API `updateEmployeeTemplateAssignment`/`deleteEmployeeTemplateAssignment` có sẵn từ trước nhưng 0 điểm gọi — nay dùng). `getTemplateEnforceStatus()` trong `lib/api/config.ts` thêm `healthy: boolean` vào type trả về (BE đã trả sẵn, chỉ thiếu type FE). Verify: `tsc`/`eslint` sạch (trừ `DevToolkit.tsx` — lỗi build có sẵn từ trước), `vitest` 30/30 (thêm 4 test enforce-banner), `next build` không hỏng thêm gì so với baseline. **Kiểm tay trình duyệt thật đầy đủ** (Playwright dev bypass qua `e2e/devLogin.ts`, script tạm đã xoá sau khi xong): 3 nút ribbon hiện lại đúng; tạo template test qua UI với `isDefault` tick → xác nhận `is_default=true` ghi đúng DB; luồng Gán→Sửa→Xoá assignment cho 1 NV mượn hoạt động đúng, cập nhật ngay không cần F5; cả 3 trạng thái banner (ON lúc backend khoẻ, UNKNOWN khi tắt backend giữa chừng) đúng trên cả `PayrollTemplateModal` và `AllAssignmentsModal`. Dọn sạch dữ liệu test (template/assignment) sau khi kiểm — `payroll_templates`/`employee_payroll_templates` về lại 0 dòng. Tài liệu: PLAN `040826-...` mục Tiến độ (Task 6 → xong, Task 0-6 hoàn tất, còn Task 7/8 chờ quyết định), `00-START-HERE.md` (xoá L8/L9 khỏi bảng lỗi, còn L10 không liên quan). **Không có commit** — chờ xác nhận người dùng.
- **060826** — Thi hành **Task 5** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (dọn dữ liệu demo, bật enforce dev local, nghiệm thu end-to-end) theo brief `self-docs/files/prompt-Task5-060826.md`, ngay trong phiên. Bước 1: xoá `payroll_templates` "demo 1" + assignment UAT của THAIDT001 (xác nhận id khớp brief trước khi xoá, theo đúng quyết định người dùng đã chốt). Bước 2: thêm `PAYROLL_TEMPLATE_ENFORCE=on` vào `Core System-backend/.env` (đã xác nhận gitignore trước khi sửa) — `template-enforce-status` trả `{"enabled":true,"healthy":true}` sau restart. Bước 3 (nghiệm thu kỳ cách ly 2099-01, mượn 2 nhân viên thật chỉ đọc — không sửa `employees`, mọi ghi/xoá theo `period_id` mới): **bắt 2 lỗi khi chạy thật** — (1) brief ghi sai shape body `template-impact` (object thay vì mảng `{empCode,basicSalary}`, đọc handler sửa lại); (2) **hiểu sai cơ chế "universe" khi dựng fixture lần đầu** — đưa cả 4 cột muốn mask vào MỘT template rồi gán cho NV làm mask KHÔNG có tác dụng gì (`employeesAffected:0`), vì cột nằm trong closure của chính template NV được gán thì KHÔNG bị mask; đọc lại `buildTemplateMaskFormulas` + đối chiếu `TestTemplateEnforce_MaskedColumnIsZero` mới hiểu đúng: `maskableUniverse` là UNION code của MỌI template trong DB, phải có ≥2 template (1 gán cho NV chỉ gồm `PHONE_ALLOW`+`FUEL_ALLOW`, 1 không gán ai chỉ để đưa `MEAL_TAX`+`MEAL_NONTAX` vào universe) mới thấy mask thật. Sau khi sửa: `template_components` sống qua 1 lần restart thật (Task 0 durability); `template-impact` trả `employeesAffected:1`/`totalGrossDelta:-900000` (khác 0, không hồi quy Task 4); `Calculate` cho NV được gán → `MEAL_TAX=0`/`MEAL_NONTAX=0`/`GROSS=20000000`, NV không gán → giá trị y nguyên (TASK-REF, dùng luôn làm đối chứng bước 9, không cần NV thứ ba); `CalculateOne` khớp tuyệt đối `Calculate`; `Finalize` ghi đúng 2 dòng snapshot `scope_type='template'` khớp tập cột đã mask. Bước 4: dọn sạch toàn bộ dữ liệu test kỳ 2099 — `payroll_records` mọi kỳ THẬT y nguyên tuyệt đối trước/sau (6435 dòng, md5 `202a20ec...`). **Phát hiện phụ ngoài phạm vi Task 5**: `origin/feature_v2` cả 2 repo hoá ra ĐÃ CÓ mọi commit Report v1 (Task 1-7, đợt trước) qua `git fetch` xác nhận — tự đính chính dòng nhật ký trước ghi "chưa push", không rõ ai đã push, không tự suy đoán. Tài liệu: `Salary-Structure-Template-Analysis-260726.md` mục 19 (mới, số đo thật đầy đủ), PLAN `040826-...` mục Tiến độ (Task 5 → xong, việc kế tiếp → Task 6). **Không có commit code nào** — Task 5 chỉ đổi dữ liệu DB dev (đã áp trực tiếp) + `.env` (gitignored, không commit theo đúng ràng buộc brief) + tài liệu (nằm ngoài git, `self-docs`/`llmwiki`/`CLAUDE.md` ở thư mục gốc không phải repo).
- **060826** — Thi hành trọn PLAN **Report v1** (`060826-report-template-config-v1-PLAN.md`, 7/7 task, `Core System-backend@feature_v2` + `Core System-frontend@feature_v2`, ngay trong phiên phân tích/soạn PLAN theo yêu cầu người dùng, không dispatch phiên khác). Backend: bảng `report_templates`/`report_template_lines` (khoá `component_code`, không FK `salary_components.id` — G1) + CRUD (gate mới `Settings.ReportTemplates`, opt-out default-allow) + `POST /reports/config/{templateId}/run` (đọc thẳng `payroll_records.computed_values`, không qua engine — G3, 422 nếu `component_code` không tồn tại). **Bắt 2 lỗi PLAN trước khi chạy mù**: (1) test Task 1 scan biểu thức Postgres `BOOLEAN` vào Go `int` — lỗi ở MỌI trạng thái migration, xác nhận bằng script Go tối giản rồi sửa `var exists bool`; (2) route Task 3 định đặt trong `RegisterReportRoutes` và tái dùng `requireReportTemplatesView` từ `RegisterConfigRoutes` — hai hàm Go khác scope, route sẽ lọt ra ngoài `r.Use(requireAdmin)` mà mọi `/reports/*` khác có (role `employee` gọi được) — bắt bởi `TestG1AllProtectedRoutesRequireRoleGateExceptAllowlist` khi chạy full suite, sửa bằng khai gate cục bộ + thêm `requireAdmin`. Frontend: 2 sheet nhúng theo đúng 3 tiền lệ có sẵn (`cfg-Template báo cáo` liệt kê/xoá, `rep-Chạy template báo cáo` chọn/chạy/xem), không đụng `ReportsRibbon.tsx` (G8); v1 cố ý chỉ liệt kê+xoá, tạo/sửa qua API (nợ có chủ đích, không phải thiếu sót). Task 6: `computePayslipRole()` (19 deduction + 11 employer_cost, xác nhận thật 30/30 bằng SELECT) thay `DEDUCTION_KEYWORDS` đoán chuỗi trong `PayslipSummaryModal.tsx` (đang sai 21/30 mã — `TOTAL_INS`/`TOTAL_DED`/`ADVANCE_1` và 11 mã `_CTY` bị xếp nhầm thành thu nhập). Verify: BE `go test ./... -p 1` **881 passed/7 failed** khớp baseline (xác nhận qua `git stash`) sau mỗi task, FE `tsc`/`eslint` sạch (trừ `DevToolkit.tsx` — lỗi build có sẵn từ trước, xác nhận lại qua `git stash`+`next build`), `vitest` 26/26. Kiểm tay bằng dữ liệu thật (curl+psql trên `payroll_engine`, kỳ 05/2026, 3273 bản ghi): CRUD template, chạy báo cáo cột `identity`/`component`/`sum` khớp DB, phân loại payslip 19/11/2/108 khớp audit — dọn sạch dữ liệu test sau khi kiểm. 6 commit tách theo task (`bb7fa8a`/`79d48db`/`a2f107d`/`a9ce55f` BE, `c1e2250`/`4d08838` FE), chưa push. Tài liệu: `self-docs/Report-Template-Config-Analysis-040826.md` mục 10 (mới), `document-map.md`, `docs/routes-permissions.md` (2 repo).
- **060826** — Hoàn tất thi hành **Task 3** (`Core System-backend@feature_v2` + `Core System-frontend@feature_v2`, phiên bị ngắt giữa chừng lúc verify BE, tiếp nối đúng brief đã chốt — không tự đổi thiết kế). BE: `templateMaskEligible` (= `maskableCodes()` ∪ `templateMaskSiblingSplits`, đo thật trên 140 component = **72 mã**, khớp dự đoán) + `templateMaskReason`, cả hai dùng chung giữa `buildTemplateMaskFormulas` (guard runtime) và `SalaryComponentService.List` (API, field `Maskable`/`MaskReason` mới `db:"-"`); 4 test mới, gồm `TestTemplateMaskEligible_MatchesGuard` chống trôi 2 bên. Nghiệm thu bằng revert-và-khôi-phục (tạm revert wiring → 1 test Task 2 cũ FAIL đúng kỳ vọng → khôi phục, diff y hệt). **Phát hiện + đóng luôn 1 nghi vấn hồi quy giả**: `TestComputeTemplateImpact_DetectsAllowanceMaskDelta` (viết ở Task 4) fail trong lần chạy full suite đầu nhưng xanh khi chạy cách ly và xanh lại ở lần chạy full suite thứ 2 — xác nhận đúng loại flaky đã biết của repo (assert `count(*) FROM payroll_records` KHÔNG lọc theo `period_id`, đụng độ với package test khác chạy song song), không phải do Task 3 gây ra — không sửa test đó (ngoài phạm vi Task 3). Nghiệm thu request thật: `curl GET /config/salary-components` — `GROSS`/`NET_PAY`/`BASIC_SAL` → `maskable:false` kèm lý do đúng; `MEAL_ALLOW` → lý do chỉ đúng 2 cột tách `MEAL_TAX`/`MEAL_NONTAX`, không còn câu "không bỏ được"; `PHONE_ALLOW` → `maskable:true`. `payroll_records` md5+count y nguyên trước/sau (`202a20ec...`, 6435 dòng). FE: `lib/template-picker.ts` (`isMaskable`/`nonMaskableSelectedCodes`, 6 test vitest) + `PayrollTemplateModal.tsx` picker disable+tooltip+lý do dưới tên cột + banner cảnh báo mã đã chọn nay hết maskable (không tự bỏ chọn); `lint`/`vitest` xanh; `pnpm build` xác nhận **hỏng sẵn từ trước** vì 1 lỗi export không liên quan (`DevToolkit.tsx`/`auth-callback`, tái hiện y hệt trên baseline qua `git stash`) — dùng `tsc` phạm vi hẹp (loại nhiễu e2e/playwright) làm gate thay thế. **Kiểm tay trình duyệt thật đã làm xong trong phiên trước đó** (Playwright+Chromium cài tạm rồi gỡ, qua `e2e/devLogin.ts`) — 3 ca click thật (`GROSS` disabled không đổi state, `MEAL_ALLOW` disabled đúng lý do, `PHONE_ALLOW` tick được), 0 dòng ghi `template_components` trong lúc kiểm, đã trả `SALARY_STRUCTURE_FEATURE_VISIBLE` về `false` và gỡ sạch playwright khỏi `package.json`/lockfile trước khi dừng — xác nhận lại `git diff`/`git stash` cho thấy đúng phạm vi Task 3, không còn vệt tạm. Cập nhật `00-START-HERE.md` (xoá tham chiếu L6 khỏi mục điều hướng — dòng lỗi L6 tự nó đã được xoá khỏi bảng lỗi từ trước; sửa "việc kế tiếp" từ Task 2/3 sang Task 5/6). `Salary-Structure-Template-Analysis-260726.md` mục 18 đã có sẵn kết quả đầy đủ từ phiên trước, không cần viết lại. Còn nợ: `pnpm build` sản xuất thật vẫn hỏng vì lỗi `DevToolkit.tsx` không liên quan — chưa sửa (ngoài phạm vi Task 3, cần quyết định riêng). Commit + PLAN cột TT (ghi hash thật) — **chờ xác nhận người dùng trước khi commit**.
- **050826** — Soạn prompt **Task 3** (`self-docs/files/prompt-Task3-050826.md`, đóng L6 — picker cột `allComponents.map(...)` không lọc gì nên `GROSS`/`NET_PAY`/`BASIC_SAL` đều tick được). Xác minh trước khi viết và bắt được **một cái bẫy do chính Task 2 tạo ra**: tập mask THỰC TẾ ở guard là `maskableCodes()` **∪** `templateMaskSiblingSplits` = **66 ∪ 6 = 72**, nhưng `maskableCodes()` chỉ trả **66** ⇒ nếu lấy cờ `maskable` cho FE từ hàm đó một mình thì UI sẽ **disable 6 cột tách** (`MEAL_TAX`/`MEAL_NONTAX`/`PHONE_TAX`/`PHONE_NONTAX`/`TRANSPORT_TAX`/`FAMILY_HEALTH_INS`) — và hậu quả cụ thể là **cắt luôn đường duy nhất để bỏ phụ cấp cơm** (vì `MEAL_ALLOW` không maskable trực tiếp). ⇒ Brief bắt **rút một helper `templateMaskEligible` dùng chung cho CẢ guard lẫn API**, kèm test `TestTemplateMaskEligible_MatchesGuard` chống trôi khi ai sửa một bên, và nghiệm thu định lượng "đúng 72 mã". Yêu cầu thứ ba lấy từ mục 16.6: `maskReason` của `MEAL_ALLOW` phải **chỉ đường qua 2 cột tách**, **KHÔNG** được nói "không bỏ được phụ cấp cơm" (câu đó sai vì `GROSS` tham chiếu các cột tách chứ không tham chiếu cột cha, và `MIN(x,730k)+MAX(0,x-730k)=x`) — có test riêng bắt cả 2 chiều (phải chứa tên 2 cột tách, không được chứa ý "không bỏ được"). Quan hệ cha–tách suy ra ở BE **từ dữ liệu** (tìm mã trong allowlist có formula tham chiếu tới nó), **cấm hardcode ở FE**. Đường đi đã trace sẵn cho brief: `salary_component_handler.go:20` → `salary_component_service.go:116` → `repo.ListActive`; `maskableCodes` cùng package `service` nên gọi trực tiếp được; 2 field mới phải có tag `db:"-"` (Bẫy 3 — thiếu là vỡ scan `sqlx`). Cũng nêu rõ **thứ ĐÃ CÓ để agent không làm lại**: `componentCount` (`PayrollTemplateModal.tsx:118`) và bộ đếm "đã chọn" (`:132`) — chỉ yêu cầu 17.2 (hiện số cột để template rỗng không bị đọc thành "tính năng hỏng") thực chất đã đạt. Kèm: FE **không có script `typecheck`** (type kiểm qua `pnpm build`), `vitest` chỉ `environment: node` nên phải tách logic thuần ra `lib/` mới test được, và bắt **kiểm tay trình duyệt** (tạm bật `SALARY_STRUCTURE_FEATURE_VISIBLE` rồi đóng lại trước commit — mở thật là Task 6). Không sửa code.
- **050826** — **Kiểm chứng độc lập Task 4** (commit `65fed61`). **ĐẠT**: cả 2 lỗ đã sửa — `salaries` + sentinel `ErrTemplateImpactNoSalaries` (`payroll_service.go:749`, guard `:766`), `empManual` thật ở `:854`/`:855` (trước là `nil`); nợ giữ đúng chủ đích (`ComputeFormulaImpact` vẫn `nil` ở `:1046`/`:1050`); route `POST` giữ nguyên gate; 3/3 test PASS; full suite **6 fail** = baseline (phiên thi hành báo 7 — chênh 1 là test flaky). **ĐÍNH CHÍNH HAI ĐIỂM TRONG PHÂN TÍCH CỦA CHÍNH TÔI** (phiên thi hành bắt được điểm đầu, tôi lượng hoá điểm thứ hai): (1) brief tôi viết bắt test neo vào `PHONE_ALLOW` với lý do "nạp qua `payroll_manual_inputs`" — **SAI**. `buildInputValues` **hardcode 41 mã** lấy thẳng từ `attendance_summary`/`employees`, không qua `source_field` cũng không qua manual input, và `PHONE_ALLOW` (= `att.PhoneAllowance`) nằm trong đó. Tức có **BA** nguồn giá trị cho cột `input`, không phải hai như tôi mô tả — tôi bỏ sót nguồn hardcode. Phiên thi hành đổi sang `ADJ_PLUS` là đúng. **Đây là lỗi nguy hiểm, không phải chi tiết**: `PHONE_ALLOW` có giá trị sẵn nên test neo vào nó **sẽ xanh ngay khi chỉ sửa lỗ 1** — đúng cái bẫy "cảm giác an toàn giả" mà brief được viết để tránh, và tôi lại tự cài vào chính brief. (2) khẳng định "delta = 0 tuyệt đối / báo cáo luôn nói không có tác động" là **nói quá**. Đo lại 66 mã maskable theo nguồn giá trị: **11 mã** đến từ hardcode attendance (`PHONE_ALLOW`, `FUEL_ALLOW`, `LIVING_ALLOW`, `OTHER_ALLOW_TAX`, `TRANSPORT_ALLOW`, `TRIP_OVERRIDE`, `MEALS_BASE/OT/TOTAL`, `RESP_SAL`, `DEPENDENT_CNT`) → báo cáo cũ **vẫn thấy delta**; **55 mã** đến từ `payroll_manual_inputs` → **mù hoàn toàn**. Phát biểu đúng: cổng duyệt cũ **mù với 55/66 mã (83%)** và **sai thang đo cho mọi cột dẫn xuất lương** — vẫn đủ nghiêm trọng để chặn bật enforce, nhưng comment gốc của repo và mục 14.6(3) của tài liệu đều nói quá. Ghi chú phụ phiên thi hành nêu (đúng, không phải bug): template 0 component → `buildTemplateMaskFormulas` trả `nil` sớm theo thiết kế TASK-REF; nhưng dễ bị đọc thành "tính năng không chạy" nên Task 3 cần hiện số cột của template. Tài liệu: `Salary-Structure-Template-Analysis-260726.md` mục 17 (mới). Không sửa code.
- **050826** — Soạn prompt **Task 4** (`self-docs/files/prompt-Task4-050826.md`) và **mở rộng phạm vi Task 4 trong PLAN từ 1 lỗ thành 2 lỗ** sau khi xác minh. Bản PLAN cũ (và cả tài liệu mục 14.6(3)) chỉ thấy lỗ `salaryOverrides` rỗng. Truy tiếp thì thấy **lỗ thứ hai chí tử hơn**: `computeEmployee(sh, emp, att, empManual)` được `Calculate` truyền `empManual` thật (`:356`, nạp ở `:253-254` qua `manualInputRepo.GetByPeriod`+`ToMap`) và `CalculateOne` cũng vậy (`:668-669`, `:707`), nhưng **cả hai hàm impact truyền `nil`** (`:824`/`:825` template-impact, `:1016`/`:1020` formula-impact). **Lỗ 2 chỉ lộ ra SAU Task 2**: tập mask nay là 66 mã cột NGUỒN có `source_field=''` ⇒ giá trị của chúng đến từ `payroll_manual_inputs` ⇒ với `empManual=nil` thì **toàn bộ 66 mã maskable = 0 ở cả hai lần mô phỏng** ⇒ delta bằng 0 **tuyệt đối**, không phải "gần 0". ⇒ **Sửa lỗ 1 một mình là "sửa mà không sửa"**: mask phụ cấp vẫn cho delta 0, báo cáo vẫn trắng, mà lúc đó đã coi là xong và test đã xanh. Và tệ hơn: **phép nghiệm thu tôi viết trong PLAN trước (neo vào `[BASIC_SAL]`) sẽ XANH khi mới sửa lỗ 1** — tức chính test của tôi tạo ra cảm giác an toàn giả cho đúng ca dùng thật (ca dùng thật là mask PHỤ CẤP; `BASIC_SAL` nằm trong blocklist nên **không bao giờ** bị mask). Đã sửa: test phải **neo vào một phụ cấp bị mask** (nạp qua `payroll_manual_inputs`) và phải **chứng minh FAIL nếu revert riêng lỗ 2**. Prompt cũng chốt: đổi `GET`→`POST` cho `template-impact` (an toàn vì FE 0 client), giữ **nguyên** bộ gate (khớp đúng gate mà `POST /formula-impact` đang dùng), khuôn handler chép từ `payroll_formula_impact_handler.go` (mảng `salaries[]` → map, sentinel → **422**), và **KHÔNG sửa `ComputeFormulaImpact`** dù nó cùng lỗi `empManual=nil` — ghi thành nợ có chủ đích để không phình phạm vi và không phải đo lại baseline của nó. Tài liệu: PLAN Task 4 viết lại (bảng tiến độ ghi "HAI lỗ"), `document-map.md`. Không sửa code.
- **050826** — **Kiểm chứng độc lập Task 2** (commit `1ede75d`). **ĐẠT**: `statutoryOverrideBlocklist` nay là `{PIT, SI_EMP, HI_EMP, UI_EMP}` (thay 3 mã chết, comment ghi rõ lý do); `maskableCodes()` tự tính lại độc lập ra **đúng 66 mã**; tập mask thực tế tại guard = 66 ∪ 6 sibling = 72; **6/6 test mới PASS**; full suite **6 case fail** = đúng baseline RBAC (phiên thi hành báo 7 — chênh 1 là test flaky `...ComputeFormulaImpact...`, không phải hồi quy). **Allowlist `templateMaskSiblingSplits` là ĐÚNG, và nó sửa một lo ngại SAI của chính tôi**: mục 16.5 tôi từng xếp `PHONE_TAX`/`PHONE_NONTAX`/`TRANSPORT_TAX`/`FAMILY_HEALTH_INS` vào nhóm nguy hiểm "mask phần tách trong khi cột cha còn giá trị → mâu thuẫn". Đọc công thức thật thì **`GROSS` tham chiếu các cột TÁCH, KHÔNG tham chiếu cột cha** (`GROSS = [EARNED_SAL]+[MEAL_TAX]+[MEAL_NONTAX]+[PHONE_TAX]+[PHONE_NONTAX]+...`) — cột cha chỉ là bậc trung gian, nên mask cột tách **loại đúng phần tiền đó khỏi GROSS**, là cơ chế đúng chứ không mâu thuẫn. Kiểm cả 6 mã: không mã nào là cột tổng. **HỆ QUẢ PHIÊN THI HÀNH KHÔNG NHẬN RA — quyết định "chấp nhận không mask được phụ cấp cơm" (16.5) nay LỖI THỜI.** `MEAL_ALLOW` vào `GROSS` **chỉ qua** `MEAL_TAX` + `MEAL_NONTAX` (đã kiểm, không đường nào khác), và `MEAL_NONTAX=MIN(x,730000)`, `MEAL_TAX=MAX(0,x-730000)` nên **`MIN(x,730k)+MAX(0,x-730k) = x` chính xác** ⇒ mask cả hai cột tách **gỡ TRỌN VẸN phụ cấp cơm khỏi GROSS**, không sót đồng nào; và cả hai đều nằm trong allowlist 6 mã. ⇒ **Template BỎ ĐƯỢC phụ cấp cơm**, chỉ là qua hai cột tách chứ không qua `MEAL_ALLOW`. **Việc phải đổi ở Task 3**: kế hoạch cũ định hiện `MEAL_ALLOW` disabled kèm lý do *"không bỏ được phụ cấp cơm"* — **câu đó nay SAI, không được hiện**. UI phải giải thích **quan hệ cha–tách**, không chỉ liệt kê cột phẳng; nếu không HR sẽ tick `MEAL_ALLOW` (vô tác dụng vì không maskable) rồi kết luận tính năng hỏng. Tài liệu: `Salary-Structure-Template-Analysis-260726.md` mục 16.6 (mới). `00-START-HERE.md` đã xoá đúng L5 khỏi bảng lỗi (còn L2, L6-L10). Không sửa code.
- **050826** — Lên PLAN + prompt cho **Task 2** (`self-docs/files/prompt-Task2-050826.md`), sau **hai vòng đo bác bỏ hai quy tắc** — cả hai đều do chính tôi đề xuất trước đó. **Đo lại trên 140 component** (số cũ đo trên 126 đã lỗi thời vì chuỗi self-heal `giatbh` thêm 15 cột): L1 "cột NGUỒN" 95/45, L2 bỏ 27 cột `format='days'` → 68, L3 bỏ 1 mã có `source_field` → 67, trừ blocklist → **66 mã**. **L1 là lớp gánh chính và đây là lý do nó đúng**: nó chặn toàn bộ 45 cột dẫn xuất **mà không cần biết tên** — đã kiểm 21 mã tổng/pháp định (`GROSS`, `NET_PAY`, `NET_PAY_HOME`, `TAXABLE_INC`, `TAXABLE_GROSS`, `TOTAL_INS`, `TOTAL_DED`, `TOTAL_PIT`, `EARNED_SAL`, `PIT`, `INS_SAL_BH`, `SI_EMP`, `HI_EMP`, `UI_EMP`, `KPCD_CTY`, `UNION_FEE`…) đều bị chặn ⇒ 15 cột `giatbh` mới thêm tự an toàn, không phải bảo trì danh sách. **Vòng đo thứ hai bác bỏ chính lựa chọn ban đầu của user**: user chọn "nới cho cột formula không phải cột tổng"; đo ra quy tắc đó lọt thêm **13 mã** mà chỉ **1** là thứ muốn (`MEAL_ALLOW`) — 12 mã collateral gồm `BONUS_TOTAL` (tổng 13 cột thưởng), `CONTRACT_TOTAL` (**cơ sở tính bảo hiểm**, đúng một trong 10 mã vừa lệch ở L12), `TOTAL_SUPPORT`, `TOTAL_POST_ADD`, `PROB_SAL`/`PROB_EARNED`/`EARNED_PAID_LEAVE` (sai lương), `DEPENDENT_DED` (sai thuế), `PHONE_TAX`/`PHONE_NONTAX`/`TRANSPORT_TAX`/`FAMILY_HEALTH_INS` (mask phần tách trong khi cột cha còn giá trị → mâu thuẫn). **Lý do gốc: độ sâu trong đồ thị KHÔNG phản ánh "có phải cột tổng"** — `BONUS_TOTAL` tham chiếu 13 cột nguồn vẫn là cột tổng nhất có thể; ba cột tổng lớn may vẫn bị chặn nhưng là **may, không do quy tắc**. Bản chất: `MEAL_ALLOW` là **cột chính sách biểu đạt bằng công thức**, không thuộc tính dữ liệu nào phân biệt được nó với `BONUS_TOTAL` ⇒ muốn đáng tin thì phải đánh dấu tường minh (phương án C, đã loại vì cột phân loại bị V4 xoá mỗi boot). Báo lại và user **đổi quyết định: bỏ luôn, chấp nhận không mask được phụ cấp cơm** ⇒ thuật toán sạch, **không danh sách ngoại lệ nào**. Chốt cuối: **phương án A nghiêm** — blocklist tối thiểu (pháp định + `BASIC_SAL`), **không** mẫu tên; chấp nhận có ý thức `ADVANCE_1`/`DEPENDENT_CNT`/`CHARITY_DED`/`*_ADJ` vẫn mask được nếu HR tick, hàng rào là UI Task 3 (L6). Prompt nêu tường minh **cả 2 quy tắc đã bị bác bỏ** để agent không vô tình dựng lại, + Bẫy 1 (**6 biến bơm runtime** `CONTRACT_TYPE`/`EMP_TYPE`/`IS_FOREIGNER`/`WORK_TYPE`/`HAS_SECOND_CONTRACT`/`HAS_TAX_COMMITMENT` không phải component, phải loại khỏi đồ thị — có test riêng), + nghiệm thu định lượng "đúng 66 mã" kèm danh sách phải-có/phải-không-có, + ràng buộc "nếu không ra 66 thì DỪNG, đừng sửa blocklist cho khớp con số". Tài liệu: PLAN Task 2 viết lại hoàn chỉnh (TT `chờ QĐ`→`chưa`), `Salary-Structure-Template-Analysis-260726.md` mục 16.5 (mới). Không sửa code.
- **050826** — Thi hành **Task 3** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (`Core System-backend@feature_v2` + `Core System-frontend@feature_v2`, theo brief `prompt-Task3-050826.md`) **ngay trong phiên phân tích/lên kế hoạch** theo yêu cầu người dùng — đóng **L6** (picker không lọc cột tổng/pháp định). BE: `templateMaskEligible` = `maskableCodes()` ∪ `templateMaskSiblingSplits` (một nguồn sự thật cho guard lẫn API — đo thật ra đúng **72** mã trên 140 component), `templateMaskReason` (5 nhánh, xác nhận `MEAL_ALLOW` chỉ đường qua `MEAL_TAX`/`MEAL_NONTAX` chứ không nói "không bỏ được"), 2 field `Maskable`/`MaskReason` (`db:"-"`) điền ở `SalaryComponentService.List`. FE: `lib/template-picker.ts` (logic thuần, 6 test vitest), picker disable + tooltip + banner dữ liệu cũ, không tự bỏ chọn. **Kiểm tay trình duyệt thật — bắt buộc theo brief, đã làm bằng Playwright+Chromium** (cài tạm, gỡ lại sau) qua dev-login có sẵn (`e2e/devLogin.ts`): đi đúng luồng người dùng thật (tab Công thức → nút ribbon → modal) → xác nhận **`GROSS` click không đổi trạng thái** (disabled thật ở tầng DOM, không chỉ CSS), **`MEAL_ALLOW` disabled với lý do đúng**, **`PHONE_ALLOW` click tick được** và bộ đếm cập nhật ngay. Không có gì bị lưu vào DB (không bấm Lưu, `template_components` vẫn 0 dòng). Phát hiện phụ: `pnpm add playwright` làm `node_modules` vỡ tạm thời (thiếu `picocolors` → `next dev` lỗi 500 toàn trang) — `pnpm install` lại tự phục hồi, không liên quan code Task 3. Verify cuối: BE `go test ./...` 6 fail = baseline, `gofmt`/`go vet` sạch; FE `pnpm lint` 0 error, `pnpm test` 6/6 xanh. Dọn dẹp đúng ràng buộc: trả `SALARY_STRUCTURE_FEATURE_VISIBLE` về `false`, gỡ `playwright` khỏi package.json/lockfile, xoá 2 script Playwright tạm. Tài liệu: `Salary-Structure-Template-Analysis-260726.md` mục 18 (mới), PLAN cột TT Task 3 → `xong`, `00-START-HERE.md` xoá L6. **Chưa commit** — chờ xác nhận người dùng.
- **060826** — Thi hành **Task 3** của PLAN `040826` ngay trong phiên (không dispatch riêng) — kiểm tay trình duyệt thật bằng Playwright + Chromium cài tạm (dev-login `e2e/devLogin.ts`), xác nhận `GROSS` disabled thật ở DOM (click không đổi trạng thái), `MEAL_ALLOW` disabled đúng lý do, `PHONE_ALLOW` tick được bình thường. Dọn dẹp đúng ràng buộc: trả `SALARY_STRUCTURE_FEATURE_VISIBLE` về `false`, gỡ `playwright` khỏi package.json/lockfile. **Đã commit** `Core System-backend@fed1568` + `Core System-frontend@52b1076` trên `feature_v2` (chưa push), sau khi user xác nhận. Kiểm chứng độc lập **PLAN mới `060826-report-template-config-v1-PLAN.md`** do một phiên khác soạn (7 task, cấu hình template báo cáo Nhóm 1 + sửa payslip role) — không tin báo cáo, tự chạy: `llmwiki-validate.py path ...` → **exit 0** (gate-clean thật); grep xác nhận 2 lỗi đường dẫn đã sửa đúng (`PayslipSummaryModal.tsx` ở `components/Core System/`, `PreviewGrid`/`cfgGrid` ở `components-page/tinh-luong/report-preview.ts`); **query DB độc lập xác nhận đúng 19 mã deduction + 11 mã employer_cost = 30/30**, quét thêm không sót mã nào. Soạn prompt dispatch **Task 1** (`self-docs/files/prompt-Report-Task1-060826.md`) — kiểm trước khi viết brief: mốc chữ cái `BY` mà PLAN ghi hiện **chưa bị trùng** (dòng cuối slice thật vẫn là `BX`), nhưng cảnh báo tường minh trong brief phải **tự kiểm lại tại thời điểm chạy** vì mốc này đã bị `giatbh` chiếm 2 lần liên tiếp trước đó (`BO→BV`, `BP→BW`, `BQ→BX`) — không tin số ghi sẵn trong PLAN.
- **050826** — **Kiểm chứng độc lập bản sửa L12+L13** (commit `bf42175`, working tree sạch). **ĐẠT**: 0 mã thiếu version đang mở, 0 mã có >1 version mở (bất biến giữ), 0 lệch `sc.formula` vs version; nhãn chữ cái đã đổi `BO→BV`/`BP→BW`/`BQ→BX` tránh trùng `giatbh`. **PHÁT HIỆN QUAN TRỌNG NHẤT: L12 hoá ra ĐÃ XẢY RA THẬT, không còn là giả thuyết.** Bóc 25 dòng version sinh lúc `11:09:28` thành 2 nhóm (dựa vào việc mã đó có dòng đã-đóng trước đó hay không): **10 mã bị ĐÓNG vì lệch rồi mở lại** — `CONTRACT_TOTAL`, `EARNED_SAL`, `HI_EMP`, `INS_SAL_BH`, `INS_SAL_UI`, `KPCD_CTY`, `RESP_EARNED`, `SI_EMP`, `UI_EMP`, `UNION_FEE` — **7/10 là bảo hiểm/công đoàn**. Nghĩa là chuỗi self-heal của `giatbh` đã sửa 10 công thức này và **nếu không có bản hoà giải thì toàn bộ 10 bản sửa bị vô hiệu âm thầm**, bảng lương vẫn tính bằng công thức bảo hiểm CŨ, không lỗi không cảnh báo. Bản sửa bắt được đúng lúc. 15 mã còn lại là component MỚI (`BONUS_SAVE_*`, `BUDGET_SAVE`, `NET_PAY_HOME`, `PROB_SAL`, `RESP_SAL`…) do câu (2) tự phủ. **Hai đính chính số liệu:** (a) component là **140** chứ không phải 141 — con số 141 là số *version đang mở*, lệch 1 vì có **một version mồ côi** `RESPONSIBILITY_ALLOW` (component đã bị `giatbh` bỏ/rename, rất có thể thành `RESP_SAL`); vô hại vì mọi phép đọc `FROM salary_components` rồi mới `LEFT JOIN`, nhưng bất biến phải phát biểu là *"mọi component ĐANG TỒN TẠI có đúng một version mở"*; (b) baseline là **6** chứ không phải 7 — chênh 1 là test fail NGẪU NHIÊN `...ComputeFormulaImpactComputesDeltaWithoutWriting` (assert số đếm toàn cục `payroll_records` trong khi package test chạy song song), cả 2 lần đo đều 0 hồi quy, đừng ai đi truy con số này. **Một báo động sai của tôi, ghi lại để không lặp:** thấy `RESPONSIBILITY_ALLOW` biến mất, tôi quét tham chiếu treo và ra 5 mã (`CONTRACT_TYPE`, `EMP_TYPE`, `HAS_SECOND_CONTRACT`, `HAS_TAX_COMMITMENT`, `IS_FOREIGNER`) — **không phải lỗi**, đó là **biến bơm runtime** từ `employees` (`payroll_service.go:1463-1472`), không phải component; và `RESPONSIBILITY_ALLOW` KHÔNG treo ⇒ rename sạch. **Bài học chuyển thành ràng buộc cho Task 2**: đồ thị phụ thuộc phải loại 5 mã bơm-runtime ra khi phân loại nguồn/dẫn xuất, nếu không một cột chỉ tham chiếu biến runtime sẽ bị xếp nhầm là "dẫn xuất" (không mask được). Tài liệu: `Formula-Versioning-PointInTime-050826.md` mục 10 (mới). Không sửa code.
- **050826** — Soạn prompt cho bản sửa **L12+L13** (`self-docs/files/prompt-L12-L13-050826.md`) và **tách khỏi Task 2** theo đúng ghi chú của user ("nếu có xung đột hãy tách"). Xung đột **không phải ở file** (L12/L13 chỉ chạm `database.go`; Task 2 chạm `payroll_template_mask.go` + `salary_component_service.go` — không giao nhau) mà ở **mức sẵn sàng**: kiểm chứng trước khi viết brief đã **bác bỏ chính quy tắc cốt lõi của Task 2 mà tôi viết trong PLAN**. Quy tắc cũ ("chỉ mask cột LÁ — cột không ai tham chiếu tới") đo trên 126 component thật thì **chặn đúng những cột tính năng tồn tại để mask**: `PHONE_ALLOW` (bị `PHONE_TAX`/`PHONE_NONTAX` tham chiếu), `MEAL_ALLOW`, `FUEL_ALLOW`/`LIVING_ALLOW`/`OTHER_ALLOW_TAX` (bị `GROSS`), `TRANSPORT_ALLOW`, `RESPONSIBILITY_ALLOW`, `HEALTH_INS` — **chỉ 24/126 mã là lá**, trong nhóm phụ cấp thăm dò chỉ `CONS_ALLOW`. **Sai ở chiều quan hệ**: mask-về-0 hoạt động NHỜ đồ thị lan truyền (mask `PHONE_ALLOW` → 2 cột tax/nontax tự về 0 → `GROSS` tự tính lại), nên "có người phụ thuộc" là **điều kiện để mask có tác dụng**, không phải dấu hiệu nguy hiểm. Nếu giao brief đó, Sonnet sẽ cài xong, **test xanh, mà mask không mask được gì có nghĩa** — loại hỏng khó phát hiện nhất vì mọi bất biến đều thoả. **Quy tắc thay thế đã đo: "chỉ mask cột NGUỒN"** (không tham chiếu cột nào khác) — 85 nguồn/41 dẫn xuất, **tự bảo vệ toàn bộ 41 cột dẫn xuất** (`GROSS`/`NET_PAY`/`TAXABLE_INC`/`TOTAL_INS`/`TOTAL_INS_CTY`/`EARNED_SAL`/`PIT`/`INS_SAL_BH`) mà không cần biết tên, nên HR thêm cột tổng mới cũng tự an toàn; cho mask 8/9 phụ cấp thăm dò. Cộng 2 lớp lọc dữ liệu đã đo: `format='days'` loại 26 cột đếm ngày, `source_field<>''` loại 1 mã — còn 58 ứng viên. **Hai ngoại lệ quy tắc dữ liệu không xử được**: `BASIC_SAL` lọt cả 3 lớp (nguồn, `source_field` rỗng, `format=currency`) nhưng mask nó là thảm hoạ (10 cột tham chiếu trực tiếp) ⇒ buộc có blocklist tường minh; `MEAL_ALLOW` là `formula` tham chiếu `MEALS_TOTAL` ⇒ bị chặn, tức **template không thể bỏ phụ cấp cơm** — giới hạn thật. Trong 58 ứng viên còn `ADVANCE_1`/`DEPENDENT_CNT`/`CHARITY_DED`/`*_ADJ` không nên mask nếu bị tick. ⇒ Task 2 chuyển sang **`chờ QĐ`** trong bảng tiến độ PLAN; bước 2 của Task 2 được **đình chỉ tường minh** kèm bảng số đo để người thi hành không làm theo bản sai. Viết `Salary-Structure-Template-Analysis-260726.md` **mục 16** (mới): đính chính + quy tắc thay thế + **3 phương án A/B/C** cho phạm vi blocklist (khuyến nghị B — blocklist theo mẫu tên, giữ được tính chất "cột tổng mới tự an toàn"; cảnh báo nếu chọn C thì cột phân loại trên `salary_components` sẽ **bị V4 xoá mỗi boot** nên đó là task riêng, không phải "thêm một cột") + quyết định riêng cho `MEAL_ALLOW`. Không sửa code.
- **050826** — **Kiểm chứng độc lập Task 1** (commit `b4d5ec6`, đã push `origin/feature_v2`) — tự chạy lệnh, không tin báo cáo. **ĐẠT toàn bộ**: `employee_payroll_templates` đã vào `RunMigrations`; seed versions **126 dòng = 126 component, đúng MỘT mốc chân trời** `2026-08-05 10:06:06.333616+07`; seed **idempotent** (tự chạy 2 lần, 126→126); `TestIntegrationCalculateOnePointInTimeFormula` **chuyển XANH** khi chạy cách ly; full suite còn **đúng 6 fail** = baseline `HANDOVER` (giảm từ 7 như dự đoán); `TemplateEnforceHealth` + handler trả `healthy`/`reason` giữ `enabled` nên không phá FE. **Tự đính chính một báo động sai của mình**: ban đầu tôi kết luận "chưa push" vì `git branch -r --contains` trả rỗng — sai, `origin/feature_v2` tồn tại và bằng HEAD; hash cũ `036cd67`/`05a2227` vẫn còn trong object store nhưng không còn trên nhánh do rebase, và PLAN/`CLAUDE.md` đã được cập nhật hash mới đúng. **PHÁT HIỆN QUAN TRỌNG NHẤT (L12) — bẫy do chính Task 1 cài, chưa gây hại nhưng đã nạp đạn:** sau khi seed, **cả 126/126 mã đều có version** ⇒ tầng 2 của `ListActiveAsOf` (`earliest`, **không lọc ngày**) luôn khớp ⇒ **tầng 3 `sc.formula` không bao giờ với tới nữa** ⇒ **`salary_components.formula` không còn ảnh hưởng kết quả tính lương**. Hệ quả: **quy ước sửa công thức bằng self-heal migration của repo — đã dùng ≥8 lần (BG PIT, BH trần BHXH, BI HEALTH_INS, BJ OT_TAX, BO–BU của `giatbh`) — từ nay ÂM THẦM VÔ HIỆU**: grep xác nhận `database.go` chỉ có **đúng 1** câu `INSERT INTO salary_component_versions` (chính seed), tức không migration nào tạo version; nên lần sửa tới sẽ đổi `sc.formula` đúng, `psql` kiểm thấy đúng, mà `Calculate` vẫn dùng formula CŨ từ version đang mở — không lỗi, không cảnh báo, và người sửa sẽ đi kiểm đúng chỗ rồi thấy đúng. Đúng loại lỗi tệ nhất mà cả tập tài liệu này liên tục cảnh báo. Hôm nay **0/126 lệch** nên chưa sai số nào — và lý do khớp là **may nhờ đặt seed cuối slice** (Bẫy 4 của brief) nên nó chụp sau toàn bộ self-heal; kiểm cả 2 mã `giatbh` vừa sửa trong `7e1928b` (`INS_SAL_BH`/`INS_SAL_UI` nay có `CONTRACT_TYPE`/`HAS_SECOND_CONTRACT`, trần 50.6tr/106.2tr) đều khớp. Nhưng seed có guard `NOT EXISTS` nên **không bao giờ chạy lại**. **Phát hiện phụ (L13):** **124/126** mã có version đang mở — `OT_TAX` và `TRANSPORT_ALLOW` bị đóng lúc 10:06:17/10:06:40 mà **không có dòng thay thế** (tổng vẫn 126) → rác test (`InsertVersion` đóng+chèn, rồi `t.Cleanup` xoá dòng mới nhưng không mở lại dòng seed). Nhẹ vì tầng 2 vẫn đỡ, nhưng **seed không tự chữa được** — kiểm thực nghiệm: chạy seed 2 lần, 2 mã vẫn mồ côi, vì guard là `NOT EXISTS (version nào)` chứ không phải `NOT EXISTS (version ĐANG MỞ)`. **Đề xuất sửa (chưa làm, chờ quyết định):** một migration "hoà giải lệch" đặt cuối slice, 2 câu idempotent — (1) đóng version đang mở nếu `sc.formula/name/rounding` đã lệch, (2) mở version mới cho mọi mã không còn version đang mở; câu (2) **thay luôn seed hiện tại** vì guard chặt hơn, nên gộp được, và tự chữa cả L13. Điểm cần xác nhận nghiệp vụ: nó khiến kỳ **đang mở** dùng formula mới kể từ mốc sửa — cần chốt đây đúng là hành vi muốn. Tài liệu: `Formula-Versioning-PointInTime-050826.md` mục 8 (mới, 6 tiểu mục), `00-START-HERE.md` thêm **L12**/**L13**. Không sửa code.
- **050826** — Thi hành **Task 4** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (`Core System-backend@feature_v2`, theo brief `prompt-Task4-050826.md`, sau Task 0/1/2 + L12/L13) — đóng **L7**: `ComputeTemplateImpact` (cổng duyệt shadow-mode duy nhất trước khi bật `PAYROLL_TEMPLATE_ENFORCE`) trước đây **luôn** báo "không có tác động" vì **HAI lỗ**, không phải một — brief cũ chỉ thấy lỗ 1 (`salaryOverrides` rỗng cả 2 lần mô phỏng); lỗ 2 (`empManual=nil`) chỉ lộ ra SAU Task 2 (tập mask nay là 66 mã cột nguồn qua `payroll_manual_inputs`, không phải attendance) — sửa lỗ 1 một mình vẫn cho delta 0 tuyệt đối. Xác nhận lại đúng 4 vị trí brief nêu trước khi sửa. Sửa cả 2 lỗ: thêm tham số `salaries`, sentinel `ErrTemplateImpactNoSalaries`, guard rỗng; nạp manual inputs đúng khuôn `Calculate` (`manualMap[emp.ID]`, không phải `empCode`) truyền vào cả 2 `computeEmployee`. Đổi route `GET`→`POST` (giữ nguyên gate), handler chép khuôn `payroll_formula_impact_handler.go`. **Phát hiện brief sai khi viết test**: ví dụ neo `PHONE_ALLOW` không đúng — mã đó lấy từ `attendance_summary` (hardcode), không qua manual input; đổi sang `ADJ_PLUS` (input, không hardcode, nằm trong công thức `GROSS`). Phát hiện phụ: template gán cho NV có 0 component thì `buildTemplateMaskFormulas` trả `nil` sớm (đúng thiết kế TASK-REF, không phải bug) — phải cho template có ít nhất 1 mã thật. **Đã chứng minh test bắt được CẢ HAI lỗ bằng revert-và-khôi-phục** (không chỉ tin nó xanh): revert riêng lỗ 2 → FAIL đúng kỳ vọng (`EmployeesAffected=0`); revert riêng lỗ 1 → FAIL đúng kỳ vọng (`GrossBefore` chỉ còn 300000, thiếu lương cơ bản) — cả 2 lần khôi phục diff y hệt bản gốc. `go test ./...` sau sửa: 7 fail khớp tuyệt đối baseline, 0 hồi quy. Nghiệm thu request thật: `POST {}` → 422, `POST` có `salaries` → 200, `GET` cũ → 405, `payroll_records` không đổi (6435 dòng, md5 y hệt). Nợ có ý thức: `ComputeFormulaImpact` (`:1016`,`:1020`) vẫn truyền `empManual=nil`, cùng họ lỗi nhưng cố ý không sửa (ngoài phạm vi, có test riêng đang xanh). Tài liệu: `Salary-Structure-Template-Analysis-260726.md` mục 17 (mới), PLAN cột TT Task 4 → `xong`, `00-START-HERE.md` xoá L7. Commit `Core System-backend@65fed61` trên `feature_v2` — **đã push**.
- **050826** — Thi hành **Task 2** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (`Core System-backend@feature_v2`, theo brief `prompt-Task2-050826.md`, sau Task 0/1 + bản sửa L12/L13) — đóng **L5**: `statutoryOverrideBlocklist` đổi 3 mã chết (`BHXH_NV`/`BHYT_NV`/`BHTN_NV`, không tồn tại DB) sang mã bảo hiểm NV thật (`SI_EMP`/`HI_EMP`/`UI_EMP`), thêm `maskableCodes()` (hàm thuần, quy tắc "cột nguồn nghiêm" đã chốt ở `Salary-Structure-Template-Analysis-260726.md` mục 16.5: L1 không tham chiếu component khác + L2 `format<>days` + L3 `source_field=''` + blocklist∪BASIC_SAL) nối vào `buildTemplateMaskFormulas` thay guard `component_type=="system"` đã chết. Đo thật 140 component: **đúng 66 mã maskable**, khớp tuyệt đối kỳ vọng. **Phát hiện giữa chừng, đã DỪNG hỏi 2 lần**: áp `maskableCodes()` thẳng vào guard làm vỡ 2 test Task 4 có sẵn (`TestTemplateEnforce_DependencyKept`, `...FinalizeSnapshotsTemplateMask` — kịch bản chọn lẻ 1 sibling trong nhóm, `MEAL_TAX` không `MEAL_NONTAX`) vì `maskableCodes()` coi mọi cột dẫn xuất là không-maskable, kể cả sibling hợp lệ cần mask khi không được chọn. Đã thử và loại 3 quy tắc thuần đồ thị (đếm breadth, kiểm phụ-thuộc-cũng-maskable, kiểm ai-phụ-thuộc-ngược) — cả 3 hoặc không bảo vệ được `GROSS` hoặc mở lại đúng 12 mã mục 16.5 đã bác bỏ. User chọn Phương án B (mục 16.4): thêm `templateMaskSiblingSplits`, danh sách tường minh khép kín 6 mã "phần tách thuế" (`MEAL_TAX`/`MEAL_NONTAX`/`PHONE_TAX`/`PHONE_NONTAX`/`TRANSPORT_TAX`/`FAMILY_HEALTH_INS`, xác nhận bằng SELECT trực tiếp), tách riêng khỏi `maskableCodes()` — guard cuối: `closure[c.Code]` thắng trước, rồi mask khi `maskable[c.Code] || templateMaskSiblingSplits[c.Code]`. 66 mã không đổi sau khi thêm allowlist (đã đo lại xác nhận). 6 test bắt buộc + 1 test cũ sửa lại (`BHXH_NV`→`HI_EMP` để test đúng nhánh blocklist) — **đã chứng minh `TestMask_NeverMasksAggregateEvenIfTemplateListsIt` bắt được bug thật** (tạm khôi phục guard cũ → FAIL đúng kỳ vọng `GROSS` lọt mask → khôi phục, diff y hệt bản gốc) và xác nhận 2 test Task 4 cũ vỡ lại đúng dự đoán khi tạm bỏ allowlist. `go test ./...` sau sửa: 7 fail khớp tuyệt đối baseline, 0 hồi quy. Khẳng định không đổi số lương thật: `PAYROLL_TEMPLATE_ENFORCE` tắt mặc định, `buildTemplateMaskFormulas` chỉ gọi trong `if sh.templateEnforce` — xác nhận bằng đọc code + `payroll_records` md5 không đổi (6435 dòng, chưa từng gọi `Calculate` lên dữ liệu thật). Tài liệu: `Salary-Structure-Template-Analysis-260726.md` mục 16.6 (mới), PLAN cột TT Task 2 → `xong`, `00-START-HERE.md` xoá L5 (giữ L6 cho Task 3). Commit `Core System-backend@1ede75d` trên `feature_v2` — **đã push**.
- **050826** — Thi hành bản sửa **L12+L13** (`self-docs/files/prompt-L12-L13-050826.md`, `Core System-backend@feature_v2`, chen vào trước Task 2 của PLAN template) — đóng bẫy Task 1 vô tình cài: sau khi seed 1 version cho mọi component, tầng 2 (earliest, không lọc ngày) của `ListActiveAsOf` luôn khớp nên tầng 3 (`sc.formula`) thành code chết → quy ước sửa công thức bằng self-heal migration (đã dùng ≥8 lần) **từ nay âm thầm vô hiệu** (L12), cộng 2 mã mồ côi không có version mở do rác test (L13). Baseline đo lại: **859 passed / 7 failed** (khớp danh sách sau Task 1, không phải "6" ghi trong brief — đo ở thời điểm khác). Preview bằng `BEGIN;...ROLLBACK;`: `UPDATE 0` + `INSERT 0 2` → `126/126`, `0` lệch — khớp đúng kỳ vọng. Thay hẳn `MigrationSeedInitialComponentVersions` (giữ nguyên tên const) bằng migration "hoà giải" 2 câu: (1) đóng version đang mở nếu lệch `sc.formula`/`name`/`rounding`, (2) mở version mới cho mọi mã không còn version mở — thứ tự tuyệt đối không đảo (đảo thì mã lệch có 2 version mở, phá bất biến). Áp thật sau xác nhận `AskUserQuestion`: lần 1 `UPDATE 0`/`INSERT 0 2`, lần 2 `UPDATE 0`/`INSERT 0 0` — idempotent. 2 test mới (`salary_component_versions_reconcile_integration_test.go`): `TestReconcile_MigrationFormulaFixTakesEffect` (L12, **đã chứng minh FAIL nếu bỏ câu (1)** — tạm bỏ/khôi phục bằng diff, không dùng git vì có sửa khác chưa commit) + `TestReconcile_RepairsCodeWithNoOpenVersion` (L13). `go test ./...` sau sửa: 7 fail khớp tuyệt đối baseline, 0 hồi quy. Nhân tiện sửa 1 xung đột nhãn chữ cái tiềm ẩn từ lúc rebase Task 1 lên `origin/giatbh` trước đó: `BP`/`BQ` của Task 1 (đặt trước khi biết `giatbh` cũng dùng đúng 2 chữ đó cho migration khác) đổi thành `BW`/`BX` — thuần comment, không đổi tên const/hành vi. **Nghiệm thu tầng thật**: restart backend thật 2 lần qua `RunMigrations()` — lần 1 là **lần đầu chuỗi self-heal mới của `giatbh` chạy thật** (nâng tổng component 126→141), kết quả `141/141` mở, `0` lệch; lần 2 y hệt (idempotent qua đường thật, không chỉ `psql -f`). Tài liệu: `Formula-Versioning-PointInTime-050826.md` mục 9 (mới) + 8.5/8.6 cập nhật "đã làm", `00-START-HERE.md` xoá L12/L13, PLAN thêm ghi chú ở mục Tiến độ. Nợ còn lại: rác test gây L13 chưa sửa tận gốc (migration tự lành mỗi boot nên không hại), test `...ComputeFormulaImpact...` vẫn fail ngẫu nhiên (không liên quan). Commit `Core System-backend@bf42175` trên `feature_v2` — **đã push**.
- **050826** — Theo yêu cầu người dùng ("push các thay đổi lên gitlab"): phát hiện `feature_v2` (local) chưa từng có remote tracking branch, và `origin/giatbh` (gốc của `feature_v2`) đã có thêm 1 commit mới (`7e1928b` — đồng nghiệp `giatbh` sửa 10 gap công thức lương thật, đổi `INS_SAL_BH`/`INS_SAL_UI` sang tham chiếu `CONTRACT_TOTAL`) mà `feature_v2` chưa có. Hỏi và được chọn **rebase lên `origin/giatbh` trước khi push** (thay vì push nguyên trạng). `git rebase origin/giatbh` conflict ở `database.go` cho cả 2 commit Task 0/1 — **xung đột thuần cộng dồn** (2 nhánh cùng thêm migration mới ngay sau `migrationSelfHealHideOfficialDays`, không đụng logic/bảng nào của nhau: `giatbh` sửa `salary_components`/`employees`, mình sửa `template_components`/`salary_component_overrides`/`employee_payroll_templates`/`salary_component_versions`). Giải bằng cách giữ cả 2 phía, **đổi nhãn chữ cái comment của mình từ BO→BV** (BO–BU đã bị nhánh `giatbh` chiếm — chỉ đổi comment, KHÔNG đổi tên const `MigrationDurableComponentCode` vì test đã tham chiếu). Sau rebase: build/vet sạch, `go test ./...` **7 fail** — khớp đúng baseline sau Task 1 (không hồi quy), `repository`+`service` package pass 100%. Push `git push -u origin feature_v2` — nhánh remote **lần đầu tồn tại** (trước đó chỉ có local). Cập nhật lại commit hash trong PLAN/`CLAUDE.md` (hash đổi do rebase: `036cd67`→`6dbe1a6`, `05a2227`→`b4d5ec6`). Commit `Core System-backend@b4d5ec6` trên `feature_v2` — **đã push**.
- **050826** — Thi hành **Task 1** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (`Core System-backend@feature_v2`, theo đúng brief `prompt-F2-Task1-050826.md` của phiên trước): đóng cả **L4** (`employee_payroll_templates` chỉ có trong Atlas) **và L11** (`salary_component_versions` rỗng, versioning vô hiệu) trong cùng một task vì cùng một gốc — "bootstrap đặt sai chỗ". Baseline đo lại tại thời điểm này: **8 fail** (không phải 24 như Task 0, vì lần restart nghiệm thu Task 0 trước đó đã vô tình tạo bảng `salary_component_versions` lần đầu, xoá 16/24 fail cũ thuộc lỗi "relation does not exist"). Bước 1: grep xác nhận `employee_payroll_templates` = 0/0 trong `database.go`/`schema.sql`, `main.go` không gọi Atlas — khớp brief. Bước 2: `MigrationEmployeePayrollTemplatesTable` (mốc **BP**) chép nguyên schema Atlas (đã idempotent sẵn), **chủ ý bỏ `COMMENT ON TABLE`** lỗi thời. Bước 3: `PayrollTemplateRepo.Ping` + `isUndefinedTableError` (SQLSTATE `42P01`) + `PayrollService.TemplateEnforceHealth(ctx) (enabled,healthy,reason)` — enforce bật mà bảng thiếu thì `slog.Error` (fail-loud) nhưng `Calculate` vẫn fail-safe (không sập tính lương); handler đổi `map[string]bool`→`map[string]any`, không phá FE cũ (chỉ đọc `.enabled`). Bước 4: 2 test mới (rename bảng thật trong test, không transaction vì `Ping()` có thể lấy connection khác trong pool; `t.Cleanup` phục hồi). Bước 5: `MigrationSeedInitialComponentVersions` (mốc **BQ**, đặt cuối slice sau BO) — guard `WHERE NOT EXISTS` (bảng không có unique constraint nên không dùng được `ON CONFLICT`), `effective_from=now()` đã chốt, **không** chép nguyên file Atlas `fix_key.sql` (không guard + `created_at` nói dối). Preview bằng `BEGIN;...ROLLBACK;` (126 dòng, 1 mốc chân trời) → áp thật vào `payroll_engine` sau khi xác nhận qua `AskUserQuestion` → chạy lại lần 2 `INSERT 0 0` (idempotent) → `TestIntegrationCalculateOnePointInTimeFormula` chạy cách ly **chuyển xanh** — nghiệm thu tự nhiên. Bước 6: `go test ./...` sau sửa: **7 fail**, khớp đúng baseline 8 trừ 1, 0 hồi quy. **Nghiệm thu tầng thật**: start backend thật với `PAYROLL_TEMPLATE_ENFORCE=on` → `GET /Core System/template-enforce-status` trả `{"enabled":true,"healthy":true}` → đổi tên bảng thật → gọi lại: `{"enabled":true,"healthy":false,"reason":"..."}` đúng yêu cầu → đổi tên lại → về `healthy:true`. Tài liệu: `Report-Template-Config-Analysis-040826.md` mục 0.6 (mới), `Formula-Versioning-PointInTime-050826.md` mục 6 (số đo thật), PLAN cột TT Task 1 → `xong`, `00-START-HERE.md` xoá L4/L11 khỏi bảng lỗi + cập nhật mục 0/3.1. Commit ban đầu `05a2227`, sau đó **rebase lên `origin/giatbh`** (xem dòng 050826 riêng bên dưới về rebase+push) → hash mới `b4d5ec6`, đã push.
- **050826** — Thi hành **Task 0** của PLAN `040826-Core System-template-on-giatbh-engine-PLAN.md` (`Core System-backend@feature_v2`, theo đúng brief dispatch của phiên trước, không tự đổi phương án): **RE-LINK** — `component_code` là khoá bền, `component_id` hạ xuống cột dẫn xuất nối lại từ code mỗi boot bằng migration mới `database.MigrationDurableComponentCode` (mốc **BO**, chạy sau V4). Baseline đo lại (không chép tài liệu cũ): `736 passed / 24 failed` — con số "6 test RBAC" ghi trong PLAN đã lỗi thời. Chứng minh khuyết tật bằng `BEGIN;...ROLLBACK;` (`truoc=1 sau=0`) trước khi sửa. Sửa 3 câu INSERT (đường ghi) ghi thêm `component_code`: `payroll_template_repo.go` `ReplaceComponents`, `salary_component_override_repo.go` `Create`+`CreateTx`. Migration idempotent (chạy 2 lần liên tiếp không lỗi), áp thật vào `payroll_engine` sau khi xác nhận qua `AskUserQuestion`. 4 test mới (2 durability + 2 orphan) — **đã chứng minh test thật bắt được bug**: revert code sửa → test FAIL đúng như kỳ vọng → áp lại → xanh. `go test ./...` sau sửa: 24 fail khớp tuyệt đối baseline (so bằng `diff`), 0 hồi quy. **Nghiệm thu ở tầng thật**: tạo 1 override thật cho `MEAL_ALLOW`, **restart backend thật** (`go run ./cmd/Core System`), xác nhận `component_id` cũ đã biến mất khỏi `salary_components` (V4 reseed thật) nhưng dòng override còn nguyên và `component_id` đã tự nối lại đúng id mới. Dọn dữ liệu nghiệm thu + 1 dòng rác rò từ vòng revert-để-chứng-minh, DB dev về lại `tc=0 sco=0`. Tài liệu: `self-docs/Report-Template-Config-Analysis-040826.md` mục 0.4 (mới), PLAN cột TT Task 0 → `xong`, `self-docs/00-START-HERE.md` xoá L1/L3 khỏi bảng lỗi + cập nhật mục 3.1. Commit ban đầu `036cd67`, sau đó **rebase lên `origin/giatbh`** → hash mới `6dbe1a6`, đã push (xem dòng 050826 riêng về rebase+push).
- **050826** — Soạn **prompt dispatch tự chứa cho Task 1**: `self-docs/files/prompt-F2-Task1-050826.md`. Xác minh trước khi viết (không chép brief cũ) và bắt được **2 điểm tương phản quyết định cách viết brief**: **Phần A rẻ bất ngờ** — DDL Atlas `20260726000000_employee_payroll_templates.sql:23-49` **vốn đã idempotent** (`CREATE TABLE IF NOT EXISTS` + 3 `CREATE INDEX IF NOT EXISTS`) nên gần như copy được nguyên, chỉ phải bỏ `COMMENT ON TABLE` lỗi thời (nội dung còn ghi "Task 4 CHƯA làm" dù Task 4 xong từ 270726); ngược lại **Phần B là bẫy** — backfill Atlas không guard + dùng `created_at` nói dối (đã ghi thành Bẫy 1-2). Xác minh thêm: `loadTemplateMaskData` giờ ở `payroll_service.go:400` (nuốt lỗi 2 chỗ), `TemplateEnforceStatus()` ở `:391` trả `bool`, handler trả `map[string]bool` nên thêm `reason` (string) phải đổi sang `map[string]any`; FE chỉ đọc `.enabled` nên thêm field **không phá FE** (và Task 6 mới sửa FE). Điểm thiết kế đáng chú ý nhất đưa vào brief: **ràng buộc "KHÔNG đổi giá trị lương" kèm phép nghiệm thu md5** — vì seed lấy đúng `sc.formula` hiện tại nên cả 3 tầng COALESCE trả cùng giá trị, số PHẢI không đổi; nếu đổi là làm sai và phải DỪNG. Đây là ranh giới giữa "an toàn" và "hy vọng". Cộng nghiệm thu Phần A ở tầng thật (rename bảng → `template-enforce-status` phải trả `healthy:false` + `reason`, không còn trả `{"enabled":true}` trơn). Baseline ghi rõ **7 case (đo thật), sau task còn 6** vì `TestIntegrationCalculateOnePointInTimeFormula` chuyển xanh — và cảnh báo 1 test fail NGẪU NHIÊN để agent không sửa code oan. Không sửa code.
- **050826** — Chốt `effective_from = now()` cho seed `salary_component_versions` (quyết định của user) và **viết tài liệu bàn giao chi tiết theo yêu cầu**: `self-docs/Formula-Versioning-PointInTime-050826.md` (canonical mới cho hạng mục "versioning công thức theo thời điểm"). Trước khi viết đã xác minh cơ chế để tài liệu không mô tả sai — và **bắt được 3 sự thật quan trọng mà nếu không kiểm sẽ viết sai tài liệu lẫn viết sai code**: (1) `SalaryComponentRepo.Update` (`:121`) **KHÔNG ghi version nào** — chỗ ghi là `InsertVersion` (`:155`), gồm 2 câu trong transaction: `UPDATE ... SET effective_until` đóng version đang mở, rồi mới `INSERT` version mới (nên bất biến là **mỗi mã tối đa MỘT version mở**); (2) `salary_component_versions` **KHÔNG có unique constraint nào trên `component_code`** (chỉ PK `id` + index lookup không unique) ⇒ **`ON CONFLICT` vô dụng**, seed buộc phải dùng `WHERE NOT EXISTS`; (3) **file Atlas backfill `20260803020000_*.sql:28` là một cái bẫy nếu chép nguyên** — nó `INSERT ... SELECT ... FROM salary_components` **không có guard nào** và dùng `effective_from = sc.created_at`; chép vào `RunMigrations()` sẽ chèn thêm ~110 dòng **mỗi boot** (migration ở đó chạy lại mỗi lần) và đóng băng một mốc **nói dối** vì `created_at` là *thời điểm boot gần nhất* (V4 xoá+chèn lại mỗi boot, cả 110 dòng cùng 4 ms) chứ không phải tuổi component. ⇒ **Đã tự sửa một chỉ dẫn nguy hiểm trong PLAN của chính mình**: bước 5 Task 1 trước đó ghi "chép logic từ file Atlas — đừng tự phát minh", nay thay bằng SQL đúng kèm cảnh báo tường minh. Tài liệu ghi đầy đủ: cơ chế **COALESCE 3 tầng** của `ListActiveAsOf` (đúng-ngày → sớm-nhất-không-lọc-ngày → `sc.formula` hiện tại) và **bảng diễn biến 6 ca A-F** cho thấy vì sao `now()` đủ dùng — mấu chốt là **seed biến tầng 2 từ cái bẫy thành cái chốt**: khi bảng rỗng, tầng 2 trả về version của lần sửa gần nhất (nên kỳ cũ trôi theo — chính là bug), còn khi đã có version khởi điểm thì tầng 2 luôn trả công thức **tại chân trời** và không đổi nữa dù sau này sửa bao nhiêu lần. Khái niệm **"chân trời lịch sử"** (history horizon) + 3 điều nó KHÔNG giải quyết (không tái tạo lịch sử trước chân trời — muốn đúng kỳ 05-07/2026 phải nhập tay từ file lương đã chốt; **chân trời khác nhau giữa dev/staging/prod nên cùng một kỳ có thể ra số khác nhau giữa 2 môi trường**, phải kiểm `min(effective_from)` trước khi so số; không thay `payroll_formula_snapshots`) + **4 bẫy thi hành** + 6 bước nghiệm thu (trong đó `TestIntegrationCalculateOnePointInTimeFormula` chuyển XANH là phép nghiệm thu tự nhiên, và baseline giảm từ 7 xuống 6) + 4 câu hỏi mở (V1 nhập tay lịch sử trước chân trời; V2 đồng bộ chân trời giữa môi trường; **V3 `PIT` cố ý KHÔNG dùng versioning** nên thuế TNCN vẫn tính lại kỳ cũ theo công thức mới — nên xác nhận lại vì chạm tuân thủ thuế; V4 test assert số đếm toàn cục). Nối chéo: PLAN Task 1 bước 5 (viết lại), `00-START-HERE.md` (L11 trỏ doc mới + thêm dòng "đọc gì theo chủ đề"), `Report-Template-Config-Analysis-040826.md` mục 0.5 (trỏ sang, không lặp nội dung), `document-map.md`. Không sửa code.
- **050826** — **Kiểm chứng độc lập Task 0** (phiên khác thi hành, commit `036cd67` trên `Core System-backend@feature_v2`) — không tin báo cáo, tự chạy lệnh. **ĐẠT**: 2 FK CASCADE đã bỏ thật (`pg_constraint` xác nhận, bảng duy nhất còn FK vào `salary_components(id)` là `salary_component_history` với `SET NULL` — vô hại), cột `component_code` + 2 index có thật, **cả 3 câu INSERT đều điền `component_code`** (đúng bẫy số 1 của brief — quên chỗ này thì dòng mới vẫn mất), const `MigrationDurableComponentCode` exported + wired ở cuối slice (`database.go:294`), diff **5 file/+330−6 không đụng handler/service/route/FE/e2e** đúng ràng buộc. **Nhưng 2 đính chính:** (1) con số "24 fail khớp baseline" **sai** — dựng `git worktree` tại `HEAD~1` đo lại: **7 case fail trước, 8 sau**; chênh đúng 1 là `TestIntegrationComputeFormulaImpactComputesDeltaWithoutWriting`, và **không phải hồi quy** (chạy cách ly pass 3/3) mà là **nhiễu test song song**: nó assert số đếm TOÀN CỤC `payroll_records` (6435→6436) trong khi Go chạy các package test song song trên cùng DB dev và một test khác gọi `Calculate` ghi 1 dòng — Task 0 thêm 4 test nên đổi thời điểm, làm cú đụng dễ xảy ra hơn; nợ hạ tầng test cần sửa bằng cách đếm theo `period_id` của chính test, không phải `-p 1`. (2) `HANDOVER-giatbh.md` mục 3 liệt kê 6 test fail sẵn nhưng thật là **7** — thiếu `TestIntegrationCalculateOnePointInTimeFormula`. **PHÁT HIỆN MỚI (L11), nặng hơn cả Task 0 vừa sửa:** tính năng **versioning hiệu-lực-theo-ngày của `giatbh` đang VÔ HIỆU** trên DB này. `salary_component_versions` **tồn tại nhưng 0 dòng**; `ListActiveAsOf` là **COALESCE 3 tầng** `COALESCE(ver.formula, earliest.formula, sc.formula)` nên bảng rỗng ⇒ luôn rơi về tầng cuối = **công thức HÔM NAY cho MỌI kỳ** — đúng cái bug mục J bàn giao được xây để diệt (*"sửa 1 formula hôm nay sẽ tính lại luôn các kỳ QUÁ KHỨ theo formula mới"*). Nguyên nhân **cùng họ L4**: `database.go` chỉ `CREATE TABLE IF NOT EXISTS`, **không có INSERT nào**; backfill nằm **duy nhất** ở `atlas/migrations/20260803000000_*.sql:26` + `20260803020000_*.sql:28` mà **backend không bao giờ chạy Atlas**. Không phải lỗi logic của họ — lỗi đặt bootstrap sai chỗ. **Hệ quả tự đính chính tài liệu của chính mình:** `Salary-Structure-Template-Analysis-260726.md` mục 15.4 ghi mask "tốt lên vì closure tính trên công thức đúng của kỳ" — đúng thiết kế, **sai thực tế**, vì `ListActiveAsOf` đang inert; đã ghi đính chính tại chỗ. **Hệ quả lên Task 1:** nay có **HAI nạn nhân cùng gốc** (`employee_payroll_templates` L4 + backfill versions L11), Task 1 phải xử lý cả hai. Tài liệu: `Report-Template-Config-Analysis-040826.md` mục 0.5 (mới), `00-START-HERE.md` (thêm L11, sửa sự thật #5 bằng số đo thật, cập nhật 3.1). Không sửa code.
- **040826** — Viết lại **Task 0** của PLAN template theo phương án **RE-LINK** + soạn **prompt dispatch tự chứa** cho phiên Sonnet thi hành (`self-docs/files/prompt-F1-Task0-040826.md`). Phiên phân tích + tạo prompt, **không sửa code**. **Việc xác minh trước khi viết brief đã cứu một phen**: Task 0 bản cũ ("đổi khoá sang `component_code`") lớn hơn ước lượng ~10 lần — đo thật ra **14 hàm** `salary_component_override_repo.go` (7 hàm là write-path cell-pin của `SetCell`/`UndoCell`), 4 câu SQL ngoài file đó, **đổi hợp đồng route** `/config/salary-components/{id}/overrides` (`{id}` là UUID trong URL), **13 call site FE** mà `tsc` KHÔNG bắt được (cả `id` lẫn `code` đều là `string`), 3 chỗ e2e, và override repo **chưa có test nào**. **Phương án nhỏ hơn ~10 lần, user chốt: RE-LINK** — `component_code` là khoá bền, `component_id` hạ xuống **cột dẫn xuất** được nối lại từ code mỗi boot bằng `UPDATE ... FROM salary_components` đặt SAU V4 (tất định vì `salary_components` có `UNIQUE (code)` = `salary_components_code_key`). Kết quả: dữ liệu sống qua reseed **và vẫn hoạt động**, mà KHÔNG đụng route/handler/chữ ký hàm/FE/e2e — chỉ 1 migration mới + **3 câu INSERT** thêm subquery điền `component_code` + test. **Phân xử được 1 mâu thuẫn giữa 2 agent khảo sát** (điều nếu để lọt vào brief sẽ khiến agent thi hành đoán): agent A nói DDL `template_components` nằm ở `database.go:946`, agent B nói grep = 0 hit — tự kiểm, **A đúng** (const `migrationPayrollTemplates` dòng 937, wired ở slice dòng 97). Bẫy quan trọng nhất ghi vào brief: **quên sửa đường GHI** thì mọi dòng mới có `component_code=NULL` và mất ở reseed kế — đúng bug đang sửa, chỉ chậm hơn. Cộng 5 bẫy nữa (không idempotent — repo dùng guard `pg_constraint` với `DO $$`, **không có tiền lệ `ADD PRIMARY KEY` idempotent**; test đè DB dev 110 component thật nên phải mô phỏng reseed bằng component `ZZ_F1_TEST` dùng-một-lần; import cycle `repository`↔`database`; chép baseline test từ tài liệu; `gofmt -w` cả thư mục). PLAN cũng được thêm **cột `TT` + mục "Tiến độ"** (trước đó không đọc được tiến độ từ đâu, dù repo vốn có quy ước này ở PLAN 220726) và ghi rõ đây là nơi DUY NHẤT ghi tiến độ. Chưa sửa code, chưa commit.
- **040826** — Tạo **đầu tài liệu** `self-docs/00-START-HERE.md` theo yêu cầu (người dùng đọc theo file trong `self-docs/`, cần một chỗ mở ra là thấy toàn cảnh). Trước khi tạo đã kiểm vai của 2 index có sẵn để không trùng: `document-map.md` là **catalog theo file** (60 mục, trả lời "file X là gì"), nhật ký trong `CLAUDE.md` trả lời "hôm đó làm gì" — **vai còn thiếu là "đang ở đâu, đọc gì, làm gì tiếp"**. Đặt tên `00-` để luôn nằm đầu khi `ls`. Nội dung 6 mục: (1) **5 sự thật kỹ thuật phải biết trước khi sửa bất cứ gì** — `salary_components` phù du, backend KHÔNG chạy Atlas, `migrations/vNN.sql` sống đúng 1 boot, nhánh thật là `feature_v2`=`origin/giatbh` (tài liệu cũ ghi `sec_dev`/`feature_1` đã lỗi thời), test có fail sẵn nên phải tự đo baseline; (2) **bảng 10 lỗi đã biết CHƯA SỬA xếp theo mức gây hại** — L1 override lương mất sạch mỗi restart (đang sai tiền im lặng), L2 phiếu lương xếp 21 mã sai vào cột thu nhập, L3-L9 nhóm template, L10 RLS vô hiệu vì app chạy bằng role superuser; (3) 3 luồng việc đang mở + việc kế tiếp; (4) 4 quyết định đang chờ user (Q1 mô hình gán, Q2 ngày hiệu lực, Q3 semantics V4, Q4 dọn dữ liệu demo); (5) đọc gì theo chủ đề; (6) quy ước cập nhật. **Thiết kế để không mục đi**: file chỉ chứa pointer + 1 dòng trạng thái, ghi rõ "file canonical thắng nếu mâu thuẫn", và quy ước **xoá dòng ở mục 2 khi lỗi đã sửa** thay vì đánh dấu "đã sửa" rồi để tích tụ (lịch sử đã có ở nhật ký). Đồng thời sửa `CLAUDE.md`: đổi mục "Tài liệu bảo mật/phân quyền" thành mục "Tài liệu" có câu mở **"Mở `self-docs/00-START-HERE.md` TRƯỚC"** để phiên sau thật sự dùng nó, giữ nguyên phần RBAC thành mục con. Thêm entry vào `document-map.md`. Không sửa code.
- **040826** — Phân tích ý tưởng **bảng cấu hình template báo cáo + tái dùng rule cho payslip** (5 ý người dùng nêu). **Phiên phân tích/thảo luận, chưa chia task, không sửa code.** **Phát hiện nền quan trọng hơn cả 5 ý, và nó bác bỏ ý 2 ở dạng nêu:** `salary_components` là bảng **PHÙ DU** — `migrationSalaryComponentsV4` chạy `DELETE FROM salary_components` + reseed 109 mã hardcode **mỗi lần backend boot**, không `ON CONFLICT`, không giữ `id`. Đo được: toàn bộ 110 dòng có `created_at` trong **cùng 4 ms** (29/07); tập mã DB = đúng 109 mã V4 + `CONS_ALLOW`, **không mã nào do người dùng tạo**; **13 mã** mà `migrations/v51_*.sql` từng INSERT nay **mất sạch** (file đã `applied` nên không chạy lại, V4 xoá mỗi boot); **11/11** dòng `salary_component_history` có `component_id IS NULL` (FK `SET NULL` đã nổ 100%); `salary_formula_configs` **15/18** dòng trỏ mã không tồn tại. **Ba bảng FK vào `salary_components(id)`, hai bảng CASCADE bị xoá mỗi restart** — và cái nặng nhất KHÔNG phải `template_components` mà là **`salary_component_overrides`**: tính năng override lương theo phòng ban/nhân viên **đang chạy thật**, đọc ở 4 điểm tính lương (`payroll_service.go:240/609/729/914`), có UI từ 250726 → HR cấu hình ngoại lệ lương, restart, ngoại lệ **biến mất không dấu vết**, lương lặng lẽ về mặc định; **sai tiền, không lỗi, không truy vết được** (bảng không có cột `component_code`). **Đính chính ý 1:** hệ thống ĐÃ có nơi tạo báo cáo — **2 thế hệ / 20 báo cáo**, Gen-2 gồm 17 template `.xlsx` nhúng `go:embed`, dispatch bằng map registry (`report_export.go:25`, không phải `switch`), và **17/17 không gọi `componentRepo`** (mã component hardcode 15 chỗ: `cpdColMap` 13 entry, `bao-hiem` ~20 lệnh `cv.Get()` cứng theo ô A..AP…). **Không có bảng cấu hình báo cáo nào** (grep `report_template`/`report_config`/`report_definition`/`report_column` = 0). Phân loại **Nhóm 1 (13/20** — một dòng mỗi NV, cấu hình hoá được**) vs Nhóm 2 (5/20** — `so-lieu-ke-toan` chỉ 11 ô tổng, `chi-phi-pb-da` pivot 2 tầng × `AllocRatio` từ `attendance_computed`, `trich-thuong-luy-ke` lũy kế 12 tháng — **không** cấu hình hoá được ở v1). Phát hiện phụ đáng giá: **không có endpoint preview nào** — cái UI gọi là preview thực chất là export `.xlsx` rồi **parse lại ở trình duyệt bằng `exceljs`** → báo cáo cấu hình-hoá nên trả JSON thẳng. **Ý 3:** soi và loại cả 3 ứng viên tái dùng — `template_components` (bị CASCADE), `salary_formula_configs` (**bảng chết**: chỉ `database.go` CREATE/DELETE/INSERT, **0 dòng Go nào đọc**, 15/18 mã mồ côi — bài học về bảng cấu hình không ai đọc), `hris_payroll_reports` (bảng hạ cánh HRIS, 0 tham chiếu Go). Đề xuất 2 bảng mới khoá theo `component_code`, đặt trong `RunMigrations()` **không phải Atlas**. **Ý 4:** xác nhận 3 nút bạn nói bỏ qua đúng là vô tri (`Lọc`/`Sắp xếp` không có `onClick`; `Trường báo cáo` gated bởi `sheet.repKey` legacy nên luôn tắt); tìm được móc sẵn — state `reportCompany` được ĐỌC 2 chỗ nhưng **không chỗ nào SET**. Render tái dùng `PreviewGrid` + `buildPreviewView` + `cfgGrid`, có tiền lệ dựng thủ công không qua `.xlsx`. **Ý 5:** sheet Cấu hình **không phải** danh sách link `/admin` (13/14 mục mở sheet nhúng), khuôn đúng là `FORMULAS_LIST_SHEET_ID`. **Trả lời câu hỏi ngược của user ("dùng được code như payslip không"): ĐƯỢC** — payslip (`PayslipSummaryModal.tsx:32-42`) và báo cáo (`report_service.go:44,118`) vốn cùng đọc `payroll_records.computed_values` theo code; nhưng **đừng tạo component mới cho số dẫn xuất** (sẽ bị V4 xoá + engine tính thêm 1 cột cho 12.044 người) — làm ở tầng bố cục dạng `sum`. Kèm **bug thật chưa sửa**: `PayslipSummaryModal` phân loại thu nhập/khấu trừ bằng **đoán chuỗi con** (`DEDUCTION_KEYWORDS`), đối chiếu 102 mã `is_visible` thật thì chỉ nhận đúng 9 mã, **21 mã bị xếp thành THU NHẬP** gồm `TOTAL_INS`/`TOTAL_DED`/`ADVANCE_1` (tạm ứng) và **11 mã chi phí phía công ty `_CTY`** đúng ra không nên có trên phiếu lương nhân viên — modal này đang dùng thật (`PayrollPage.tsx:2876`). Đây biến ý 2 từ "tổ chức code gọn hơn" thành **bản sửa cho lỗi số liệu đang chạy**. **Quyết định user chốt:** sửa nền trước; v1 chỉ Nhóm 1; schema có `doc_type` ngay nhưng v1 chỉ thi hành report; ý 2 dùng bảng dòng-bố-cục riêng trỏ `component_code`. **Thứ tự chốt: F1 (Task 0 mở rộng) → F2 (Task 1) → báo cáo v1 → phần còn lại của template**; đổi semantics V4 sang UPSERT **hoãn có ý thức** (cần quyết định thật: `DO UPDATE` ghi đè sửa tay HR vs `DO NOTHING` làm fix ship ra không áp được, mà chuỗi self-heal BF→BN của `giatbh` đang dựa vào việc V4 luôn thắng) — và **không chặn báo cáo v1**. Đã cập nhật `040826-Core System-template-on-giatbh-engine-PLAN.md` ở 3 chỗ: **Task 0 mở rộng từ 1 sang 2 bảng** (thêm Phần B bước 9-12 cho `salary_component_overrides`, kèm bằng chứng repo ĐÃ `JOIN` lấy `sc.code` nên phía đọc vốn nói bằng code → sửa rẻ), bổ sung bằng chứng đo được cho G1, thêm **G5 cấm đổi semantics V4** trong phạm vi PLAN đó. Tài liệu: `self-docs/Report-Template-Config-Analysis-040826.md` (canonical, 443 dòng). Chưa sửa code, chưa commit.
- **040826** — Tái mở tính năng **"Cấu trúc lương theo cấp bậc"** (người dùng gọi là "template cho payslip") đang HOLD từ 270727: tổng hợp lại kiến trúc + logic, rồi lên PLAN mở rộng. **Phiên thuần phân tích + lên kế hoạch, không sửa một dòng code nào.** Xác định đúng đối tượng trước: không có "payslip template" nào khác trong 3 repo — đây là `payroll_templates`+`template_components`+`employee_payroll_templates`, thứ quyết định NV nào được tính cột nào. Tính năng **không bị comment** mà bị tắt bằng **2 công tắc ở 2 repo**: `SALARY_STRUCTURE_FEATURE_VISIBLE=false` (`TinhLuongExcel.tsx:57`, ẩn 3 nút ribbon) và env `PAYROLL_TEMPLATE_ENFORCE` không có trong `.env` (engine không mask). Người dùng chốt giữa phiên: **lấy các thay đổi engine từ commit của `giatbh` làm chuẩn chính, ta triển khai ý tưởng trên nền engine của họ** — phát hiện `feature_v2` HEAD **chính là** `origin/giatbh` (`bf1f46e`), hai bên đã cùng một cây, nên việc là so LOGIC chứ không phải merge; nhánh của họ có `Core System-backend/HANDOVER-giatbh.md` (15 nhóm thay đổi A-O). **PHÁT HIỆN CHÍ TỬ (mới, không có trong bất kỳ tài liệu nào):** bàn giao của `giatbh` mục K ghi bài học *"bất kỳ bảng mới nào FK vào `salary_components.id` sẽ bị xoá sạch — LUÔN dùng `component_code`"* (họ rút ra sau khi `salary_component_versions` của chính họ bị xoá 3 lần); `template_components` của ta FK **đúng** vào `salary_components(id)` `ON DELETE CASCADE`, và `migrationSalaryComponentsV4` — chạy **mỗi lần backend boot** — mở đầu bằng `-- STEP 1: XÓA TOÀN BỘ (cascade xóa template_components)` + `DELETE FROM salary_components;` (comment của chính tác giả V4, viết lúc tính năng template chưa tồn tại). **Bằng chứng thực nghiệm khớp hoàn hảo**: DB thật còn 1 `payroll_templates` (`demo 1`) + 1 `employee_payroll_templates` (`THAIDT001`) nhưng `template_components` = **0 dòng** — không phải "UAT chưa kịp chọn cột" như tôi đoán ban đầu mà là **dấu vết cấu hình đã bị xoá**. Hệ quả: sau mỗi restart mọi template có 0 cột → `len(templateCodes)==0` → mask trả `nil` → **enforce âm thầm thành no-op cho toàn bộ NV**, trong khi banner FE nói "enforce ĐANG BẬT — SẼ ảnh hưởng bảng lương thật". Tính năng vừa không chạy vừa nói dối là đang chạy. Khuyết tật thứ hai cùng họ: `employee_payroll_templates` **chỉ tồn tại trong `atlas/migrations/`**, mà backend **không bao giờ chạy Atlas** (`cmd/Core System/main.go` chỉ gọi `RunMigrations`+`RunFileMigrations`) — bảng có trên DB dev này chỉ vì phiên 260726 `psql -f` bằng tay; môi trường sạch thì thiếu bảng, `loadTemplateMaskData` nuốt lỗi nên enforce tắt im lặng dù status trả `enabled:true`. **Ba phát hiện bác bỏ chính tài liệu cũ của mình** (đo trên DB thật, không chép tài liệu): (1) "phòng thủ 3 lớp" của mask thực chất **chỉ còn 1 lớp** — `statutoryOverrideBlocklist` gồm 4 mã nhưng **chỉ `PIT` tồn tại** trong DB (mã bảo hiểm thật là `TOTAL_INS`/`INS_SAL_BH`/`INS_SAL_UI`/`TOTAL_INS_CTY`, **không mã nào trong blocklist**), và guard `component_type=='system'` khớp **0 dòng** sau khi bản fix PIT 270726 chuyển PIT sang `formula` → hệ quả phụ ngoài ý muốn của bản fix đó, chưa ai ghi lại; (2) biện pháp giảm rủi ro mà mục 7 tuyên bố "đã có" (*UI chỉ cho chọn cột phụ cấp, ẩn cột tổng*) **chưa bao giờ được xây** — `PayrollTemplateModal.tsx:134` là `allComponents.map(...)` không lọc gì, `GROSS`/`NET_PAY`/`TAXABLE_INC` đều tick được → ghép với (1) thành **đường đi im lặng từ một cú tick nhầm tới sai lương hàng loạt**, không lỗi, không test nào đỏ; (3) `ComputeTemplateImpact` truyền `salaryOverrides` **rỗng** cho cả 2 lần mô phỏng → `BASIC_SAL=0` cả hai → delta triệt tiêu → cổng duyệt **duy nhất** trước khi bật enforce luôn báo "không có tác động"; repo **đã tự biết** (comment `payroll_service.go:814-819`) và hàm sinh sau nó `ComputeFormulaImpact` đã chọn cách đúng là từ chối thẳng, hàm này chưa sửa theo. Thêm **lệch nguyên tắc** so với nền engine họ: đóng góp lớn nhất của `giatbh` (mục J — `ListActiveAsOf` tại 5 điểm) làm công thức từng cột point-in-time, `employee_payroll_templates` cũng có ngày hiệu lực, nhưng **"template gồm cột nào" thì không** → sửa tập cột hôm nay rồi tính lại kỳ quá khứ sẽ mask theo tập cột HÔM NAY, đúng cùng lớp bug mục J vừa loại bỏ. Cũng ghi nhận khoảng trống nghiệp vụ lớn nhất: tên là "theo cấp bậc" nhưng **dữ liệu không mô hình hoá cấp bậc ở đâu cả** (assignment khoá theo `employee_id`) → HR phải gán tay từng người trong **6.357** NV có cấp bậc (29 cấp bậc, 12.044 NV, 47% không có `employee_level_id`). Người dùng chốt: **chưa quyết mô hình gán**, yêu cầu phân tích sâu 2 phương án trước (đã viết mục 15.7: A = thêm bảng `level_payroll_templates` có ngày hiệu lực, resolve 2 mức, 29 dòng thay vì 6.357, NV đổi cấp bậc tự đổi cấu trúc, nhưng sức công phá rộng nên bắt buộc sửa cổng shadow-mode trước; B = giữ khoá theo NV + thêm gán hàng loạt, rủi ro kỹ thuật thấp nhất nhưng liên kết cấp bậc→cấu trúc không được lưu ở đâu); và **bật `PAYROLL_TEMPLATE_ENFORCE=on` chỉ ở dev local**. Tài liệu: `self-docs/Salary-Structure-Template-Analysis-260726.md` **mục 14 + 15** (canonical, không tạo file rời), PLAN thi hành `llmwiki/wiki/sources/draft/040826-Core System-template-on-giatbh-engine-PLAN.md` (10 task, 4 ràng buộc bao trùm G1-G4, Task 0/1 là GATE hạ tầng, Task 7/8 chờ quyết định; qua cổng harness R2/R7/R18 nên mỗi task có Files+Interfaces+code thật để dispatch được). Không tạo HTML (quy ước máy local). Chưa sửa code, chưa commit.
- **030826** — Xuất bảng đối chiếu **mã field template ↔ nguồn DB** ra Excel: `document/BangCong-Template-FieldMap-TASK-REF.xlsx` (116 mã, 1 dòng/mã, 2 sheet — bảng map + truy vấn gốc). **Không gõ tay dòng nào**: mã field đọc thẳng dòng 7 template đã nhúng qua `ReadFieldMap` (đúng G1, gồm 4 mã hỏng `{r}` đã chuẩn hoá), tên hiển thị từ dòng 8, cột "nguồn theo template" từ dòng 4 (file gốc tự khai `WD`/`Core System`/trống), biểu thức SQL nguyên văn từ `source_db.go` — nên file không thể cũ đi lệch khỏi code như tài liệu chép tay. Kết quả: **20 ĐÃ map**, 31 chặn Task 7b (lưới ngày), 65 chưa map — trong đó **23 mã có nguồn ỨNG VIÊN đã xác minh tồn tại thật bằng `\d`** trên DB restore (`ts_ot_reconciliation`, `ts_time_off_plan_balances_pivot`…), để riêng cột I và ghi rõ là GỢI Ý chưa nối, chưa đối chiếu giá trị nào. **Phát hiện đáng giá nhất: `DateQuit` (cột L) có nguồn nằm SẴN trong truy vấn hiện tại** — `wd_workers.termination_date` đang được đọc ở `WHERE` để lọc người nghỉ trước kỳ (436/4.211 NV có giá trị), chỉ là không xuất ra `Row`; mã dễ nối nhất trong 65 mã còn lại, không cần JOIN mới, không chờ quyết định nghiệp vụ. Công cụ `cmd/tmp-fieldmap` dùng một lần, **đã xoá** (build sạch, working tree sạch); ghi bằng excelize, kiểm bằng openpyxl + round-trip LibreOffice→PDF (đúng G9). Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 22.
- **020826** — `Core System-adapter@feature_v1`: chốt cổng hiệu năng A5 bằng **phương án (d)** — không nằm trong 3 lựa chọn (a)/(b)/(c) đã trình bày, vì cả 3 dựa trên một giả định mà chính phép đo D4 vừa bác: **cache số không cần thiết để Excel/LibreOffice hiển thị đúng** (tiêu đề PDF ra đúng "THÁNG 7/2026" dù `A2` KHÔNG có cache — trình đọc tự tính công thức không cache, chỉ bỏ qua khi cache đã tồn tại; nên §18.4 tôi viết "LibreOffice phớt lờ `fullCalcOnLoad`" là **kết luận sai của chính tôi**, thứ chắn đường là cache CŨ chứ không phải cái cờ). Tách `ClearStaleCache` khỏi `RecalcAll`; `Render` luôn xoá cache, **mặc định không tính lại**, bật lại bằng `WithRecalc()`/`--recalc`/`?recalc=1` cho trường hợp duy nhất cần (đọc `.xlsx` bằng code không qua ứng dụng bảng tính — hiện repo không có). **Kỳ đầy đủ 3.768 NV qua CLI thật: 8,5 phút (timeout) → 5,18 giây**, 463.928 công thức, 0 ô còn cache. Test `TestRender_DefaultPathCachesNothing` đọc XML thô khoá bất biến. Đã commit + **push** `e91ed3c`+`1473409` lên `origin/feature_v1` (`555840c` Task 7 đã có sẵn trên remote từ trước, không phải phiên này đẩy — các báo cáo cũ ghi "chưa push" đã lỗi thời). **Nhánh remote adapter là `feature_v1`, KHÔNG có `feature_1`** (đó là quy ước của backend/frontend). Bẫy kỹ thuật: máy này **không có lệnh `timeout`** — bọc `git fetch` trong `timeout 60` khiến lệnh chết im lặng và ahead/behind in ra số remote CŨ, trông hợp lệ mà sai. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 20.5.
- **010826** — `Core System-adapter@feature_v1` (chưa commit, phiên thứ 6): sửa D3+D4 (cache cũ trong `RecalcAll` từ phiên trước) + đo lại hiệu năng A5. D3: bug thật là ô THÀNH VIÊN nhóm shared-formula rơi về `<v>` CŨ (kỳ 202603) thay vì tính lại, vì `CalcCellValue` gặp ô đã mất `<f>` (bị `SetCellFloat` trên ô MASTER xoá cả nhóm) — **brief đề xuất pass "materialize" bằng `SetCellFormula` KHÔNG kèm `FormulaOpts`; đã KIỂM THỰC NGHIỆM và bắt được đề xuất đó SAI** (không đổi `c.F.T`, nên vẫn còn `shared` → vẫn bị xoá nhóm khi tới lượt ô cũ là master) — sửa đúng bằng `FormulaOpts{Type: normal, Ref: ""}` cho mọi ô, xác nhận bằng test tối giản + round-trip LibreOffice thật (soffice không báo lỗi/sửa chữa). D4: `UpdateLinkedValue` (API gợi ý brief) **xác nhận thực nghiệm XOÁ ĐƯỢC** `<v>` cache cũ của ô công thức chuỗi (tiêu đề báo cáo) — nhưng phát hiện phụ nó cũng xoá sạch `wb.CalcPr` (làm mất cờ `FullCalcOnLoad`), đã tự gọi lại `EnableFullCalcOnLoad` ngay sau bên trong `RecalcAll`. Test mới `TestRecalcAll_DoesNotLeakStalePeriodCache` khoá cả 5 assert brief yêu cầu (a-e), render cho kỳ 2026-07 (khác kỳ 2026-03 mọi test cũ dùng — lý do bug cũ lọt lưới): PDF đọc qua LibreOffice→`pdftotext` xác nhận tiêu đề đúng "THÁNG 7/2026", không còn "3/2026". Đo lại hiệu năng 20/200/1.000 dòng: chậm hơn tuyệt đối ở N nhỏ (+52%/+26%/+3,4% giảm dần theo N) nhưng **ngoại suy kỳ đầy đủ 3.768 dòng không đổi thực tế (~509s≈8,5 phút cả trước lẫn sau)** — 3 lựa chọn A5 (chấp nhận CLI nền/`--limit`/bỏ RecalcAll cho HTTP) giữ nguyên. Full suite cuối: 80 passed/24 skip (không `DATABASE_URL`), 104 passed/0 fail (có `DATABASE_URL`) — không hồi quy (baseline brief ghi "44/47" đã lỗi thời, không khớp HEAD thật). Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 20.
- **010826** — `Core System-adapter@feature_v1` (chưa commit): A1 điều tra block trùng chấm công (kết luận Workday `Is_Deleted="1"` không được lọc ở view/sync, 26/26 khớp giả thuyết, chưa sửa — thuần điều tra theo yêu cầu). A2 mở file thật bằng mắt (LibreOffice→PDF 23 trang) tìm ra bug MỚI, nặng hơn D1 cũ: `RecalcAll` làm hỏng GIÁ TRỊ CACHE của các ô THÀNH VIÊN nhóm shared-formula (không phải formula MASTER) khi master bị xử lý trước trong cùng vòng Pass 2 — sai cả tiêu đề báo cáo lẫn header lưới ngày, test cũ chỉ khoá số lượng/cấu trúc formula chứ không khoá giá trị nên không bắt được; ĐÃ CHỈ RA nguyên nhân + hướng sửa (RecalcAll 3-pass), CHƯA sửa (đúng phạm vi yêu cầu). A3 đóng lỗ hàng rào coverage: thêm `TestCoveredFieldCodes_ExistsAndIsSubsetOfTemplate` (không cần DB) + `TESTING.md` — phát hiện repo **không có CI nào chạy `go test` cả** (không chỉ 2 test cần `DATABASE_URL`). A5 (Task 8): thêm CLI `adapter report --period --out` + HTTP `GET /reports/bangcong?period=`, dùng chung `bangcong.CheckPeriodAvailable` (mới) báo lỗi kỳ không có dữ liệu kèm danh sách kỳ đang có. **Cổng hiệu năng đo thật 20/200/1.000 dòng dữ liệu DB thật** (không giả định): `RecalcAll` tuyến tính ~4,3s + 0,134s/dòng, kỳ đầy đủ 3.768 dòng ước ~8,5 phút — khớp CLI thật timeout >2 phút khi chạy không cắt. Trình bày 3 phương án đánh đổi (chấp nhận CLI nền / thêm `--limit` / bỏ RecalcAll cho HTTP), **không tự chọn**, chờ user quyết. Full suite cuối: 79 passed/24 skip (không `DATABASE_URL`), 103 passed/0 fail (có `DATABASE_URL`) — không hồi quy. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 18-19 (A1/A2), 19.1-19.2 (A3/A5).

- **310726** — Thi hành Task 7 (DBSource đọc DB thật) của PLAN `BangCong-Xlsx-Template-Golang-310726.md` (phiên thứ 4, brief bị cắt/hỏng giữa chừng ở đúng đoạn Task 7.1b-7.4 — đã đọc PLAN mục 14/15 để tái lập ý định, nói rõ chỗ suy luận). **Trước khi code, chạy lại 43 test cũ để lấy baseline — bắt được 3 test đỏ KHÔNG do phiên này gây ra**: `go.mod` đang ghi `excelize v2.11.0` (vi phạm G7 "giữ v2.10.0") — do nhánh `feature_v1` bị rebase lên `develop` mới (đồng nghiệp khác, commit `0bdd5de`) kéo theo 8 dependency bị nâng version. Excelize v2.11.0 đã SỬA đúng quirk evaluator (`#DIV/0!` giả kèm giá trị đúng) mà `recalc.go`'s `Diagnostics` được xây để sống chung — hành vi tốt hơn nhưng phá 3 test đang khoá bất biến đó. Sửa: `go get excelize@v2.10.0` + `go mod tidy`, xác nhận bằng thực nghiệm (xoá cache, build lại, 3 test đỏ tự xanh, không sửa dòng test nào) — đúng tinh thần "một dependency lệch version âm thầm đổi hành vi runtime". Task 7.1: `DBSource` JOIN `raas_monthly_attendance`+`wd_workers`(qua `worker_wid`)+`wd_organizations`(qua `supervisory_org_wid`, khoá FK — an toàn hơn `supervisory_org_reference_id` brief gợi ý, đo cả 2 cách đều khớp 3768/3768). **Phát hiện lớn nhất**: phần lớn field brief bảo lấy qua JSONB `raw->>'...'` **đã có sẵn cột phẳng `numeric(18,6)` tách riêng** trên `raas_monthly_attendance` — đối chiếu giá trị khớp 100% trên toàn mẫu, dùng cột phẳng an toàn kiểu hơn hẳn cast text. Task 7.1b: `raas_monthly_attendance.hire_date` vẫn TEXT (chưa có cột chuẩn hoá như lời hứa Q3), nhưng `wd_workers.hire_date` **đã sẵn kiểu `date`** (tồn tại độc lập, không liên quan lời hứa Q3) — JOIN lấy trực tiếp, không tự parse text ở Go. Task 7.2: kỳ DB chỉ có 1 (`2026-06-21→2026-07-20`, 3768 NV); lọc Q6 đo thật cho kết quả **0 người bị loại** (23 người có termination_date đều sau đầu kỳ) — quy tắc vẫn cài đúng, chỉ là dữ liệu kỳ này không kích hoạt; 3 dòng `company_reference_id` rỗng KHÔNG lọc (Q6 không nhắc company), 2/3 trông như dữ liệu test Workday nhưng không tự đoán loại. Task 7.5: **đính chính PLAN §14.2/§14.5 đã lỗi thời** — đọc lại `internal/db/migrations/` phát hiện cả 9 view + 2 bảng "chưa có migration" đó **đã có migration thật** (0007/0017/0018/0020/0022/0030/0031/0035, trên `develop`) từ lúc nào đó giữa lúc viết mục 14 và lúc thi hành Task 7 — không phải do phiên này, nhưng bắt được bằng cách đọc code thật thay vì tin PLAN. Cơ chế `requiredViews`+`to_regclass` vẫn cài (rỗng hiện tại, để sẵn cho Task 7b). **45 test case/29 hàm test — 0 fail** (từ 43 lên 45), `go build`/`go vet`/`gofmt` sạch toàn repo bao gồm cả các package khác (`internal/store`, `internal/workday`...). Chưa commit. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 16b (mới).

- **310726** — Sửa D1 (CỔNG) + quyết D2 + Task 5 + Task 6 của PLAN `BangCong-Xlsx-Template-Golang-310726.md` (phiên thứ 3, theo đúng thứ tự A→B→C→D). D1: `RecalcAll` cũ đọc-ghi trong 1 lượt làm excelize xoá cả nhóm shared-formula (416/464 công thức là shared) ngay khi ghi ô chủ — phá 295 ô ở dòng 6/7/8/9 (35/31/124/124 → 6/2/8/3). Sửa: 2 lượt tách biệt (đọc hết vào memory trước, ghi sau). Xác nhận lại bằng công cụ KIỂM độc lập (`xlsx_inspect.py` bổ sung đếm công thức theo dòng) trên file thật, không tự chấm điểm bằng excelize — khớp đúng baseline. Test hồi quy `TestRecalcAll_PreservesSharedFormulaGroups`. A2 (đi kèm D1): đổi `RecalcAll` trả `(Diagnostics, error)` — lỗi hạ tầng vẫn hard-fail, `CalcCellValue` lỗi nhưng giá trị parse được số thì tin giá trị + ghi diagnostic, bỏ nhánh skip-theo-số-dòng (dòng mầm giờ luôn có đúng 2 diagnostic `EK12`/`EO12`, steady-state bình thường). D2: đọc source `cell.go` xác nhận KHÔNG có API công khai nào vừa giữ công thức vừa đúng kiểu số (mọi setter giá trị đều `removeFormula`, `SetCellFormula` là hàm duy nhất không xoá formula nhưng gán cứng `t="str"` vô điều kiện) — chọn nhánh CHẤP NHẬN, khoá bằng test đọc thẳng XML thô qua `archive/zip` (không tin excelize tự báo cáo). Task 5: `Source`/`FixtureSource`/`Render` — 3 dòng fixture (đủ dữ liệu / thiếu vài mã / thiếu hẳn StdWorkDay), tính tay AS/BI/BJ/STT rồi kiểm chéo Go+LibreOffice khớp tuyệt đối 12/12 giá trị — **lần đầu STT được test với dữ liệu có cột G thật** (trước đó acceptance test không điền G nên STT luôn ra 0, chưa ai test được tiêu chí Task 3). Sửa luôn lỗi hạ tầng test brief chỉ ra (LibreOffice `--outdir` trùng đường dẫn input làm mất bản excelize gốc, khiến phiên trước quy sai lỗi D1 thành "LibreOffice phá công thức"). Task 6: `ReadFieldMap` đọc dòng 7 template — phát hiện dòng 7 làm 2 vai theo cột (lưới ngày M-AQ là công thức WEEKDAY, chỗ khác là mã field literal), lọc sai ban đầu ra 147 mã (lẫn 31 formula), sửa đúng còn 116, khớp mốc PLAN; 4 mã hỏng `{r}` chuẩn hoá đúng, bẫy `SeniorityRate` không lẫn vào `Seniority1..12`, 0 overlap mã field × công thức dòng mầm. **43 test case/27 hàm test — 0 fail** (từ 31 lên 43). `go build`/`go vet`/`gofmt` sạch toàn repo. Chưa commit. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 11 (mới); mục 9/10 đánh lại số vì trùng với báo cáo kiểm chứng độc lập của phiên khác (nội dung giữ nguyên, không sửa).

- **310726** — Thi hành Task 2 (GATE) + 3 + 4 + 4.5 của PLAN `BangCong-Xlsx-Template-Golang-310726.md` trong `Core System-adapter` (phiên độc lập tiếp nối Task 0+1, không làm lại — kiểm chứng độc lập trước khi bắt đầu). Task 2: `ShiftRows` (quét ký tự, cờ trong-chuỗi, chỉ dịch số dòng không `$`) — 13 test bảng + 1 test quét toàn bộ 123 công thức thật của dòng mầm, GATE XANH. Task 3: `Builder.ExpandRows(n)` nhân dòng mầm — **2 phát hiện tự bắt bằng test**: (1) 2 ô literal trong dòng mầm (`JB12`=ngày PayrollMonth, `BU12`=Used 0) phải chép verbatim (không qua `ShiftRows`), xác nhận bằng đọc `JB13..17`/`BU13..17` trong file NGUỒN trước khi Task 1 xoá; (2) brief nói footer "ROW()-relative, copy unchanged" — SAI, test bắt ngay: `JA<footer>` có tham chiếu CÙNG DÒNG (`F13`) không qua `ROW()`, phải qua `ShiftRows(formula,n)` như mọi công thức khác (trừ `JB`, range tĩnh phải "giãn" riêng). Task 4: brief/PLAN ghi SAI ô neo (`J6` — chỉ là công thức trung gian); đọc chuỗi `J6=K6=JB6=JB12` tìm ra `JB12` mới là ô literal thật. Phát hiện phụ: ghi `time.Time` trực tiếp làm excelize tự đổi style index (190→232) — sửa bằng tự tính serial number Excel rồi ghi `float64` thuần, giữ style. Task 4.5: **tự phát hiện 1 lỗi excelize nghiêm trọng KHÔNG có trong brief** — `SetCellFormula` gán cứng `c.T="str"` vô điều kiện, làm rò **INDEX bảng shared-string** ra làm giá trị cache khi cache bằng CHUỖI (bắt được nhờ hiện tượng công thức ra `22` lại hiện `684` — khớp đúng "chuỗi thứ 684"); sửa bằng chỉ cache kết quả SỐ qua `SetCellFloat` (bỏ qua shared-string hoàn toàn), chấp nhận NỢ: cột hiển thị mã chấm công (text, M-AQ) chưa cache được. Thêm phát hiện mở rộng lỗi `#DIV/0!`-dù-giá-trị-đúng mà brief chỉ tiên liệu cho dòng mầm — xác nhận nó xảy ra ở BẤT KỲ dòng nhân viên thiếu input, không riêng dòng mầm. Nghiệm thu end-to-end: chuỗi 2 tầng thật (`AS`,`BI`,`BJ`) tính tay rồi kiểm chéo **2 engine độc lập khớp tuyệt đối** — Go/excelize và `soffice --headless` (LibreOffice có sẵn máy) đọc lại bằng openpyxl (công cụ thứ 3). **20 test case/12 hàm test xanh 100%**, `go build`/`go vet`/`gofmt` sạch toàn repo. Chưa commit. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 9 (mới).
- **310726** — Thi hành Task 0 (GATE) + Task 1 của PLAN `BangCong-Xlsx-Template-Golang-310726.md` trong `Core System-adapter` (brief tự chứa, dispatch cho Sonnet). Task 0: pin `excelize v2.10.0`, round-trip `OpenFile→SaveAs` không sửa ô nào, kiểm bằng `tools/xlsx_inspect.py` (openpyxl, khác thư viện với excelize) — `diff before/after` **rỗng tuyệt đối** (76 merge, 1081 công thức, 123 công thức dòng 12, 22 fill/15 theme+tint khớp 100%) → GATE XANH. **Phát hiện mâu thuẫn thật giữa brief và PLAN gốc** (PLAN ghi `sanitize.py`, brief ghi `sanitize.go`+excelize) — đã hỏi qua `AskUserQuestion`, user chọn theo brief (khớp với G9 "không đảo vai công cụ ghi/kiểm" mà chính phiên trước đã tự thêm vào PLAN). Task 1: `sanitize.go` xoá 6 sheet thừa + dòng 44→26/20/17→13 (PII + `#REF!`), xoá theo thứ tự dòng cao trước, nhúng qua `embed.go`. **Tự phát hiện lỗi nghiêm trọng do chính excelize gây ra khi xoá dòng** (không phải do brief hay PLAN sai): `RemoveRow` tự "vá" tham chiếu công thức, và vá SAI 1 ô trong dòng mầm — `A12` (cột STT) gốc `=SUBTOTAL(3,$G12:G$13)` bị co thành `=SUBTOTAL(3,$G12:G$12)` (range suy biến) vì `$13` trỏ vào dòng nhân viên vừa xoá. Xác minh bằng cách đọc `A13..A17` **trong file nguồn trước khi mất** — thấy `$G$13` là điểm neo cố định của cả dãy STT (`A13..A17` = `SUBTOTAL(3,$G$13:G{row})`, cache 1,2,3,4,5), chứng minh `G$13` trong A12 là tham chiếu **có chủ đích hướng tới tương lai** (dòng 13 sẽ lại là dòng nhân viên đầu tiên sau khi Task 3 nhân dòng mầm) chứ không phải tham chiếu chết cần dọn. Quét toàn sheet 1-12+18 (bỏ qua chuỗi trong nháy kép, tránh bẫy `"TASK-REF"`/`"TASK-REF"`) xác nhận chỉ đúng 2 ô bị ảnh hưởng thật: `A12` (sai, đã vá lại bằng `SetCellFormula` ngay trong `sanitize.go` kèm comment giải thích) và `JB18` footer (excelize tự co đúng thành `JB2:JB12`, không cần sửa). Đối chiếu lại 3252 ô dòng 1-12 nguồn-vs-template sau khi vá: 0 sai khác. Phát hiện phụ: `f.GetSheetDimension()` bị stale sau `RemoveRow` (vẫn báo `A1:JK44` dù sheet thật còn 13 dòng) — ghi chú cho Task 3 không được tin hàm này. Test: 7/7 pass (`spike_test.go` + 6 test trong `template_test.go`, PII-check đọc động từ nguồn lúc chạy test, không hardcode tên/mã NV vào source). `go build`/`go vet` sạch. Chưa commit (đúng yêu cầu, để nguyên working tree). Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 8 (mới, "Kết quả thi hành Task 0 + Task 1").
- **310726** — Đội DB trả lời 6 câu Q1-Q6 (mục V4 tài liệu bàn giao) → phân tích + đối chiếu DB vừa restore + viết thiết kế Task 7 (PLAN mục 15) và brief dispatch. **Bốn câu dùng được ngay:** Q1 không cần guard sync (`DBSource` đọc thẳng, rủi ro chấp nhận có ý thức); Q2 tên field XML ổn định; Q3 đội DB sẽ flatten `hire_date` → ta parse ở Go nhưng viết sao cho tự dùng cột `date` khi nó xuất hiện; Q5 danh sách công ty lấy từ `wd_organizations`, đã tra đúng đường dẫn `raw->'Organization_Data'->>'Name'` (thật: `CTDGROUP_0001` 3.231 NV = Enterprise Construction JSC, `CTDGROUP_0002` 515, `TASK-TASK-REF` 17, **3 dòng company_reference_id RỖNG**, `TASK-TASK-REF`/`TASK-TASK-REF` 1/1); Q6 lấy tất cả trừ người nghỉ trước đầu kỳ, giữ nếu có payback. **Hai câu có vấn đề thật, đã phản hồi lại:** (A) **phản đối đề xuất gộp `wd_calculated_time_block_tags` vào `wd_calculated_time_blocks`** — đo trên DB thật: **36/2.990 block có ĐÚNG 2 tag**, và tổ hợp có nghĩa nghiệp vụ (`Cash_1x + Off_in_Lieu_1x` = tăng ca trả một phần tiền một phần nghỉ bù; `Cash_1x + Off_in_Lieu_2x`), gộp 1 cột đơn là **mất im lặng đúng loại dữ liệu ảnh hưởng tiền**; chính migration `0004` đã ghi lý do này từ trước ("34 of 1,882 ... verified"), nay là 36/2.990 — đề nghị giữ bảng con, hoặc nếu gộp thì dùng cột mảng `text[]` chứ không cột đơn. (B) **Q4 — ba nguồn nói ba cửa sổ kỳ khác nhau**: template hardcode ô `A6 = DATE(YEAR(J6),MONTH(J6)-1,21)` → **21→20**; DB thật `period_start/end = 2026-06-21→2026-07-20` → **21→20**; đội DB trả lời nên dùng **18→17**. Kiểm dữ liệu: **CÓ block cho ngày 18/19/20 tháng 7** (29/60/34) nên câu "chỉ lấy được đến 17" là ràng buộc **VẬN HÀNH** (lúc chạy lương 18-20 chưa chốt), không phải ràng buộc dữ liệu → nếu chốt 18→17 thì **phải sửa công thức `A6` của template** (đụng 31 cột ngày + tiêu đề + `internal/payperiod`), không tự quyết, chờ người dùng; trong lúc chờ `DBSource` đọc đúng `period_start/end` có trong DB nên không phụ thuộc quyết định đó. **Thiết kế Task 7 đổi hướng:** KHÔNG truy vấn `wd_calculated_time_blocks`/`_tags` trực tiếp mà đọc **9 view đội DB đã dựng** — vừa đơn giản hơn vừa tránh đúng tranh chấp (A). **Task 7 chủ động KHÔNG điền `Workdays1..31`** (tách thành Task 7b): `ts_attendance_daily_pivot` cho đúng hình dạng nhưng quy tắc ra chuỗi mã vẫn chờ nghiệp vụ — `calculated_quantity` tính bằng NGÀY (0.5/1.0) còn hậu tố mã trông như GIỜ (`-8`/`-10`/`-16`), không có tag nào ứng với `ON` (ốm <30 ngày), chưa rõ `Uncompensated_Day`/`OFF`/`DT`; đoán ở đây là tạo ra bảng chấm công trông đúng mà sai, tệ hơn để trống. Tài liệu: PLAN mục 15 (mới).
- **310726** — Restore dump DB thật `document/payroll_adapter_dump_20260731_170100.sql` (953 MB, 192.428 dòng, plain SQL, dump từ PG **16.14**) vào `payroll_adapter`. **Đích thật là `localhost:5432`** đọc từ `.env` của adapter, KHÔNG phải cổng 5434 của docker-compose (Docker đang tắt); server local là PG **14.18**. **Trình bày 4 vấn đề TRƯỚC khi chạy rồi mới xin quyết định:** (1) DB đích không rỗng — 33 bảng/216 MB dữ liệu thật, mà dump có `CREATE TABLE` **không có `DROP`** nào; (2) role `adapter` không tồn tại trên server local, dump có **72 lệnh `OWNER TO adapter`**; (3) lệch phiên bản 16.14→14.18 — đã quét trước và xác nhận **không có syntax riêng của 15/16** (không `COMPRESSION`/`NULLS NOT DISTINCT`/`CREATE EXTENSION`), chỉ 2 dòng `\restrict`/`\unrestrict` là meta-command psql mới nên lọc qua `sed` khi stream (không tạo file tạm 953 MB); (4) Docker tắt nên không dùng được `postgres:16` của compose. User chọn **drop + tạo lại, KHÔNG backup** (phương án có backup đã được nêu ngay bên cạnh) và **bỏ qua role** để owner là `postgres` — 216 MB cũ đã mất, ghi lại đây vì là lựa chọn có ý thức. Restore **15 giây**, exit 0, 72 dòng lỗi và **phân loại lại thì 72/72 đều là `role "adapter" does not exist`**, không lẫn lỗi thật. Kết quả: **46 bảng + 9 VIEW**, 128 index, 87 constraint, 429 MB. **PHÁT HIỆN LỚN NHẤT: 9 view + 3 bảng KHÔNG nằm trong 29 migration nào của repo** — đội DB đã dựng thêm, và chúng trả lời gần hết tài liệu bàn giao vừa viết: **`ts_attendance_daily_pivot`** (một dòng mỗi (NV, ngày), **một cột numeric cho MỖI tag**) là đúng hình dạng cần cho lưới `Workdays1..31`; `ts_time_off_plan_balances_pivot` (`annual_leave_balance`/`sick_leave_balance`/`off_in_lieu_balance`) cho `AddIn`/`Used`/`Budget2`; `ts_ot_reconciliation` (`requested/submitted/denied/actual/variance_hours`, tính bằng GIỜ) cho `OT_CN`/`OT_Ho`; `ts_worker_job_change_log/_diff` + `ts_worker_organization_change_diff` cho `OrgStructure1..3`; `ts_worker_compensation_change_*` cho `AmountTrip*`/`AmountTrans*`; cộng 2 bảng mới `raas_ot_actual_blocks`/`_tags`. Tự chạy 3 truy vấn V1 đã soạn cho đội DB: **19 tag thật** (`Actual_Working_Day` 675 block/117 NV, `OFF` 638/219, `Paid_Holiday` 299/35…), và **`calculated_quantity` tính bằng NGÀY** (gần như chỉ `0.5`/`1.0`) trong khi hậu tố mã hiển thị (`L-8`, `TASK-REF`, `x8-4`) trông như GIỜ → V1 thu hẹp mạnh nhưng **chưa đóng**, quy tắc sinh hậu tố vẫn là quyết định nghiệp vụ. **Hai ràng buộc dữ liệu thật ảnh hưởng ngay Task 7:** (a) `raas_monthly_attendance` chỉ có **ĐÚNG MỘT kỳ `2026-06-21→2026-07-20`** (3.768 NV) nên Task 7 phải chuyển từ kỳ 03/2026 mà test đang dùng sang 07/2026; (b) `ts_attendance_daily_pivot` chỉ phủ **227/3.768 NV (~6%)** nên 31 cột chấm công sẽ trống cho ~94% người. Tài liệu: PLAN mục 14 (mới), và **viết lại tài liệu bàn giao** `Task7-DB-Source-BanGiao-DoiDB-310726.md` thêm mục 0 "bản rút gọn — chỉ còn 5 việc thật sự cần bạn" (A quy tắc sinh mã, B độ phủ 6%, C chỉ 1 kỳ, D xác nhận 19 ánh xạ, E 9 view chưa có migration nên máy khác restore từ migration sẽ thiếu) — không gửi bản cũ vì phần lớn câu hỏi đã tự trả lời được.
- **310726** — Phân tích ảnh hưởng nếu đổi cấu trúc bảng DB lên code sinh báo cáo bảng chấm công (thuần phân tích, không sửa code). Kiểm bằng bằng chứng: **cả package `internal/report/bangcong/` không có một dòng nào biết DB tồn tại** — import chỉ có `excelize`, stdlib, `internal/payperiod`; không `pgx`/`database/sql`/`internal/db`/`internal/store`, không chuỗi SQL hay tên bảng. 4 tầng, chỉ tầng cuối chịu đòn: Excel/template (5 file) + `mapping.go` (đọc dòng 7 template, khoá theo mã field) + `source.go` (hợp đồng `Row map[string]any` theo mã field, không theo cột DB) đều **miễn nhiễm**; chỉ `source_db.go` (Task 7, chưa viết) phải sửa — đúng lý do đã tách Task 5/fixture khỏi Task 7/DB. Chia 5 loại thay đổi: (a) đổi tên cột/bảng/kiểu và (b) **đổi key trong `raw` JSONB** đều nhẹ, 1 file — và (b) là loại PHỔ BIẾN NHẤT vì adapter cố ý lưu `raw JSONB` tên field gốc Workday không rename (`0001`: *"nothing is renamed or flattened"*), `raas_monthly_attendance` chỉ flatten 3 cột số còn 35 field nằm trong `raw`, nên "đổi cấu trúc" ở đây phần lớn KHÔNG phải DDL; (c) thêm mã field mới thì phải sửa **template Excel** chứ không phải code; (d) **MẤT nguồn của mã đang dùng = nguy hiểm nhất** — không đặt key → ô trống → cột dẫn xuất âm thầm ra 0 (nối trực tiếp F2), sai số liệu mà không lỗi, không test đỏ; (e) `raas_monthly_attendance` dùng delete+reinsert toàn kỳ nên đọc trúng lúc sync thấy dữ liệu nửa vời. **Hàng rào đang thiếu:** chỉ có test khoá phía template (116 mã, 0 clash), KHÔNG có test nào khẳng định "mã field `DBSource` hứa cung cấp thì thật sự có nguồn" → đã đặt thành yêu cầu bắt buộc của Task 7 để biến loại (d) từ sai-im-lặng thành sai-ồn-ào. Kết luận: nhẹ về code, nặng về đúng/sai số liệu nếu chưa đóng F2. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 12 (mới).
- **310726** — Kiểm chứng độc lập phiên 3 (sửa D1/D2 + Task 5 + Task 6) — `Core System-adapter@develop`, **43 test pass**, build/vet/gofmt sạch, chưa commit. **D1 ĐẠT và tôi kiểm mạnh hơn test của agent:** test của họ chỉ khẳng định *số lượng* công thức + `O9` "còn chứa DAY" — chưa loại trừ rủi ro thật là khi excelize giãn shared formula, nếu nó chép nguyên văn thân ô CHỦ sang ô THÀNH VIÊN thì `O9` thành `DAY(M6)` (vẫn chứa DAY, vẫn đủ số lượng, nhưng lệch 2 ngày). Tôi **so nội dung từng ô 4 dòng lưới ngày: 314 ô, 0 lệch** — `M9=DAY(M6)`, `N9=DAY(N6)`, `O9=DAY(O6)` offset đúng từng ô. Việc `shared 416→101` là hệ quả chủ đích, lập luận của agent đúng (bất biến cần giữ là NỘI DUNG công thức, không phải trạng thái shared). Task 5/6 đạt: `Render`+`FixtureSource`, STT ra đúng 1/2/3 (lần đầu test được vì fixture có điền cột `G`), `Diagnostics` = `{EK15,EO15}` cho dòng thiếu `StdWorkDay` và Render vẫn thành công, 116 mã field/0 trùng/0 clash với công thức. **Hai phát hiện MỚI của tôi:** (F1) **kỳ tháng 2 rò 2 cột hiển thị ngày NGOÀI kỳ** — kỳ 21→20 dài bằng số ngày tháng đầu nên tháng 31 ngày vừa khít 31 cột, tháng 30 tràn 1 cột và nhánh `IF(DAY(AQ6)=21,…)` xử đúng, **tháng 2 tràn 3 cột (AO ẩn, AP+AQ ĐANG HIỆN = 22/3 và 23/3) và guard không bắt vì `DAY=23≠21`**; hạn chế của template gốc, không do code Go. Kèm theo: **không có `period_test.go`, mọi test dùng đúng 1 kỳ** → tiêu chí "verify 3 kỳ 28/30/31 ngày" của Task 4 CHƯA TỪNG CHẠY, và **chính PLAN của tôi ghi sai giả định** về nhánh guard nên tiêu chí đó vốn đã sai (đã đính chính trong PLAN). (F2, quan trọng cho cam kết sản phẩm) **ràng buộc G2 KHÔNG bảo vệ được cột DẪN XUẤT**: G2 nói "để trống, cấm điền 0 vì điền 0 làm tổng ra số trông hợp lệ mà sai" — nhưng **để trống cũng ra đúng con số đó** vì Excel coerce ô trống thành 0; bằng chứng `BJ15 = BI−AR = 22−(trống) = 22` → "Chênh lệch ngày công = +22" vô nghĩa mà trông hợp lệ, và toàn bộ nhóm C làm `BW/CF/DR/EG/EK = 0` cho MỌI dòng, tức báo cáo nói "Thành tiền tất cả PC = 0" như một câu trả lời thật thay vì "chưa có dữ liệu". G2 bảo vệ ô ĐẦU VÀO, không bảo vệ ô DẪN XUẤT — cần **quyết định của người dùng** (chấp nhận cho UAT kèm legend / làm trống luôn cột tổng / hiện dấu hiệu trực quan), không tự quyết. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 11.8-11.9 (mới) + đính chính tiêu chí verify Task 4, `document-map.md`.
- **310726** — Thi hành Task 0→4.5 của PLAN bảng chấm công (`Core System-adapter@develop`, 31 test pass, chưa commit) + **kiểm chứng độc lập bởi phiên chủ, không tin báo cáo agent**. Đạt: `ShiftRows` (13 case, 3 bẫy nháy kép `x8-4`/`TS/2-2`/`TASK-REF`+`TASK-REF` — mã chấm công trong nháy kép là DỮ LIỆU, regex quét mù sẽ phá), `ExpandRows` (mầm 12 giữ nguyên, dữ liệu 13…12+n, footer `JB2:JB<12+n>` giãn đúng), `SetPeriod` (neo 1 ô, kỳ 21→20, tiêu đề tự ra "THÁNG 3/2026"), và **chuỗi `input → formula → số` chạy thật — xác nhận bởi HAI engine độc lập** (excelize + LibreOffice headless; `soffice` có ở `/opt/homebrew/bin` nên phép kiểm chéo đã chạy thật chứ không bị skip) khớp số tính tay kể cả ca âm `BJ=−1`. **Hai lỗi tự tìm ra, test của agent không bắt được, CHƯA SỬA:** (D1 nặng) `RecalcAll` **phá 295 ô công thức** ở dòng 6–9 — truy tới gốc: **416/464 công thức trong template là SHARED formula** (`<f t="shared" si="N"/>` thân rỗng ở ô thành viên), `SetCellFloat` lên ô CHỦ xoá cả nhóm, tới ô thành viên thì `GetCellFormula` trả `""` nên nhánh `if formula==""{continue}` bỏ qua → mất công thức vĩnh viễn, **lưới ngày đóng băng**, phá đúng 2 cam kết "neo 1 ô cả lưới tự đúng" + "giữ công thức sống"; sửa bằng đọc HAI LƯỢT (đọc hết công thức vào memory trước khi mutate ô nào). (D2 TB) `SetCellFormula` ép `c.T="str"` nên **mọi giá trị cache là text** — `AR13` input ra `22` số, `AS13` cache ra `'22'` chuỗi. **Đính chính một kết luận sai của chính tôi giữa chừng:** ban đầu tôi tưởng LibreOffice phá công thức — thực ra `acceptance_test.go` cho `soffice --convert-to` ghi **cùng đường dẫn** với input nên file excelize bị ghi đè, tôi đang soi bản LibreOffice; tự sinh lại file bằng Go mới thấy lỗi nằm ở `RecalcAll` (D1). Cũng phát hiện tiêu chí verify "STT ra 1/2/3" của Task 3 **chưa test nào phủ** (acceptance test không điền cột `G` nên `SUBTOTAL(3,…)`=COUNTA ra 0; tự điền `G`/`F` mới xác nhận `$G12:G$13` là đúng). Hạn chế có chủ đích: công thức trả text/`""` không cache được do **rò chỉ số shared-string** (agent tái hiện: cache `"22"` hiện ra `684` = index bảng chuỗi) → **cột lưới ngày `M–AQ` sẽ không bao giờ có cache**, Excel mở thì đúng, code đọc thì rỗng. Task 5–10 chưa làm; Task 7 (DB) chặn bởi Docker daemon không chạy + bảng quy đổi tag→mã chấm công chưa ai định nghĩa. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` mục 9 (mới, có bảng trạng thái 10 task), `document-map.md`.
- **310726** — Lên PLAN build template Excel "Bảng chấm công CBNV" bằng Go + excelize trong `Core System-adapter` (chưa code dòng nào). Trước đó cùng phiên trả lời câu hỏi kỹ thuật "dùng Python tạo Excel trong project Go được không": được, nhưng adapter đang `CGO_ENABLED=0` + runtime `gcr.io/distroless/static-debian12` (**không shell/libc/python**) nên thêm Python = đổi hẳn base image (~2MB → ~150MB+, mất distroless), trong khi `Core System-backend` đã có `excelize v2.10.0` chạy production ở ~20 file → khuyến nghị excelize, và user chốt đúng hướng đó. **Mổ file thật bằng openpyxl trước khi viết plan** (không đoán): phát hiện "sheet thứ 2" nhập nhằng vì file có **4/7 sheet ẩn** — sheet thứ 2 theo thứ tự file là sheet ĐẶC TẢ mapping, còn tab nhìn thấy thứ 2 mới là template thật; đã hỏi và user chọn `202603_Bang_Cong,_Phu_Cap_Repor`. **Đính chính con số của chính tôi:** lúc hỏi tôi ghi "~60 công thức" — đo thật là **1.081 ô công thức / 51 dạng / 123 công thức mỗi dòng nhân viên**, càng khẳng định quyết định "giữ công thức sống" là đúng. 3 phát hiện định hình kiến trúc: (a) lưới ngày là **4 dải 31 cột** nối nhau bằng công thức (`EU–FY` đầu vào → `FZ–HD` → `HE–II` → `M–AQ` hiển thị) nên chỉ phải đổ dữ liệu vào 31 cột, 93 cột tự chạy; (b) **dòng 12 vốn đã là dòng mầm** (đủ 123 công thức, không dữ liệu, không `#REF!`) → nhúng template làm sạch bằng `go:embed` + nhân bản dòng mầm thay vì dựng lại style bằng code (**15/22 tổ hợp fill dùng màu theme+tint** — excelize không có API màu theme, sai màu thì không test nào bắt được); (c) **không tin `DuplicateRow`** mà tự viết `ShiftRows` có test, vì rủi ro số một là regex dịch số dòng quét mù làm hỏng chuỗi literal `"L-10"`/`"x8-4"`/`"TASK-REF"` (mã chấm công nằm trong nháy kép). Lỗi có sẵn trong file gốc: 10 ô `#REF!` (gói gọn ở 5 dòng mẫu sẽ xoá), 4 mã field hỏng do find/replace (`Workdays{r}` = `Workdays9`, quên xử lý là trượt 4 cột), và **PII thật** (5 họ tên + mã NV) nên template nhúng repo phải làm sạch. Bản đồ dữ liệu chia 3 nhóm theo chính dòng 4 của file (`WD` 103 cột / `Core System` 6 / `??? Not available` 2): nhóm C chưa có nguồn → **để trống, cấm điền 0**. Còn mở quan trọng nhất: **bảng quy đổi tag Workday → mã ô chấm công chưa ai định nghĩa**. Sau đó theo yêu cầu: chốt **mốc M1 = Task 0+1** (kèm giải thích vì sao KHÔNG tách được mốc "template chưa có công thức" — nhúng file gốc thì 1.081 công thức đi kèm sẵn, xoá đi rồi Task 2/3 nhét lại là việc vứt đi) và soạn brief dispatch tự chứa cho Sonnet. **Tự phát hiện một lỗi trong PLAN của chính mình khi soạn brief:** bản đầu định làm sạch template bằng `sanitize.py` (openpyxl) — việc đó **tự phá phép kiểm của chính nó**, vì Task 0 tồn tại để chứng minh *excelize* không làm hỏng file, mà template thật lại do *openpyxl* ghi ra thì bản đã kiểm và bản thật là 2 file của 2 thư viện khác nhau, chứng minh xong không nói được gì. Đã sửa PLAN + thêm ràng buộc **G9: một công cụ GHI (excelize/Go), một công cụ KIỂM (openpyxl chỉ đọc), không đảo vai** — cũng khớp sự thật Python không tồn tại trong runtime image distroless; ghi lý do vào mục 6.1 để người sau không tưởng "Python sai từ đầu" (sai ở VAI, không sai ở công cụ). Thêm rủi ro mới vào bảng: xoá dòng 13–17 có thể làm lệch tham chiếu footer `SUBTOTAL` dòng 18 — excelize tự dịch tham chiếu khi xoá dòng, phải test xác nhận chứ không tin. Tài liệu: `self-docs/BangCong-Xlsx-Template-Golang-310726.md` (canonical, 10 task + mốc M1 + 9 ràng buộc bao trùm), `document-map.md`.
- **290726** — Tóm tắt cách hoạt động của engine tính lương theo rule + bảng lương (thuần đọc/giải thích, KHÔNG sửa code). Xác minh bằng đọc trực tiếp `engine.go`/`payroll_service.go`/`payroll_scoped_resolver.go`/`payroll_template_mask.go`/`payroll_record_repo.go` + truy vấn DB dev thật (41 cột `formula`, 70 `input`, 0 `config`/`manual`/`system`) — không dựa vào tài liệu cũ. Điểm chốt để nhớ: rule là DỮ LIỆU (`salary_components.formula`), engine chỉ thông dịch; thứ tự tính là topological sort theo `[MÃ_CỘT]` chứ KHÔNG theo `seq`; engine fail-soft (công thức lỗi → log + 0, mã cột lạ → 0 im lặng) nên rule sai và rule đúng-bằng-0 nhìn giống nhau; `BASIC_SAL` không đọc từ `employees` mà từ bảng lương HR upload; lưới bảng lương lấy DÒNG từ `attendance_summary` đã chốt (LEFT JOIN `payroll_records`), giá trị từ `computed_values` JSONB nên thêm cột không cần migration; `SetCell` không ghi thẳng kết quả mà ghi override `cell_pin` priority 1000 rồi `CalculateOne`; `PIT` hiện là `MAX` của 5 đường thẳng và KHÔNG đọc bảng `pit_brackets`. Tài liệu: `self-docs/Core System-Engine-Rule-Flow-290726.md` (canonical, có bảng chuỗi rule 4 tầng trích từ DB + bảng 11 ngộ nhận ↔ thực tế), `document-map.md`.
- **290726** — Push `feature_1` cả 2 repo lên GitLab (`Core System-backend` `226d8f3..9fe9147` 5 commit, `Core System-frontend` `095494f..f8b9bb0` 6 commit) — toàn bộ việc đồng bộ cột lương + impact của 280726. **Cố ý KHÔNG commit 5 file đang modified vì không thuộc đợt việc này**, trong đó `Core System-frontend/lib/msal.tsx` là code sản phẩm chạm auth (stub bỏ qua đăng nhập Azure khi thiếu `NEXT_PUBLIC_AZURE_CLIENT_ID` — đưa lên nhánh chung thì mọi môi trường quên set biến đó sẽ tự tắt đăng nhập). Theo yêu cầu "đưa vào .gitignore": phát hiện khối `.gitignore` lạ của 270726 **đã có sẵn** trong working tree (`llmwiki/`, `harness/`, `.pre-commit-config.yaml`, `.gitignore`, `.claude/`) nhưng **hoàn toàn vô tác dụng** vì cả 4 file đều đang TRACKED — `.gitignore` chỉ chặn file chưa theo dõi. Trình bày 2 hướng thật (`git rm --cached` xoá khỏi repo chung, đồng nghiệp pull về mất file; hay `skip-worktree` chỉ ẩn cục bộ), user chọn **`skip-worktree`** cho cả 4 + thêm `.claude/settings.json` của backend. Đã áp dụng 5 file (FE: `lib/msal.tsx`, `.gitignore`, `.pre-commit-config.yaml`, `.claude/settings.json`; BE: `.claude/settings.json`) và **kiểm chứng bằng `git add -A` thật** — stage rỗng, nội dung sửa local còn nguyên. **Hệ quả cần biết cho phiên sau: `git status` sẽ KHÔNG còn thấy thay đổi ở 5 file này (bit `S` trong `git ls-files -v`), và `git pull` sẽ TỪ CHỐI nếu origin sửa một trong chúng** — lúc đó phải `git update-index --no-skip-worktree <file>`, pull, rồi bật lại. Không commit, không đụng tracking, không xoá gì khỏi repo.
- **290726** — Sửa lỗi modal "Thêm cột lương" **chặn 4/5 loại cột** (người dùng báo bằng ảnh chụp: điền đủ mã + tên, chọn loại `input`, nút Lưu xám câm không nói lý do). Nguyên nhân: `canSubmit` (`SalaryComponentModal.tsx`) đòi `formula.trim() !== "" && validation.checked && validation.valid` **vô điều kiện cho mọi `componentType`**, trong khi `input`/`config`/`manual`/`system` lấy giá trị từ lưới/bảng cấu hình chứ không có công thức; tệ hơn `runValidate()` `return` sớm khi công thức trống nên `validation.checked` **vĩnh viễn false** → ngõ cụt kín, kể cả bấm "Kiểm tra công thức" cũng không gỡ được. Backend `Create` không validate formula dòng nào → rào thuần FE tự dựng tự chặn. **Xác nhận không phải hồi quy của đợt 280726** bằng `git log -S`: dòng đó có từ `64f90a5` (24/07, đợt S004 gốc), commit `e81bb22` hôm 28/07 chỉ thêm import/prop/render panel, không chạm `canSubmit`; 5 ngày không ai gặp vì modal sinh ra cho tab Công thức nên trước giờ chỉ tạo cột loại `formula`. **Đính chính một nhận định sai của chính tôi giữa chừng:** đã nói công thức thừa trên cột `input` là "code chết" — SAI, `engine.go` `case "input","config","manual"` **có** `evalExpression(c.Formula, vals)` làm giá trị mặc định, mà `buildOrder` lại **chỉ** trích phụ thuộc cho loại `formula` (`engine.go:86`) nên công thức đó chạy **không đảm bảo thứ tự topo**; vì vậy chỉ ẩn ô mà vẫn gửi state ẩn sẽ tạo đúng loại lỗi tệ nhất (người dùng không thấy nhưng hệ thống vẫn lưu và vẫn tính) → thêm chốt `formula: needsFormula ? formula : ""` cho cả create lẫn update. Kiểm dữ liệu thật trước khi sửa: 41 cột `formula` (đều có công thức), **69 cột `input` — 0 cột nào có công thức**, 0 cột `config`/`manual`/`system` → không phá gì đang chạy. Engine **không có** `case "system"` lẫn `default:` (gỡ 27/07 khi chuyển PIT sang formula) nên cột `system` không bao giờ có giá trị và sẽ luôn mang dấu `⚠ chưa tính` của nhãn Task 4 — người dùng quyết định GIỮ loại này, đã thêm cảnh báo nền vàng nói thẳng thay vì tự ý bỏ khỏi danh sách. 3 thay đổi: (1) `needsFormula = componentType === "formula"` gắn yêu cầu theo đúng loại, nhánh `formula` giữ NGUYÊN cả 3 ràng buộc cũ kể cả TASK-REF; (2) ẩn cùng lúc ô công thức + danh sách "Dùng trong công thức" + nút "Kiểm tra công thức", thay bằng ghi chú nguồn giá trị theo từng loại (nội dung khớp đúng `switch` thật của engine), và footer liệt kê điều kiện còn thiếu ngay cạnh nút Lưu; (3) chốt không gửi công thức còn sót. Verify: `tsc` sạch, `eslint` sạch, `npm run build` sản xuất thật xanh, `vitest` 20/20. Commit `Core System-frontend@b4216c4` + `@fd01c9e`, **chưa push**. **Còn nợ: chưa kiểm tay trình duyệt** (và phải restart FE vì máy đang chạy production từ build cũ); không có test tự động khoá bất biến vì `vitest.config.ts` repo cố ý chỉ có `environment: "node"`, không mount được component. Tài liệu: `self-docs/Salary-Component-Modal-Type-Gating-290726.md` (canonical), `document-map.md`.
- **280726** — Kiểm chứng độc lập kết quả thi hành PLAN `280726-salary-column-sync-and-edit-impact` ở **tầng HTTP thật** (không tin báo cáo phiên trước, tự chạy lại test + đọc lại code + gọi API thật). Đóng được món nợ Task 8 Step 3 mà không cần trình duyệt: phát hiện `internal/middleware/auth.go:205` có sẵn bypass `Authorization: Bearer dev` khi `DEV_USER_EMAIL` set (`.env` local đã set, `database.go:51` tự cấp super-admin), chạy server bằng `AUTO_MIGRATE=false go run ./cmd/Core System` để không ghi schema/seed. **Xác nhận claim quan trọng nhất của phiên trước là đúng** (fixture Task 6 nay neo vào `[BASIC_SAL]`, khẳng định delta `+10tr` — bỏ `salaries` là test fail ngay). **Tìm ra 2 lỗi mà TOÀN BỘ gate đều xanh không bắt được** (`go test`, `go vet`, `tsc --noEmit`, `eslint`, `next build`): (1) **nặng** — `GET .../{id}/usage` trả `null` (Go nil slice) thay vì `[]` cho `overrideRefs`/`templates`, mà FE gọi thẳng `.length`/`.map` ở 6 chỗ (`DeleteColumnDialog.tsx:86,88,98,100,101`, `FormulaImpactPanel.tsx:71,74`) → `TypeError` trong thân render, trắng màn hình, và đây là **trường hợp phổ biến nhất** (hầu hết cột không có override, DB chưa có template nào) nên dialog xoá + panel tác động hầu như luôn vỡ; `tsc` không bắt được vì type khai `OverrideRef[]` không-nullable — type nói dối về API thật; (2) `ComputeFormulaImpact` (do PLAN viết) quét mù mọi key bằng `asFloat()` trong khi bản đồ giá trị chứa cả field chuỗi/uuid → ~4 dòng log/NV/lần mô phỏng, kỳ 12k người là ~100k dòng cho MỘT lần bấm nút, kèm nội dung hardcode `[PIT] TAXABLE_INC` (di sản từ thời hàm còn riêng của PIT) vu oan cho PIT — phát hiện nhờ đọc output test thấy `TAXABLE_INC non-numeric string: "ZZIMP1"` với `ZZIMP1` là mã nhân viên. Sửa: lỗi 1 hai lớp (`GetUsage` coerce `nil → []` + `getSalaryComponentUsage` chuẩn hoá `?? []` ở API client làm chốt cho mọi consumer sau), lỗi 2 thêm `numericValue()` trả cờ thay vì im lặng quy 0. 3 test mới khoá bất biến **ở tầng thật chứ không tầng struct**: `json.Marshal` rồi tìm chuỗi `null`, capture `log.Writer()` đếm dòng log, unit test hàm thuần. Bằng chứng TASK-REF ở tầng HTTP: md5 toàn bộ `computed_values` + `count` + `max(updated_at)` y nguyên trước/sau khi gọi `POST /Core System/formula-impact` thật (299 dòng lương, kỳ 05/2026); số học kiểm tay đúng (`byComponent` 1 dòng `CONTRACT_TOTAL` delta `3.273.000` = 3273 × 1000); nhánh 422 đúng; `reorder` kiểm nhánh xác thực (400 thiếu danh sách/mã lạ, 401 không token) mà **không** ghi `seq` thật. Quan sát UX chưa sửa: `totalNetDelta` có thể = 0 trong khi cột khác lệch thật (cột không nằm trên đường tới `NET_PAY`) — câu "Tổng thực lãnh lệch 0 đ" dễ đọc thành "không ảnh hưởng". Verify sau sửa: `go test ./...` đúng 4 fail baseline, `vitest` 20/20, `next build` xanh 41/41 trang. Commit `Core System-backend@9fe9147` + `Core System-frontend@f8b9bb0`, **chưa push**. **Còn nợ: kiểm tay trình duyệt 4 điểm FE** — lỗi 1 chính là bằng chứng bước này không thay thế được bằng test và build. Tài liệu: `self-docs/Salary-Column-Sync-Impact-280726.md` mục "Vòng kiểm chứng độc lập ở tầng HTTP thật".
- **280726** — Thi hành trọn PLAN `280726-salary-column-sync-and-edit-impact-PLAN.md` (9/9 task, thứ tự lát cắt A→C→B→D rồi 8+9), không migration DB. Backend: `GET /config/salary-components/{id}/usage` (closure phụ thuộc NGƯỢC nhiều tầng qua `extractDeps` + 5 truy vấn đếm override CASCADE/template/mồ côi), `PUT /config/salary-components/reorder` (ghi `seq` trong transaction, KHÔNG lọc `is_system` vì `seq` chỉ là thứ tự hiển thị — thứ tự tính là topological sort độc lập ở `buildOrder`), `POST /Core System/formula-impact/{periodId}` (mô phỏng 2 lần tính khác đúng 1 biến là công thức, 422 khi thiếu `salaries`). Frontend: dialog xoá hiện đủ 3 nhóm hệ quả + tick xác nhận riêng cho nhóm mất dữ liệu thật (giữ NGUYÊN nhánh 409 `usedBy` cũ), nhãn `⚠` + tooltip 2 kịch bản ở header cột chưa tính (đặt ở header, KHÔNG đổi nội dung ô để không chạm xuất Excel/copy vùng), kéo-thả lưu thật xuống `seq` (debounce 800ms, người không quyền vẫn kéo xem tạm không bị lỗi quyền dội mặt), `FormulaImpactPanel` dùng chung 2 luồng sửa công thức (advisory, không chặn nút Lưu). **Ba ràng buộc tuyệt đối đều kiểm bằng lệnh chứ không tuyên bố suông:** grep `calculatePayroll` trong `components-page/tinh-luong/` = 0; trong `ComputeFormulaImpact` số lần `salaryOverrides: map[string]float64{}` = 0 và cả 2 lần mô phỏng dựng từ CÙNG closure mang `in.Salaries`; diff `payroll_service.go` = +208/−0 nên `ComputeTemplateImpact` không bị đụng. **Phát hiện 4 chỗ PLAN ghi sai so với code thật** (ngoài 3 chỗ PLAN đã tự dặn grep — cả 3 đều đúng là sai nếu đoán: `toFloat`→thật ra `asFloat`, `uuidParse`→không tồn tại phải dùng `uuid.Parse`, `PayrollPeriod.status`→có thật nên dùng được nhánh kỳ đã chốt): fixture `payroll_periods` thiếu 3 cột NOT NULL; `dbtest` không có `QueryRow`; **fixture Task 6 của PLAN làm test xanh giả** (cột `input` không nguồn giá trị → nhân 2 vẫn 0, delta 0, không chứng minh gì) nên đã neo lại vào `[BASIC_SAL]` và khẳng định delta đúng `+10tr` — chính test này mới khoá được ràng buộc "truyền salaries vào cả 2 lần"; `loadSalarySession()` gọi trong thân render gây lệch hydration, đã chuyển vào `useEffect`. Tránh lại bẫy `gofmt` của 270726 (`salary_component_service.go` bị gofmt đòi đổi `''` thành smart-quote ở comment dòng 32 có sẵn từ HEAD — không chạy `gofmt -w`). Verify: `go vet` sạch, `go test ./...` **đúng 4 fail baseline** sau mỗi task (baseline tự đo trước khi sửa, không chép tài liệu); `payroll_records` giống hệt trước/sau toàn bộ suite ở cả 3 chỉ số (6435 dòng, `max(updated_at)`, tổng `NET_PAY`) + 5 truy vấn dọn dẹp đều 0; FE `tsc` sạch, `eslint .` 0 error/13 warning tiền tồn tại ở file không đụng, `vitest` 20/20, `next build` sản xuất thật xanh sau mỗi task FE. 7 commit (`Core System-backend` `93303ad`/`5fa961f`/`1bc278f` + docs, `Core System-frontend` `56ddded`/`560868a`/`34e105c`/`e81bb22` + docs), **chưa push**. **Còn nợ: chưa kiểm tay trình duyệt** 4 điểm sửa FE (PLAN có bước này). Tài liệu: `self-docs/Salary-Column-Sync-Impact-280726.md` mục "Kết quả thi hành" (canonical), 2 file `docs/routes-permissions.md`, `document-map.md`.

- **270727** — Fix bug "Gán" (invalid request body — FE gửi date trần, backend decode thẳng vào `time.Time` đòi RFC3339, đã sửa qua DTO trung gian parse "2006-01-02", commit `8a5b882`); thêm label "Từ ngày"/"Đến ngày (tuỳ chọn)" thay placeholder vô tác dụng trên `input type="date"` (commit `9d683a4`); xoá nút "Xem trước tác động" theo yêu cầu (giữ backend, xoá FE, commit `e066f7a`); thêm Task E — endpoint `GET /config/employee-Core System-templates` (lọc theo company scope, test 2 công ty thật Enterprise/UNI) + màn hình tổng hợp `AllAssignmentsModal.tsx` (commit `e2bd34f`/`4b5431e`). **Phát hiện + sửa lỗi nghiêm trọng:** PIT tự reset về `component_type='system'` 2 lần trong phiên — điều tra ra nguyên nhân thật: `RunMigrations()` (`database.go`) chạy vô điều kiện mỗi lần backend restart (`AUTO_MIGRATE=true`), 1 bước trong đó (`migrationSalaryComponentsV4`) DELETE+INSERT lại toàn bộ `salary_components` từ snapshot hardcode có PIT='system' — migration fix của tôi ở `atlas/migrations/` (chạy tay 1 lần) không nằm trong danh sách này nên bị xoá mỗi restart. Đối chiếu `migrationPaidDaysToFormula` xác nhận quy ước đúng của codebase: thêm 1 bước tự-hồi (`WHERE component_type='system'`) đặt SAU khối V4. Đã thêm `migrationPITSystemToFormula`, verify bằng mô phỏng reset+chạy 2 lần (lần 2 xác nhận `UPDATE 0`), commit `fe8805b`. Theo yêu cầu người dùng: **HOLD toàn bộ tính năng "Cấu trúc lương theo cấp bậc"** (sẽ làm ở phase sau) — xác nhận kill-switch mặc định OFF khiến engine tính lương thật không bị ảnh hưởng gì (3 test khoá bất biến đã xác nhận). Bổ sung hướng dẫn thao tác bật `PAYROLL_TEMPLATE_ENFORCE` (dev: `.env`; prod: cần hỏi ops, repo không có cấu hình deploy) vào self-docs (trước đó chỉ có khái niệm, chưa có bước cụ thể). Tài liệu: `self-docs/Salary-Structure-Template-Analysis-260726.md` mục 12 cập nhật + mục 13 mới (trạng thái HOLD). Đã commit + push cả 2 repo.

- **270726** — Hoàn tất PLAN `260726-salary-structure-usage-and-grading-PLAN.md` Task 5+6+7 (tiếp nối Task 3.5-4.6 đã xong trước đó cùng ngày) + migration `pit_brackets`, theo yêu cầu `/fable5` tuần tự, xác nhận sau mỗi phần. **Task 5.1 (kill-switch `PAYROLL_TEMPLATE_ENFORCE`, mặc định off):** thêm `templateEnforceEnabled()` + field `calcShared.templateEnforce`, wire vào `computeEmployee`. Phát hiện phụ khi viết (Move 3 — quét negative-space trước khi coi task xong): `Finalize()`'s `snapshotTemplateMasksForPeriod` chưa gate theo kill-switch — nếu để nguyên, tắt enforce vẫn ghi snapshot "cột này bị mask" dù `payroll_records` thật của kỳ đó không hề bị mask (snapshot nói dối so với số đã tính) — đã gate luôn hàm này, thêm test khoá bất biến. Phải sửa lại 8 test Task 4.6 cũ (thêm `t.Setenv(...,"on")`) vì mặc định off mới làm chúng gãy hàng loạt. **Task 5.2 (shadow-mode `GET /Core System/template-impact/{periodId}`):** sửa 2 lỗi PLAN gốc ghi sai so với code thật (route pattern `/periods/{id}/...` → đúng phải `/Core System/<action>/{periodId}`; gate `requireSalaryComponentsView` → đúng phải `requirePayrollCalculate+companyReadScope+requireView`, xác nhận bằng đọc trực tiếp `router.go`). **Phát hiện lỗi thiết kế nghiêm trọng trước khi code** (Move 4 — tự phản biện draft trước khi viết): nếu `computeEmployee` tự đọc `templateEnforceEnabled()` (env global) như Task 5.1 đã làm, bản mô phỏng "giả lập bật" của endpoint sẽ HOÀN TOÀN VÔ TÁC DỤNG khi env thật đang off (mặc định) — cả 2 lần tính (trước/sau) bị cùng 1 giá trị chi phối, không có gì để so sánh. Sửa tận gốc: `templateEnforce` thành field trên `calcShared` (đọc env 1 lần lúc `Calculate`/`CalculateOne` dựng), `ComputeTemplateImpact` ép cứng true/false độc lập với env thật cho 2 kịch bản mô phỏng. Thêm 3 test tích hợp (service-level: đối chiếu số đúng dấu âm + xác nhận 0 dòng ghi `payroll_records`; HTTP-level qua router thật: 5 role × 403/qua-gate đúng theo gate đã sửa). **Task 6 (FE, `Core System-frontend`):** `PayrollTemplateModal.tsx` (CRUD template + chọn cột) + `EmployeeTemplateAssignPanel.tsx` (gán NV + lịch sử + gợi ý theo cấp bậc) + nhúng `TinhLuongExcel.tsx` (2 nút ribbon mới trong sheet "Danh sách cột lương"). Trước khi paste code PLAN, xác nhận lại hợp đồng API thật (đọc `client.ts`/`employees.ts`/`SalaryComponentOverridesPanel.tsx`) — phát hiện PLAN giả định SAI: `request<T>()` trả thẳng `Promise<T>`, KHÔNG bọc `{data}` như PLAN draft (`.then((r) => r.data)` sẽ crash runtime) — chỉ `getEmployees` dùng `requestWithTotal` mới có `.data`; đã sửa toàn bộ điểm gọi trước khi viết. Verify bằng `next build` sản xuất thật (không chỉ `tsc`/`eslint`) — sạch, chỉ warning tiền tồn tại không liên quan. **Migration `pit_brackets`** (`20260727010000_pit_brackets_2026_law.sql`, additive): thêm 5 bậc thuế 2026 (`effective_from=2026-01-01`), giữ nguyên 7 bậc cũ — đã dry-run BEGIN/ROLLBACK trước, xác nhận với người dùng qua `AskUserQuestion`, áp dụng thật vào `payroll_engine`, `atlas.sum` re-hash. Full suite `go test ./...` sau toàn bộ: đúng 4 fail baseline cũ (2 report route + 2 pipeline route), 0 hồi quy qua mọi bước. **Task 7 (tài liệu):** cập nhật `self-docs/Salary-Structure-Template-Analysis-260726.md` (mục 9 bảng checklist đầy đủ Task 5.1/5.2/6, mục 10 đánh dấu bug PIT đã sửa), `self-docs/PIT-Formula-Fix-270726.md` (mục 5 — pit_brackets đã làm), SPEC gốc `260726-salary-structure-usage-and-grading.md` (Task 3/4/5 → `[x]`), PLAN cùng tên (Task 5 viết lại chi tiết, đánh dấu 3 chỗ PLAN gốc sai đã sửa), `document-map.md` (5 entry), `Core System-backend/docs/routes-permissions.md` (thêm kill-switch + shadow-mode route vào section "Core System Templates" đã có sẵn từ trước). Đã commit + push cả 2 repo: `Core System-backend@feature_1` 3 commit tách riêng (`3779ae8` Task 5.1+5.2, `e5683b6` migration `pit_brackets`, `60570b1` un-ignore `Core System-backend/docs/` + docs) — phát hiện phụ khi commit: `.gitignore` có sẵn 1 khối thay đổi uncommitted KHÁC không phải của mình (harness/metrics, self-docs/, llmwiki/... — không có trong HEAD, không rõ từ đâu), đã tách staging cẩn thận (dựng bản HEAD + đúng phần `docs/` của mình, stage, khôi phục lại working tree) để không commit hộ thay đổi lạ đó; `Core System-frontend@feature_1` 1 commit (`e340672`). Theo yêu cầu tiếp theo: **rebase `feature_1` lên `origin/develop`** (Core System-frontend) vì MR đang conflict — 1 file xung đột (`TinhLuongExcel.tsx`, cả 2 nhánh cùng sửa) ở 3 điểm, 2 điểm đầu chỉ gộp state/mount độc lập, điểm thứ 3 phát hiện `develop` đã dời nút "+ Cột mới" sang prop `onAddColumn` của `FormulasRibbon` — không tái tạo lại nút trùng. Verify sau rebase (`tsc`/`next build` sản xuất thật, 0 lỗi) rồi force-push (`--force-with-lease`, xác nhận qua `AskUserQuestion` trước khi force) → `ea7b59a`. Tài liệu: `self-docs/Salary-Structure-Template-Analysis-260726.md` mục 11 mới (trạng thái git chi tiết cả 2 repo, thay các chỗ ghi "chưa commit" đã lỗi thời).
- **270726** — Theo yêu cầu người dùng (`/orca-workflow`, xem 2 ảnh chụp UI thật của `PayrollTemplateModal`/`EmployeeTemplateAssignPanel` vừa build ở dòng trên): trả lời 3 câu hỏi review UX — "xem record đã gán ở đâu", "banner có phù hợp không", "dùng ở UAT hay cả go-live". Verify bằng đọc/grep code trực tiếp (không suy đoán), phát hiện 3 gap thật: (1) `ListAssignmentsByEmployee` chỉ tra 1 NV/lần, không có endpoint nào liệt kê "ai đang gán template X"; (2) banner "Chưa ảnh hưởng bảng lương thật..." rò rỉ tham chiếu nội bộ `"Task 5, PLAN 260726"` + ngụ ý sai rằng C&B tự "bật enforce" được trong UI (thực tế là biến môi trường, chỉ ops đổi được) + là text TĨNH sẽ nói sai ngay khi enforce bật thật; (3) **phát hiện quan trọng nhất:** endpoint shadow-mode `GET /Core System/template-impact/{periodId}` (Task 5.2, đã xanh ở backend) **0 màn hình FE nào gọi tới** — mục đích duy nhất của Task 5.2 (C&B xem trước tác động trước khi bật enforce) hiện không dùng được. Kết luận: tính năng hiện tại chỉ phù hợp UAT, chưa sẵn sàng go-live. Theo yêu cầu tiếp theo: **lên PLAN thi hành chi tiết (không code)** để làm ở phiên chat mới — `llmwiki/wiki/sources/draft/270726-Core System-template-audit-and-preview-ui-PLAN.md` (4 task, Files/Interfaces/Steps cụ thể, không migration DB: A backend 2 endpoint mới — `GET /config/Core System-templates/{id}/assignments` + `GET /Core System/template-enforce-status`; B FE hiện danh sách NV gán trong `PayrollTemplateModal`; C FE màn hình mới "Xem trước tác động" nối endpoint Task 5.2 lần đầu; D FE sửa 2 banner dùng trạng thái enforce thật). Cập nhật `self-docs/Salary-Structure-Template-Analysis-260726.md` mục 12 (mới) + `document-map.md` (2 entry). Chưa code dòng nào.
- **270726** — Sửa bug PIT (thuế TNCN) luôn = 0 toàn hệ thống — phát hiện độc lập khi viết test Task 4.6 (không liên quan tính năng cấu trúc lương ở dòng dưới). Nguyên nhân: `CalculateInputs.PeriodStart`/`PeriodEnd` chưa từng được gán ở bất kỳ đâu → `filterActivePITBrackets` luôn lọc rỗng bracket thật (`effective_from=2009-01-01`) → PIT luôn 0; xác nhận bằng dữ liệu production thật (0/6435 `payroll_records` có PIT>0) và xác nhận không phải hồi quy (lỗi nằm nguyên văn từ commit đầu tiên `47753b4`). Đưa cho 1 phiên Opus riêng phân tích trước (thuần đọc+suy luận). Theo yêu cầu người dùng: **đổi kiến trúc PIT** từ `component_type='system'` (logic Go hardcode) sang `formula` (cấu hình qua UI như 40 cột khác), dùng bậc thuế THEO DỮ LIỆU HR CUNG CẤP (0-10/10-30/30-60/60-100/>100tr @ 5/10/20/30/35%) — đã giải mã bằng toán học công thức HR đưa và **xác nhận tường minh với người dùng** rằng bậc này KHÁC bảng `pit_brackets` 7 bậc cũ trong DB (lệch thật ~850k tại thu nhập 20tr, không phải sai số) trước khi migration — không tự chọn thay. Nhánh expat/intern trong công thức HR hoãn lại (thiếu 3 field dữ liệu + engine không hỗ trợ cú pháp `=TRUE`/so sánh chuỗi, đã verify bằng đọc parser). Migration `20260727000000_pit_system_to_formula.sql` (PIT.is_system=true nên phải UPDATE trực tiếp + tự ghi audit, vì CRUD thường sẽ no-op thầm lặng) đã áp dụng DB dev. Dọn sạch code chết (PIT là component `system` duy nhất — xoá `evalSystemComponent` + 7 hàm/field liên quan, `NewFormulaEngine` bỏ tham số brackets, cập nhật 10 call site). 2 test mới (migration verify + 8 case ranh giới bậc) + 1 test cũ cập nhật. Phát hiện phụ: `engine.go` gốc có CRLF lẫn trong repo LF (đã chuẩn hoá, xác nhận diff thật chỉ 253 dòng qua so sánh bỏ CRLF); `gofmt -w` chạy blanket cả thư mục làm hỏng 1 file không liên quan (`salary_component_service.go`, ký tự smart-quote thay ASCII trong comment) — đã `git checkout` revert. Không recompute 128 record `finalized` kỳ 05/2026 (theo yêu cầu, `payroll_records` thật hoàn toàn không bị đụng). Full suite: 4 fail baseline cũ, 0 hồi quy. Tài liệu: `self-docs/PIT-Formula-Fix-270726.md`. Đã commit `Core System-backend@42fd253` trên `feature_1` — **chưa push**.
- **270726** — Thi hành PLAN `260726-salary-structure-usage-and-grading-PLAN.md` Task 3.5→4.6 trên `Core System-backend@feature_1` (đổi model Opus 4.8 xhigh cho phần rủi ro nhất — wiring engine), xác nhận sau mỗi task theo yêu cầu người dùng. Task 3.5: cài `atlas` CLI (`brew tap ariga/tap`, máy chưa có), re-hash `atlas.sum`, baseline `go test ./...` (4 fail — khớp baseline cũ đã biết). Task 4.1: `ResolveAssignedTemplatesAsOf`/`ListAllTemplateComponentCodes` (bulk, dùng `sqlx.In`+`Rebind` khớp `ResolveLevelAsOf` thay vì `pq.Array` như PLAN gợi ý — nhất quán convention repo). Task 4.3: wire vào `calcShared` qua 1 helper DUY NHẤT `loadTemplateMaskData` (cả `Calculate` lẫn `CalculateOne` gọi chung) — triệt tiêu tận gốc bẫy "quên 1 trong 2 điểm khởi tạo" thay vì chỉ dựa test bắt lệch. Task 4.4 buộc gộp chung commit với 4.3.4 (Go không biên dịch được nếu tách — biến `tplSig` chỉ dùng ở `cacheKey`; tách sai còn gây bug tiền thật: 2 NV cùng `sig` khác template đụng cache). **Phát hiện lỗi thiết kế nghiêm trọng ở Task 4.2 khi soi lại bằng Opus** (trước khi viết Task 4.6): quy tắc mask v1 (`mask = tất cả − closure(template) − statutory − system`) chỉ đi chiều XUỐNG (dep của cột template), nhưng các cột TỔNG (`GROSS`/`TAXABLE_INC`/`PIT`/`NET_PAY`) đi chiều LÊN (tiêu thụ cột template) — verify bằng đọc đồ thị 110 cột thật: 1 template chỉ liệt kê vài phụ cấp (cách tự nhiên HR sẽ làm) sẽ zero-hoá TOÀN BỘ chuỗi tổng, không chỉ phụ cấp ngoài cấu trúc. Phát hiện trước khi chạy thật (0 template/assignment), không hại ai. User chọn hướng: sửa "Option A — inclusion + tập luôn tính cố định", rồi lại chọn nâng cấp tiếp sang **union-of-templates (data-driven)**: `maskableUniverse` = union mọi cột xuất hiện trong bất kỳ template nào đang tồn tại (không hardcode) — HR thêm/bớt phụ cấp qua UI, không cần sửa code. Task 4.5: **không dùng SQL thô như PLAN gốc đề xuất** (sẽ sai với thiết kế v3) — tái dùng nguyên `buildTemplateMaskFormulas`/`buildTemplateUniverse` ở tầng Go, snapshot theo TEMPLATE (không theo từng NV). Task 4.6: 7 test khoá bất biến (gộp đúng 8 bất biến PLAN yêu cầu), dùng component/PIT bracket THẬT từ DB (không phải bản rút gọn tay), 1 test end-to-end thật qua `Calculate`+`CalculateOne` (kỳ cách ly `2099-01`/`2099-02`, tự dọn sạch, không đụng dữ liệu thật). **Phát hiện phụ nghiêm trọng, ĐỘC LẬP với việc này, CHƯA SỬA:** PIT (thuế TNCN) luôn = 0 toàn hệ thống — `CalculateInputs.PeriodStart`/`PeriodEnd` chưa từng được gán ở bất kỳ đâu (grep 0 kết quả), bracket thật có `effective_from=2009-01-01` (≠ zero-value) nên `filterActivePITBrackets` luôn lọc rỗng; xác nhận bằng dữ liệu production thật (0/6435 `payroll_records` có PIT>0, kể cả dòng thu nhập chịu thuế 20+ triệu); đã loại trừ giả thuyết "PIT.Formula rỗng gây null" (system-type bỏ qua Formula hoàn toàn, verify bằng test tạm). Đã viết prompt bàn giao cho 1 phiên Opus riêng (thuần đọc+suy luận, không code) phân tích blast radius/phương án sửa — KHÔNG tự sửa (ngoài phạm vi, đổi số sẽ ảnh hưởng toàn bộ PIT lịch sử đã chốt, có thể chạm tuân thủ thuế). Toàn bộ: `go vet` sạch, full suite sau mỗi task chỉ 4 fail baseline cũ (đã biết, không hồi quy), DB xác nhận sạch sau mỗi lần test (không sót dữ liệu kỳ cách ly). Đã commit 10 commit tách riêng theo từng đơn vị logic + push `Core System-backend@feature_1` (`884244c`…`358585a`). Cập nhật tài liệu: `self-docs/Salary-Structure-Template-Analysis-260726.md` (mục 7 thêm phần "3 vòng thiết kế mask", mục 8 cập nhật điểm "2 điểm khởi tạo" đã giải quyết tận gốc, mục 9 viết lại full checklist + bảng commit, mục 10 mới — bug PIT), SPEC gốc (Task 4 → `[x]`, Assumptions A1 → đã trả lời), PLAN (tick Task 3.5-4.6, ghi chú "nội dung Task 4.2/4.5 trong file là bản thiết kế ĐẦU, đã bị thay khi thi hành" để tránh người đọc sau nhầm), `document-map.md` (3 entry), `Core System-backend/docs/routes-permissions.md` (section mới cho 12 route Core System-template + ghi chú enforce hiện live nhưng 0 hiệu lực thật). Còn lại theo PLAN: Task 5 (shadow-mode + kill-switch), Task 6 (FE), Task 7 (docs — coi như phần lớn đã làm xong đợt này).
- **260726** — Phiên nghiên cứu/quyết định (không code): chốt **Approach A1 = engine enforce THẬT theo template, thi hành bằng cơ chế mask-về-0** (không lọc danh sách component). Tự verify bằng đọc code trực tiếp thay vì tin mô tả SPEC — xác nhận rủi ro người dùng nêu là ĐÚNG và còn thêm 1 điểm thứ 3 chưa ai nêu: ngoài `engine.go:89` (`buildOrder` bỏ qua dep thiếu) và `engine.go:612` (biến thiếu → 0 im lặng), vòng nạp `source_field` tại `payroll_service.go:394-398` lặp trên chính `sh.components` nên lọc thô sẽ làm **cột input ngừng được nạp** từ attendance; thêm nữa lọc ở tầng OUTPUT cũng không đủ vì cột bị ẩn **vẫn cộng vào tổng thu nhập** (sai tiền, không chỉ sai hiển thị). Giải pháp: sinh override `formula="0"`, tái dùng nguyên `scopedResolver`/`applyResolvedFormulas`/`engineCache` — `engine.go` không sửa dòng nào; tập mask = tất cả − **tập đóng phụ thuộc** của cột trong template − `statutoryOverrideBlocklist` − cột `system`. Bác phương án trung gian "làm B trước" (khối lượng B và A giống hệt ở Task 3, làm B trước chỉ tạo giai đoạn bảng gán không có hiệu lực). Tra cứu lại Frappe/ERPNext xác nhận **chuẩn ngành cũng dính đúng lỗi silent-0 này** (component ngoài structure → công thức lấy 0, không báo lỗi). Phát hiện khi khảo sát: **Task 3 backend thực ra đã code xong nhưng chưa commit** (migration `20260726000000_employee_payroll_templates.sql` đã áp DB `payroll_engine`, repo/service/handler/12 route đầy đủ, `go build` sạch) — còn nợ hash `atlas.sum` và toàn bộ FE. Tài liệu: `llmwiki/wiki/sources/draft/260726-salary-structure-usage-and-grading-PLAN.md` (PLAN 10 task có lệnh chạy + output kỳ vọng), `self-docs/Salary-Structure-Template-Analysis-260726.md` mục 7-9 (quyết định + impact 5 điểm ngoài engine + checklist), cập nhật SPEC gốc + `document-map.md`. Chưa sửa dòng code nào.
- **260726** — Tiếp nối phiên 250726 (`Core System-frontend`, nhánh `feature_1`), theo yêu cầu người dùng qua nhiều lượt liên tiếp: (1) phát hiện khi mở thật trên UI — tab "Công thức" không có view riêng cho rule lương, chỉ đè ribbon lên sheet "Bảng lương tháng" đang mở sẵn (dễ hiểu nhầm "không hiển thị gì") — sửa: chuyển vào tab lần đầu tự mở sheet nhúng "Danh sách cột lương" (đọc sống `data.allComponents`, không phải snapshot tĩnh, tự cập nhật sau Thêm/Sửa/Xoá vì TASK-REF đòi hỏi) thay cho modal `NameManagerModal.tsx` cũ (đã xoá file). (2) Xác nhận engine đã hỗ trợ đầy đủ `+ - * /`, so sánh, `IF/IFS/SUM/MIN/MAX/ABS/ROUND` (đọc trực tiếp `engine.go:417-759`, không suy đoán) — thêm "Chèn hàm nhanh" (chip bấm chèn khung mẫu đúng vị trí con trỏ, tái dùng `insertAtCursor` đã tổng quát hoá từ Task 4 cũ) + cheat-sheet cú pháp trong `SalaryComponentModal.tsx`. (3) Rà soát toàn bộ nút riêng ribbon "Công thức", tối ưu 3 nút: "Tính tổng" mở modal Thêm cột (trước đây vô tri), "Dùng trong công thức" mở đúng modal theo ngữ cảnh (trước chỉ `alert`), "Đặt tên" tách khỏi "Trình quản lý tên" thành tạo-mới-trực-tiếp (trước trùng hành vi) — để nguyên "Tài chính"/"Ngày & Giờ" (vô tri, chưa có hàm backend tương ứng, chờ quyết định phạm vi riêng). Thêm banner giải thích trong sheet mới + tooltip (title, qua `ref`+`querySelectorAll` không sửa `ribbon-static.ts`) cho toàn bộ 8 nút riêng, sau khi nhận phản hồi "chính người xây tính năng còn nhầm ý nghĩa". (4) Commit + push cả 2 repo (`Core System-frontend@3d9fae4`, `Core System-backend@b16f9bf`), sau đó pull+merge `origin/develop` vào `feature_1` cả 2 repo theo yêu cầu — frontend có 2 conflict thật đã tự giải quyết (`SalaryComponentModal.tsx`: hợp nhất theme dark-mode `var(--tl-*)` của develop với `fnChipStyle` mới của mình; `TinhLuongExcel.tsx`: hợp nhất tham số `scope`/`filteredIdx` — tính năng lọc cột theo sheet của develop — với `dualActions` của mình, đồng thời **áp fix bug thật do develop tìm ra**: nhãn ribbon "Trình quản lý<br>tên" khiến `textContent` liền không dấu cách, khớp chuỗi cũ luôn `false` — nút icon lớn vô tri từ Task 6, chỉ "Đặt tên" chạy được; đã sửa cả tooltip cùng lỗi này). Backend fast-forward sạch, không conflict. Push lại cả 2 repo sau merge (`Core System-frontend@b4a5021`, `Core System-backend` fast-forward `e76e689`) — 1 lần mất kết nối SSH `code.vsol.vn` tạm thời, thử lại thành công. `tsc --noEmit`/`eslint` sạch sau mọi bước. (5) Theo yêu cầu người dùng, nghiên cứu 3 hệ thống Core System thật (Frappe/ERPNext HRMS qua DeepWiki, Odoo, Oracle HCM) đối chiếu với engine thật của dự án (`engine.go`, `payroll_service.go`) cho câu hỏi "5 bảng liên quan salary_components, bảng nào chưa dùng, dùng sao cho đúng" — phát hiện mô hình 3 tầng Component→Structure→Structure Assignment là chuẩn chung cả 3 hệ thống, tầng thứ 3 (gán structure↔nhân viên có ngày hiệu lực) là mảnh còn thiếu trong schema `payroll_templates`/`template_components` hiện tại. Viết `self-docs/Salary-Structure-Template-Analysis-260726.md` (phân tích canonical, không phải báo cáo ngày) + `/propose` SPEC `llmwiki/wiki/sources/draft/260726-salary-structure-usage-and-grading.md` (Phần A: hướng dẫn dùng override, không code; Phần B: cấu trúc lương theo cấp bậc, để trống 2 quyết định mở — engine có enforce thật hay chỉ preset UI, khoá `employee_level_id` hay `level_code`) — dừng ở bước chờ duyệt, chưa code task nào của SPEC mới này. Cập nhật `self-docs/document-map.md` (sửa lại entry cũ của `240726-formulas-tab-fe.md` cho khớp trạng thái mới, thêm 2 entry mới).
- **250726** — Theo yêu cầu người dùng (`/orca-workflow`, tiếp nối đánh giá 5 bảng `salary_components` từ 240726): `/propose` SPEC `llmwiki/wiki/sources/draft/240726-salary-component-db-hardening.md` (4 hạng mục: index, CHECK constraint `config_table`, UI tối thiểu cho `salary_component_overrides`, `COMMENT ON TABLE` đánh dấu `payroll_templates`/`template_components` "reserved") — user duyệt làm cả 4, chọn thứ tự Task 1→2→3 (gộp 1 migration) trước, xác nhận sau mỗi task. Phát hiện phụ khi duyệt: nhánh làm việc thật của cả 2 repo là `feature_1`, không phải `sec_dev` như quy ước cũ trong file này (`sec_dev` không tồn tại, kể cả remote) — ghi nhận trong SPEC, chưa tự sửa mục "Nhánh làm việc" ở trên (cần xác nhận đây là chủ đích rename/merge hay lệch quy ước thật). Đã triển khai + verify xong cả 4 hạng mục: migration `Core System-backend/atlas/migrations/20260725000000_salary_components_db_hardening.sql` (3 index + CHECK constraint + comment RESERVED, verify bằng `BEGIN;\i;ROLLBACK;` trước, áp dụng thật vào `payroll_engine` sau khi xác nhận, thử insert `config_table` sai tên bị từ chối đúng thiết kế), `Core System-frontend/components-page/tinh-luong/SalaryComponentOverridesPanel.tsx` (mới, CRUD override theo phòng ban/nhân viên) nhúng vào `SalaryComponentModal.tsx` (mode edit, cờ `canManage` có sẵn). `tsc --noEmit`/`eslint` sạch trên toàn bộ file sửa. Chưa mở trình duyệt xác nhận UI thật, chưa commit.

- **240726** — Theo yêu cầu người dùng (`/orca-workflow`, task tracker S004, tiếp nối khảo sát 230726): `/propose` đầy đủ (SPEC `llmwiki/wiki/sources/draft/240726-formulas-tab-fe.md`, sequence diagram `llmwiki/html/240726-formulas-tab-fe-seq.html`) rồi triển khai tuần tự 7 task, xác nhận sau mỗi task, user tự code (không dispatch agent khác) trên `Core System-frontend`. User chọn Approach A3-a cho Task 6 ngay lúc duyệt (tên = alias cho `salary_components.code`, không xây bảng "named values" độc lập). Kết quả: cả 7/7 task T0022-T0028 chuyển `done` — Task 1 (`FormulasRibbon.tsx`: gắn `onClick`/label-match delegation cho toàn bộ 6 nút, không sửa `ribbon-static.ts` vì file đó auto-extracted), Task 2 (`SalaryComponentModal.tsx` mới — form dùng chung Thêm/Sửa, gọi `validateFormula` trước lưu, gate quyền `hr_admin`/`cb_staff` khớp seed thật `20260723010000_role_permission_matrix_seed.sql`), Task 3 (`DeleteColumnDialog.tsx` mới — xử lý đúng nhánh 409 `usedBy`, không xoá), Task 4 (danh sách mã cột bên cạnh ô formula trong Task 2, chèn `[MÃ_CỘT]` đúng vị trí con trỏ qua `selectionStart/End`), Task 5 (`FormulaHistoryList.tsx` mới — nối `GET /config/salary-components/{id}/history` đã có sẵn thật, không cần endpoint ghép như tài liệu 230726 từng dự đoán), Task 6 (`NameManagerModal.tsx` mới — liệt kê+lọc mã cột, tái dùng danh sách đã load). `data.ts` thêm `allComponents`/`refetchComponents` để sheet chính cập nhật ngay sau lưu (TASK-REF), không cần F5. `tsc --noEmit` + `eslint` + `next build` sạch sau mỗi task (đã chạy lại toàn bộ, không chỉ file vừa sửa). Task 7 (tài liệu, dòng này): cập nhật `document/TASKS_v1.md` mục S004 (7 dòng T0022-T0028 → `done`) + `document/screen-task-breakdown/tasks.csv` (7 dòng đồng bộ, xác nhận CSV vẫn parse đúng cột qua `csv` module Python — 1 lỗi format tiền tồn tại ở dòng T0045/S006 không liên quan, không phải do sửa lần này). Chưa chạy dev server/trình duyệt thật để xem UI (user tự code và tự kiểm chứng chức năng theo yêu cầu SPEC "người dùng tự code phần FE"). Chưa commit.
- **230726** — Theo yêu cầu người dùng (`/orca-workflow`, task tracker S004 T0022/T0023 "Tab Công thức"): khảo sát backend thật (không phải hỏi/đoán) để viết tài liệu kỹ thuật cho đội frontend hiểu cấu trúc BE (đội đó chỉ làm giao diện). Phát hiện chính: T0022 (Chèn hàm) có backend đầy đủ thật — engine công thức viết tay (`internal/service/engine.go`, parser/evaluator, topological sort phụ thuộc, chống vòng lặp), lưu công thức theo cột trong bảng `salary_components` (không hardcode), override theo phòng ban/nhân viên (`salary_component_overrides`), snapshot công thức lúc chốt kỳ (`payroll_formula_snapshots`), CRUD + validate + lịch sử đầy đủ qua `SalaryComponentService`/`Handler`. Tiện thể xác nhận luôn T0026 (đang P0 "chưa rõ đã wiring hay còn mẫu") **đã wiring thật**, không phải dữ liệu mẫu. Ngược lại, T0023 (Trình quản lý tên) **chưa có module backend riêng** — "tên" hiện tại chỉ là `salary_components.code`, không phải danh sách biến/hằng số độc lập kiểu Name Manager — cần quyết định thiết kế trước khi code nếu UI cần khái niệm tên tuỳ ý thật sự. Tài liệu mới: `Core System-backend/docs/formulas-name-manager-backend.md` (thuần phân tích cho đội khác đọc, không code, không migration). Cập nhật `self-docs/document-map.md`.
- **230726** — Khảo sát theo yêu cầu người dùng (task tracker T0012/T0013): xác nhận 2 nút "Phê duyệt"/"Từ chối" bảng lương chỉ đổi state React, không có route BE nào; tra DB dev xác nhận `hr_admin`/`site_admin`/`search_profile` chưa có dòng permission nào cho `module=Core System action=approve` (đang "được duyệt" chỉ vì default allow-until-configured). `/propose` + `/plan` đầy đủ (SPEC+PLAN `llmwiki/wiki/sources/draft/230726-Core System-approve-reject-flow(.md/-PLAN.md)`, sequence diagram `llmwiki/html/230726-Core System-approve-reject-flow-seq.html`) — Phương án A (approve/reject ở cấp `PayrollRecord`, độc lập với status/finalize), dùng cờ `approve` có sẵn trong RBAC, reject bắt buộc lý do + tự chuyển về draft, seed permission tường minh (5 role). Triển khai xong cả 2 task code: Task 1 backend (migration `payroll_records.approved_at/approved_by/rejected_reason` + seed `permissions`, route `POST /Core System/approve/{id}`+`/reject/{id}` dùng `RequirePermission`+`RequireCompanyScopeMatch` theo record ID mới, audit log atomic) — `go test ./...`: 712 passed, 25 failed (baseline cũ, không hồi quy, đã xác nhận qua worktree ở commit trước). Task 2 frontend (nối 4 điểm gọi UI thật — ribbon, icon từng dòng, "Duyệt hàng loạt" — vào API mới; `deriveApproval()` tách đúng Approve/Reject khỏi status/finalize, trước đây `statusToApproval` gộp sai 2 khái niệm). Task 3 (tài liệu) đã cập nhật `document-map.md` + `RBAC-Backlog-Tracklist-160726.md` mục D3 (phân biệt rõ với gap uỷ quyền/delegation, chưa đóng). Migration đã áp dụng vào DB dev `payroll_engine` (xác nhận qua AskUserQuestion). Đã commit `Core System-backend@409c1d3` + `Core System-frontend@b784711` trên `sec_dev` — **chưa push**.
- **220726** — `/orca-workflow` đề xuất UI/UX cho các màn hình RBAC → `/propose` + `/plan` đầy đủ (SPEC + PLAN trong `llmwiki/wiki/sources/draft/220726-rbac-ui-ux-improvements(.md/-PLAN.md)`, sequence diagram `llmwiki/html/220726-rbac-ui-ux-improvements-seq.html`). User duyệt làm tuần tự Task 1→2→3→4, xác nhận sau mỗi task. Đã xong Task 1 (Audit log tóm tắt + modal xem JSON) và Task 2 (UI xem/sửa `roles.priority` kể cả role hệ thống, backend `RoleRepo.UpdatePriority`/`RoleService.UpdatePriority` + route `PUT /roles/{id}/priority`) — chi tiết đầy đủ + trạng thái git (chưa commit) đã ghi vào mục "Tiến độ" đầu file PLAN. Task 3 (sheet Rà soát quyền) + Task 4 (tìm kiếm/nhóm module) + Task 5 (ghi chú kiến trúc, thuần tài liệu) còn lại — đọc file PLAN mục "Tiến độ" trước khi làm tiếp, không cần hỏi lại từ đầu.

- **220726** — Sửa finding MEDIUM 1.4 + LOW 1.6 từ `RBAC-Technical-Code-Audit-210726.md`: `internal/middleware/auth.go` — `isSuperAdmin`/`allRoleCodes`/nhánh query chính của `getAppRoles` giờ `slog.Error` khi gặp lỗi DB THẬT, vẫn im lặng đúng như cũ khi là trạng thái hợp lệ (`sql.ErrNoRows`/0 role) — fail-safe cho phép/từ chối không đổi. 2 dòng `fmt.Printf` cũ (token invalid/valid) chuyển sang `slog.Warn`/`slog.Debug` kèm `request_id`, tôn trọng `LOG_LEVEL` — `token valid` (có email, PII) giờ ở mức Debug, không hiện mặc định. 6 test mới trong `auth_integration_test.go` (mỗi lỗi đều có 1 nhánh xác nhận im lặng ở trạng thái hợp lệ + 1 nhánh xác nhận có log khi lỗi DB thật, mô phỏng bằng đóng kết nối). Chưa viết test cho dòng Debug `token valid` (cần hạ tầng JWKS/JWT giả lập chưa có sẵn, chấp nhận rủi ro thấp vì thuần log-level). `go test ./...`: 711 passed, 9 failed (baseline cũ, không hồi quy). Cập nhật `self-docs/RBAC-Technical-Code-Audit-210726.md` mục 1.4/1.6 + `document-map.md`. Đã commit + push `Core System-backend@14c9dc2` lên `sec_dev`.
- **220726** — Sửa finding HIGH #2 từ `RBAC-Technical-Code-Audit-210726.md` (theo yêu cầu: phân tích chi tiết trước, chọn Phương án A, rồi triển khai): role-hierarchy và priority-tier hết dùng chung 1 danh sách role. Thêm `UserDirectRolesKey` (`internal/middleware/auth.go`, song song `UserRolesKey` — `getAppRoles` trả thêm `directRoles`). `resolveModuleActionPermission` viết lại nhận `directRoles`: role trực tiếp có dòng permission riêng → tuyệt đối, không tham khảo tổ tiên; chỉ fallback lên tổ tiên (kế thừa 140726) khi role trực tiếp không có dòng nào. Phát hiện phụ khi viết test: thiết kế "gắn nhãn priority theo sponsor" ban đầu (Phương án A bản đầu) vẫn cho tổ tiên không dòng nào đồng bỏ phiếu như role ngang hàng qua OR, vô hiệu hoá deny tường minh của role trực tiếp — phải sửa lại thuật toán mới đúng. Test mới `internal/middleware/permission_priority_hierarchy_integration_test.go` (2 test khoá bất biến), cập nhật `auth_integration_test.go` + helper `requestWithRoles` (`permission_integration_test.go`). Không đổi HTTP contract/route nào. `go test ./...`: 703 passed, 9 failed (baseline cũ, không hồi quy). Cập nhật `self-docs/RBAC-Priority-Hierarchy-Collision-Fix-220726.md` (mục 6) + `RBAC-Technical-Code-Audit-210726.md` mục 1.2 đánh dấu đã sửa. Đã commit `Core System-backend@7af7611` trên `sec_dev` — **chưa push** (user tự push).
- **220726** — Sửa finding HIGH #1 từ `RBAC-Technical-Code-Audit-210726.md`: `RoleRepo.Update` không còn no-op thầm lặng trên system role (thêm `ErrRoleUpdateNoRows` kiểm `RowsAffected()`), `RoleService.Update` phân biệt role-không-tồn-tại (`ErrRoleNotFound`) vs role-hệ-thống (`ErrRoleNotEditable`) dựa vào `GetByID` đã gọi sẵn, cả 2 đều không ghi audit log giả nữa; `RoleHandler.Update` trả 404/409 thay vì 200 giả. Test mới ở `internal/repository/role_integration_test.go` + `internal/service/role_service_audit_integration_test.go`. `go test ./...`: 700 passed, 9 failed (baseline cũ, không hồi quy). Cập nhật `self-docs/RBAC-Technical-Code-Audit-210726.md` đánh dấu đã sửa. Đã commit + push `Core System-backend@1616618` lên `sec_dev`.

- **220726** — Từ `RBAC-Tri-Party-Comparison-210726.md` (so sánh nội bộ+Gemini+GPT): triển khai P0#2 (3 route ESS IDOR-shaped — phát hiện frontend chưa nối dây thật, filePath hardcode demo; thêm self-scope TOFU `employees.hris_portal_employee_code` cho attendance-calendar, audit-log-only cho leave-details/timesheet-raw vì cookie opaque không đối chiếu được), P1#1 (audit log deny persist vào `audit_logs` qua context db stash ở AuthJWT), P3#1 (tài liệu `self-docs/RBAC-Permission-Precedence-220726.md` — đặc tả precedence 4 cơ chế authorization). Migration `20260722000000_hris_portal_employee_code.sql` đã áp dụng vào `payroll_engine` (xác nhận user). `go test ./...`: 695 passed, 9 failed (baseline cũ). Chưa commit.

- **210726** — Theo yêu cầu người dùng (3 bản phân tích trước — nội bộ+Gemini+GPT — chỉ nhìn logic/chính sách, thiếu góc kỹ thuật/code thật): audit code trực tiếp `internal/middleware/auth.go`/`permission.go`/`scope.go`, `internal/repository/role_repo.go`, `internal/service/role_service.go` — 6 finding đã tự tay đọc code + `grep` xác nhận (không chỉ tin agent): `RoleRepo.Update` no-op thầm lặng trên system role kèm audit log ghi sai giá trị (HIGH, tái hiện được ngay); `expandRoleHierarchy` (kế thừa quyền) và priority-tier (`resolveModuleActionPermission`) vô tình dùng chung `UserRolesKey` nên tổ tiên suy ra qua hierarchy cũng "bỏ phiếu" tranh chấp ưu tiên như role gán trực tiếp — leo thang/mất quyền ngoài ý muốn admin khi priority UI ra đời (HIGH, đang ngủ); cycle-safety của recursive CTE hierarchy dựa vào `UNION` (không phải `UNION ALL`) hoàn toàn ngẫu nhiên, không comment giải thích — rủi ro treo hệ thống nếu ai "tối ưu" nhầm (MEDIUM); lỗi DB trên đường resolve role bị nuốt hoàn toàn không log, hạ quyền âm thầm (MEDIUM); Assign/RemoveRole ghi audit log dù no-op (LOW/MEDIUM); `fmt.Printf` PII (email) ra stdout không qua `slog`/`LOG_LEVEL` (LOW). Đã loại trừ nghi vấn SQL injection, transaction `UpdatePermissions`, fail-open scope — xác nhận không phải bug. Tài liệu: `self-docs/RBAC-Technical-Code-Audit-210726.md` (mới), cập nhật `document-map.md`. Chưa sửa code nào — thuần audit, chờ người dùng chọn mục ưu tiên.
- **210726** — Theo yêu cầu người dùng, so sánh 3 bản phân tích RBAC độc lập (nội bộ `RBAC-Architecture-Reassessment-200726.md` + 2 second-opinion từ Gemini/GPT dùng context prompt phiên trước tạo): ma trận đồng thuận 7 rủi ro, 5 điểm 3/3 đồng thuận mạnh (RLS BYPASSRLS, 3 route ESS IDOR-shaped, SoD self-escalation cần chuyển sang chặn, default-allow không nên vĩnh viễn, audit log deny cần persist), 1 điểm bất đồng (Gemini muốn hợp nhất 4 cơ chế NGAY qua gateway middleware, nội bộ+GPT muốn hoãn — giữ quyết định hoãn), 6 hạng mục mới do 2 bên ngoài nêu. Tài liệu: `self-docs/RBAC-Tri-Party-Comparison-210726.md` (mới), cập nhật `document-map.md`. Chưa code.
- **210726** — Ưu tiên #3 mục 3, module thứ 2+3: Settings.Roles (9 route) và Departments/Companies (9 route) — cả hai xác nhận không đổi hành vi thật trước khi wire (config_admin đã full quyền, hr_admin mặc định allow-all). Module `Companies` chưa từng có tên trong UI "Ma trận theo Role" — đã thêm vào `Core System-frontend/lib/rbac-constants.ts` trước khi wire backend. Coverage 49/184 (27%) → 67/184 (36%). `go test ./...`: 671 passed, 9 failed (baseline cũ). Đã commit `Core System-backend@b5a439c`.
- **210726** — Quyết định: bỏ qua module thứ 6 (HRIS Connector/Sync/Pipeline) trong task-list Ưu tiên #3 mục 3 — không wire `RequirePermission`, giữ nguyên `RequireRole("hr_admin")`. Coi như đã hoàn tất mục 3 (5/6 module xong, 1 module chủ động bỏ qua). Cập nhật `self-docs/RBAC-Priority3-Module-Rollout-Tasklist-210726.md`.
- **210726** — Ưu tiên #3 mục 3, module thứ 4+5: Config/* (tách 5 module con mới `Settings.SalaryComponents/PITBrackets/TransportAllowances/PeriodConfig/EmployeeLevels`) và Attendance/Timesheets/AllowanceOverrides (tách `AttendanceSummary`/`AttendanceComputed`/`AllowanceOverrides` module mới) — cả hai xác nhận không đổi hành vi thật trước khi wire; bỏ qua route `/attendance/items` stub theo quyết định người dùng. Coverage 67/184 (36%) → 137/184 (74%). `go test ./...`: 689 passed, 9 failed (baseline cũ). Chi tiết: `self-docs/RBAC-Architecture-Reassessment-200726.md` mục 17, `self-docs/RBAC-Priority3-Module-Rollout-Tasklist-210726.md`. Đã commit `Core System-backend@6e13444` (xác nhận qua `git log`, dòng nhật ký này ghi lúc chưa commit — cập nhật lại trạng thái).

- **210726** — Tiếp tục RBAC: (1) sửa `AuditLogRepo.List` không trả cột `details` (old/new) — dữ liệu đã ghi đúng qua `Create()` nhưng API đọc lại luôn rỗng, nay đã trả đúng (test mới `internal/repository/audit_log_repo_integration_test.go`). (2) Bắt đầu Ưu tiên #3 mục 3 (mở rộng `RequirePermission`, Phương án A), module đầu tiên **Employees**: 30 route, action view/create/edit/delete; phát hiện bảng `permissions` đã có sẵn dữ liệu cấu hình từ trước cho module này (cb_staff, config_admin) — xác nhận người dùng giữ nguyên và cho enforce ngay (cb_staff mất quyền xóa dependents/educations/experiences/languages/work-histories, đúng ý cấu hình cũ, không phải hồi quy). Coverage 19/184 (10%) → 49/184 (27%). Task-list các module còn lại: `self-docs/RBAC-Priority3-Module-Rollout-Tasklist-210726.md` (Roles/\*, Departments/Companies, Config/\*, Attendance/Timesheets/AllowanceOverrides, HRIS Connector/Sync/Pipeline — chưa làm lúc viết dòng này). `go test ./...` trên `payroll_engine`: 660 passed, 9 failed (baseline cũ). Đã commit `Core System-backend@f751bda` (xác nhận qua `git log`, dòng nhật ký này ghi lúc chưa commit — cập nhật lại trạng thái).
- **210726** — Đánh giá kiến trúc RBAC tổng quan (self-docs/RBAC-Architecture-Reassessment-200726.md) tiếp tục: (1) triển khai Ưu tiên #1 Phương án A (`logAuthzAllowed`/`logSuperAdminBypass` mức Debug/Info kèm `request_id`, `LOG_LEVEL` env var) và Ưu tiên #3 Bước 1-2 (coverage test tự động 19/184 fine-gated, phát hiện 3 route ESS không tự lọc theo JWT như comment cũ) — đã commit `Core System-backend@fdc3c2a` trên `sec_dev`, **chưa push** (bị người dùng dừng lại giữa chừng để chuyển hướng công việc, chưa yêu cầu lại). (2) Xử lý mục 4 (bề mặt cấu hình RBAC không kiểm soát tự-cấp) theo hướng "audit-only trước" người dùng chọn — nối audit trail thật cho Create/Update/UpdatePermissions/AssignRole/RemoveRole role (`RoleService.audit` trước đây là dead code), cờ `selfReferential` không chặn. (3) Phân tích Ưu tiên #3 mục 3 (mở rộng RequirePermission ra module còn thiếu) — đề xuất Phương án A (thủ công từng module) phù hợp hơn Phương án B (bảng khai báo route) ở quy mô hiện tại. `go test ./...` trên `payroll_engine`: 655 passed, 9 failed (baseline cũ). Chưa commit đợt (2)/(3).

- **200726** — Triển khai Phase 1A test infra (theo `RBAC-Phase1A-Test-Plan-200726.md`, đối chiếu 2 file kế hoạch của phiên Claude web với code thật). Backend: 2 test tích hợp mới (`permission_priority_scope_integration_test.go`) chứng minh priority và company/department-scope là 2 trục độc lập; áp dụng migration `roles.priority` vào DB dev `payroll_engine` (đã hỏi và được xác nhận); tiện thể sửa 1 bug thật tiền tồn tại trong helper test `seedEmployee` (`ON CONFLICT` sai khớp ràng buộc unique composite từ 160726), làm sạch 3 test baseline hay fail. Bật gate cục bộ có sẵn (`make hooks`) thay vì thêm CI GitLab mới. Bằng chứng xanh thật lần đầu trên Postgres: `internal/middleware` 28/28 passed, toàn repo 644 passed/9 fail (baseline cũ, xác nhận qua `git stash`). Frontend: tách `roleMatrixChecked` khỏi closure thành hàm thuần `lib/permission-resolve.ts`, dựng Vitest lần đầu cho repo (`vitest.config.ts`, script `test`), 4 unit test đầu tiên xanh. Tài liệu: cập nhật `self-docs/RBAC-Improvement-Analysis-200726.md` (mục 8, 9). Đã commit + push cả 2 repo trên `sec_dev`: `Core System-backend@56d1ac4`, `Core System-frontend@99ec144`.
- **200726** — Đưa RBAC (Phân quyền/Gán quyền NV/Ma trận theo User/Ma trận theo Role) từ bản v1 (backup) sang bản Excel đang sống (`Core System-frontend/components-page/tinh-luong`), gộp hiển thị Gán quyền NV theo nhân viên + ẩn/hiện ribbon RBAC theo role (`hr_admin`/`config_admin`). Phát hiện + sửa 2 lỗi thật khi test đa-role: (1) FE `roleMatrixChecked` ghi đè sai default khi Lưu ma trận role khác `hr_admin`; (2) BE `resolveModuleActionPermission` gộp chung permission mọi role rồi mới xét mặc định — role đã cấu hình kéo tụt role chưa cấu hình dù mạnh hơn. Sửa thuật toán BE theo Hướng A (OR theo từng role) + nền tảng Hướng C (`roles.priority`, chưa có UI, mặc định trung lập không đổi hành vi). Thêm role hệ thống `config_admin` (IT thuần cấu hình RBAC, tách khỏi nghiệp vụ HR) — seed tự động, gate `/roles/*` + audit + đọc nhân viên read-only. Xác nhận cơ chế super-admin bypass (`super_admins`) không phải bug. Test: 3 test tích hợp BE mới tái hiện đúng bug (chưa chạy được thật, thiếu `TEST_DATABASE_URL`), `go build`/`go vet` sạch, `go test ./...` chỉ còn 2 fail baseline cũ (đã xác nhận không phải hồi quy qua `git stash`). Tài liệu canonical: `self-docs/RBAC-Improvement-Analysis-200726.md`, cập nhật `document-map.md`. Đã commit + push cả 2 repo trên `sec_dev`: `Core System-frontend@7af350b` + `@20004c6` (merge) + `@acc670b`; `Core System-backend@eb3f3f1` (đẩy kèm cả `@4662a11` từ 160726 trước đó chưa push được).
- **160726** — Triển khai nhóm A (A1, A2) + B1 từ `RBAC-Backlog-Tracklist-160726.md`, dừng trước B2 theo quyết định người dùng (quy mô B2 lớn hơn B1 đáng kể — 16 route package riêng biệt, không phải 1 điểm dispatch trung tâm như B1). A1: `Upsert()`/`UpdateFromHRIS()` (`employee_repo.go`) đổi khoá từ `(employee_code, company_code)` sang `hris_id`, kèm `resolveHRISIDForUpsert` xử lý placeholder-id-thăng-cấp-thành-id-thật khi Workday gửi hris_id lần đầu cho một dòng cũ. A2: thêm `companies.hris_id` + `employee_roles.scope_company_id` (FK), `ResolveCompanyScope` đổi sang JOIN sống giống `ResolveDepartmentScope`. B1: sửa điểm dispatch trung tâm `ExportReport` giới hạn company theo scope người gọi TRƯỚC khi rơi vào 13 report generator (11 file sửa trực tiếp `GetByPeriod`, 7 file được bảo vệ gián tiếp qua companyCode đã bị giới hạn), gắn `companyReadScope` middleware vào route `/reports/*`. Cả 3 đều có migration (`atlas/migrations/20260716000000_*.sql`, `20260716010000_*.sql`, đã áp dụng vào DB dev `payroll_engine` sau khi hỏi và xác nhận đúng DB) + test tái hiện (employee đổi mã giữ hris_id, company đổi mã JOIN bắt kịp, ExportReport chặn company ngoài phạm vi) + xác nhận `go test ./...` toàn repo: 623 passed, 12 failed — đúng 12 fail baseline cũ, không hồi quy mới. Phát hiện phụ trong lúc làm: `Core System-adapter` (Workday adapter riêng) có `wd_employees.employee_wid` cùng loại anti-pattern (lưu id ổn định nhưng upsert theo `employee_code`) nhưng không giao với RBAC/employee_roles (chỉ phục vụ tính lương nội bộ của adapter) nên không nằm trong phạm vi sửa. Cập nhật `self-docs/RBAC-Backlog-Tracklist-160726.md`, `document-map.md`. Đã commit `Core System-backend@4662a11` trên nhánh `sec_dev` — **chưa push được**: `git push origin sec_dev` timeout kết nối SSH tới `code.vsol.vn` (GitLab nội bộ, có thể cần VPN/mạng công ty). Cần push lại khi có kết nối.
- **160726** — Tổng hợp toàn bộ hạng mục RBAC/bảo mật còn mở (từ 4 tài liệu 140726–160726) thành 1 tracklist canonical: `self-docs/RBAC-Backlog-Tracklist-160726.md`, xếp theo cái gì đang chặn (sẵn sàng làm ngay / chờ quyết định nghiệp vụ / chờ dọn dữ liệu / có thể shadow-mode trước / ngoài phạm vi RBAC). Ưu tiên đề xuất: (1) chuyển app sang DB role non-superuser để RLS trên `employees` có hiệu lực thật (phát hiện cũ ở 150726, hiện "tồn tại nhưng vô hiệu", chưa từng nêu bật thành action item riêng), (2) company-scope sang FK, (3) employee upsert sang `hris_id`, (4) SoD shadow-mode (G7, không cần chờ quyết định mới bắt đầu được), (5) áp dụng agent_data_role (G10) ở dev/staging trước. Cập nhật `document-map.md`. Chưa code, chỉ tổng hợp tài liệu.
- **160726** — Bổ sung mục 7 (đặc tả cột cụ thể cho đội data/adapter) vào `self-docs/RBAC-Workday-Golive-Impact-160726.md`: `companies.hris_id`, `hris_attendance_daily.org_structure_id` (cột mới), `employees.hris_id` (đã có, cần xác nhận cách populate), `employee_roles.scope_company_id` (nội bộ RBAC). User xác nhận có toàn quyền quyết định quy cách dữ liệu — đã chốt **cả 2 cột mới dùng kiểu `uuid` gốc** (khớp `employees`/`org_structures` đã có), không phải điểm mở bàn với đội data/adapter; chỉ còn vài sự thật kỹ thuật (tên field API Workday, tính bất biến của id) cần họ xác nhận.
- **160726** — Phân tích ảnh hưởng của golive Workday (đổi mã nhân viên/công ty/phòng ban) lên RBAC đã triển khai. User xác nhận Workday cấp id ổn định + code/name riêng cho cả NV/công ty/phòng ban — đối chiếu code lộ ra: department-scope đã đúng chuẩn từ đầu (`scope_department_id` là FK thật, `org_structures` upsert theo `hris_id`), nhưng **2 rủi ro thật cùng họ nguyên nhân** (dùng chuỗi mã nghiệp vụ làm điểm neo thay vì id/FK): (1) upsert nhân viên khoá theo `(employee_code, company_code)` dù cột `hris_id` có sẵn nhưng bỏ không dùng — nguy cơ `employee_roles` mồ côi khi Workday đổi mã NV; (2) company-scope (`employee_roles.scope_company_code` là string snapshot, `companies` không có `hris_id`, upsert theo code + `DO NOTHING`) — đổi mã công ty sẽ làm mất quyền **cả một công ty** cùng lúc. User chọn hướng xử lý cho cả hai: **chuyển sang hris_id/FK giống mẫu department-scope** (chưa code cả hai). Ngoài ra `hris_attendance_daily` thiếu `org_structure_id` (chỉ có tên) — đề xuất thêm cột nhưng thuộc phạm vi đội dữ liệu/connector, không tự làm. Tài liệu canonical: `self-docs/RBAC-Workday-Golive-Impact-160726.md`, cập nhật `document-map.md`. Chưa code, chưa commit.
- **160726** — Triển khai G1/G4/G9/G10 (đã phân tích ở 150726). G1 (`internal/app/router_role_gate_test.go`, `chi.Walk()` test-guard) **tìm ra 2 lỗ hổng high-risk mới cùng loại thiếu RequireRole**: Employee routes (List/GetByID/contracts/dependents/educations/**bank-accounts**, IDOR hoàn toàn, không kiểm tra chủ sở hữu) và HRIS connectors + departments (thiếu requireAdmin). Đã vá cả 3 (Employee: `search_profile` chỉ List/GetByID theo xác nhận người dùng qua 2 AskUserQuestion; HRIS/departments: thêm lại requireAdmin, không cần hỏi vì không mơ hồ thiết kế). G4: `GET /roles/access-review` (cờ multipleRoles/unusualScope). G9: `self-docs/RBAC-JML-Checklist-Van-Hanh-150726.md`. G10: script `create-agent-data-role.sql` đã viết nhưng **auto-mode Claude Code chặn tự chạy** (cấp quyền DB thật), người dùng chọn giữ lại chưa áp dụng. `go test ./...`: 612 passed, 12 failed (baseline cũ, không regression). Tài liệu mới: `self-docs/RBAC-Improvement-Analysis-160726.md` (canonical), cập nhật `RBAC-Bao-Cao-Bao-Mat-150726.md`, `architecture-synthesis.html`, `document-map.md`, `Core System-backend/docs/routes-permissions.md`. Chưa commit.
- **150726** — Phân tích đầu mục RBAC còn lại ngoài G1–G10 (đối chiếu `Structure-Security-001.md`+`Core System-Architecture-Roadmap.md`, 6 mục R1–R6) + đề xuất ABAC thuần không OPA (3 phương án, chốt Phương án B — bảng cột tường minh, shadow mode giai đoạn đầu; ABAC chưa code, chờ số liệu nghiệp vụ: ngưỡng tiền, giờ hành chính, IP). Triển khai R1 (RLS trên `employees` — migration `20260715000000_employees_row_level_security.sql`, fail-open, wire qua `ResolveReadCompanyScope`+`internal/repository/scoped_executor.go`; **phát hiện app đang dùng role Postgres `postgres` superuser/BYPASSRLS nên RLS chưa có tác dụng thật, cần đổi role DB ở vận hành thật sau**) và R2 (mask 6 nhóm phụ cấp + breakdown cho `site_admin` trên `GET /hris/attendance-daily/matrix`, `maskAllowancesForSiteAdmin`). Test mới: 1 integration (tự tạo Postgres role NOSUPERUSER/NOBYPASSRLS để chứng minh RLS có hiệu lực thật) + 3 unit (masking). `go test ./...` (có `TEST_DATABASE_URL`): 396 passed, 12 failed (baseline cũ, không regression). Cập nhật `RBAC-Improvement-Analysis-150726.md` (mục 8/9 mới), `RBAC-Bao-Cao-Bao-Mat-150726.md`, `architecture-synthesis.html`, `routes-permissions.md`. Chưa commit.
- **150726** — Commit + push `Core System-backend@5b59fff` lên `sec_dev`: permission-per-action (View/Edit/Export) cho Core System/PayrollPeriods/Reports (Option C, allow-until-configured), fix bug thật `Lock()` chưa từng ghi `locked_by`/`locked_at`, structured `access_denied` logging (gap G2, `internal/middleware/access_audit.go`) đã đóng gói cùng commit này. "Duyệt thay" vẫn chưa làm, chờ quyết nghiệp vụ.
- **150726** — Triển khai G2 (audit log có cấu trúc cho quyết định TỪ CHỐI quyền): `internal/middleware/access_audit.go` + wire vào 5 điểm deny thật (`RequireRole` ×2, `RequireUnrestrictedScope`, `RequireCompanyScopeMatch`, `EnforceSiteAdminDepartmentFilter`, `RequirePermission`); dùng `log/slog` stdlib, không thêm dependency, không migration DB. Test: 3 unit test mới + mở rộng 1 test tích hợp có sẵn. `go test ./...`: 371 passed, 4 failed (baseline cũ, không regression). Cập nhật `self-docs/RBAC-Improvement-Analysis-150726.md`, `self-docs/RBAC-Bao-Cao-Bao-Mat-150726.md`, `self-docs/architecture-synthesis.html`, `Core System-backend/docs/routes-permissions.md`. Chưa commit.
- **150726** — Commit + push `Core System-backend@8a341ef` lên `sec_dev`: đóng 3/4 yêu cầu bảo mật mới (role-gate cho route đọc Core System lõi, company-scope cho READ, audit reason bắt buộc + old/new value). Yêu cầu 1 (permission chi tiết theo action) chưa làm, cần quyết kiến trúc trước. Chi tiết: `self-docs/RBAC-Improvement-Analysis-150726.md`.
- **150726** — Cập nhật `self-docs/architecture-synthesis.html` khớp tiến độ thật: 5 role, 9 gap (thêm 5 gap mới từ 4 yêu cầu bảo mật), footer trỏ commit `sec_dev` vừa push.
- **150726** — Phân tích 4 yêu cầu bảo mật mới (phân quyền chi tiết theo action, ẩn dữ liệu theo pháp nhân, phạm vi chỉ HR/C&B, audit log old→new+reason bắt buộc); đối chiếu với RBAC hiện tại, phát hiện xung đột: đề xuất mở `/timesheets` cho `site_admin` (T4) có thể trái yêu cầu "Core Core System chỉ HR/C&B" — đang chờ user quyết.
- **150726** — Định nghĩa `site_admin` theo DB seed thật (`Site Administrator` / "Project site secretary, view project timesheets"), đề xuất phương án phân quyền (Option A/B), user chọn hướng Option B 2 bước.
- **150726** — Commit + push nhánh `sec_dev`: `Core System-backend@a3caeef` (scope middleware company/department), `Core System-frontend@4d624c3` (reconcile navItem roles, xoá `pm`, doc gap `site_admin`).
