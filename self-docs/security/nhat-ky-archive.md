---
id: self-docs/security/nhat-ky-archive
canonical_question: 'Technical guide and specification: Nhật ký công việc theo ngày
  — Archive'
aliases:
- Nhật ký công việc theo ngày — Archive
- Nhat Ky Archive 160926
entity_type: how_to
domain: self-docs > security
last_verified: 2026-09-17
---

# Nhật ký công việc theo ngày — Archive (110826–310826)

Chuyển từ `CLAUDE.md` mục "Nhật ký công việc theo ngày" ngày 160926 để giảm token nạp mỗi session — tiếp nối `Nhat-Ky-Archive-170826.md` (bao phủ đến 150726). `CLAUDE.md` giữ lại log từ 030926 trở về sau (gần nhất). Nội dung dưới đây nguyên văn, không rút gọn.

- **310826** — Khảo sát toàn bộ luồng migrate DB trong `Core System-backend`, phát hiện 3 luồng song song (boot-time auto-migrate, Atlas, script fallback cũ) và phát hiện quan trọng: `docker-entrypoint.sh` (sửa 300826) đã âm thầm biến Atlas thành luồng migrate THẬT thứ 2 chạy trên production (trước đó tưởng chỉ là bookkeeping khai báo). User chốt xoá hẳn Atlas, chỉ giữ luồng boot-time. Nhánh `chore/remove-atlas-migration-flow` (từ `develop_v1`, không phải `sec_dev` dù việc này chạm schema RBAC — theo lựa chọn user vì mục đích là dọn tooling, không đổi hành vi phân quyền). Trước khi xoá, đối chiếu kỹ 41 file atlas migration với luồng chính, phát hiện Atlas là nơi DUY NHẤT tạo ra 1 khối lớn schema đang dùng thật chưa từng được port: toàn bộ approval engine (`approval_requests/rules/steps`), Workday org/holiday sync (`wd_organizations/wd_cost_centers` — xác nhận dev DB thật cũng đang thiếu 2 bảng này), `salary_component_pending_changes`, bản thân bảng `adapter_sync_state`, và gần như toàn bộ RBAC hybrid scoping (`roles.parent_role_id/priority`, `employee_roles.scope_company_code/scope_department_id/scope_company_id/email` + trigger + RLS trên `employees`, `employees.hris_id`/`hris_portal_employee_code`, `companies.hris_id`). Port nguyên văn 19 file mới `migrations/v70-v88.sql` vào luồng chính (tracked-once qua `schema_migrations`, đúng thứ tự phụ thuộc), sửa 2 chỗ thiếu guard idempotent (`ADD CONSTRAINT`/`CREATE POLICY`) trước khi copy để không vỡ trên DB đã có sẵn qua atlas. Xoá `atlas.hcl`, `atlas/migrations/` (41 file), stage atlas + entrypoint atlas-apply trong `Dockerfile`/`docker-entrypoint.sh`, `Makefile` targets, `scripts/atlas-check.sh`, `.githooks/pre-commit`, hook atlas trong `.claude/settings.json`. **Xác minh migrate mechanics thật** (không chỉ đọc code): dựng DB test từ `pg_dump`/`pg_restore` DB dev thật, xoá thủ công đúng các đối tượng atlas-only để mô phỏng "môi trường chưa từng chạy atlas apply", chạy `RunFileMigrations` — 19 file chạy sạch, chạy lại lần 2 xác nhận idempotent, đối chiếu `\d employee_roles` khớp tuyệt đối schema thật, số bảng chênh đúng 2 (`wd_*`, dev thật cũng đang thiếu). `go build`/`go vet` sạch, `go test ./...` 843 passed/0 failed (0 hồi quy). Phát hiện phụ không sửa (tiền tồn tại, ngoài phạm vi): `RunMigrations` có bug thứ tự khiến bootstrap từ DB hoàn toàn trống bị lỗi ở migration 83 (`insurance_configs` chưa tồn tại) — khớp giới hạn đã tài liệu hoá sẵn, không phải do việc này gây ra. Chưa commit, chưa push. Xem `self-docs/Atlas-Migration-Flow-Removal-310826.md`.
- **250826** — Khảo sát hạ tầng approval engine có sẵn (240726, chỉ nối dây cho `payroll_record`), rồi `/propose`+`/plan`+thi hành đầy đủ SPEC "duyệt tính lương 2 cấp (cb_lead→cb_director) + hạn chót theo `period.end_date+N` + cảnh báo trễ hạn + hợp nhất khoá kỳ (thủ công HR + auto-lock 21-20) + version Core System sau khi khoá kỳ, đi qua lại đúng 2 cấp duyệt" — 12 task, cả 2 repo, nhánh `sec_dev` (BE tách từ `feature/Core System-period-schedule-gate` để giữ code 240826 mà PLAN phụ thuộc). 4 quyết định thiết kế chốt qua `AskUserQuestion` trước khi viết PLAN. 3 phát hiện quan trọng lúc đọc code thật làm tinh chỉnh cách thi hành (không phải SPEC sai): (1) `/Core System/approve/{id}` đã multiplex submit+act qua `SubmitOrAct`, actor không đủ quyền thì lỗi bị nuốt trả 200 (đúng vấn đề "detach" SPEC nêu); (2) trang `/v1/approvals` đã là bộ hành động đa cấp thật, giảm khối lượng cần làm; (3) `AdapterSyncService` không ghi dữ liệu nào có `period_id` — diễn giải lại thành "tạm dừng toàn bộ vòng đồng bộ khi có kỳ đang khoá" thay vì chặn theo từng kỳ. BE: 2 role mới, cột deadline (2 kênh migration — atlas file + Go-const idempotent, vì backend không tự chạy atlas apply), `resolveDueDate`, route `/approvals/submit` + `/Core System/{id}/versions`, hợp nhất `checkLock()` (vá luôn lỗ hổng có sẵn: route Reject trước đây không hề gọi `checkLock()`), bảng `payroll_record_versions` tái dùng nguyên chain rule của `payroll_record`. FE: nút "Trình duyệt lại" (phát hiện thêm: sau reject, nút Duyệt/Từ chối cũ tắt hẳn, không còn cách nào trình duyệt lại), banner quá hạn, nút "Tạo bản sửa" khi kỳ khoá. `go test ./internal/...`: 1052/6 (6 fail xác nhận tiền tồn tại qua `git stash` trước mỗi commit, 0 hồi quy). FE `tsc`/`eslint` sạch, `vitest` 109/2 (2 fail tiền tồn tại không liên quan). Migration áp bằng `psql` trực tiếp lên DB dev local (theo lựa chọn user). Chưa push, chưa merge. Xem `self-docs/Core System-Multilevel-Approval-Period-Lock-250826.md`.
- **250826** — Kiểm bằng Playwright click-through thật (dev-login bypass, `go run ./cmd/Core System` + `npm run dev` local) toàn bộ tính năng vừa code ở mục trên: luồng duyệt 2 cấp thật (submit→duyệt cấp 1 (role `cb_lead` gán tạm)→duyệt cấp cuối (role `cb_director` gán tạm)→`approved_at` đúng), banner+nhãn "Quá hạn", nút "Trình duyệt lại", khoá kỳ thủ công chặn đúng 423 cho CẢ approve và reject (click UI thật, không chỉ curl), nút "Tạo bản sửa" tạo version+tự trình duyệt+hiện đúng nhãn "Bản sửa bảng lương (sau khoá kỳ)" trên `/v1/approvals`. **Phát hiện + sửa 1 bug thật lúc reload trang sau "Trình duyệt lại"**: `payroll_records.rejected_reason` không được xoá khi resubmit → `deriveApproval()` ở FE vẫn hiện "Từ chối" dù đã có request mới "pending" — sửa bằng xoá `rejected_reason` trong cùng transaction với `CreateRequestTx`, thêm test `TestIntegrationResubmitClearsRejectedReason`, xác nhận lại bằng Playwright sau khi vá. `go test ./internal/...`: 1053/6 (0 hồi quy, +1 test mới). Đã dọn sạch toàn bộ dữ liệu test (period/record/version/role gán tạm) khỏi DB dev, tắt cả 2 server. Vẫn chưa push, chưa merge.
- **240826** — Thi hành SPEC/PLAN "nút Duyệt tất cả bảng lương + gate tính lương theo chốt công + chu kỳ lương 21-20" (8/9 task, Task 9 = tài liệu này). Bắt đầu từ yêu cầu khảo sát "luồng duyệt bảng công tạo bảng lương", qua 4 lượt trao đổi phát hiện 3 dead code/cấu hình mồ côi quan trọng: (1) bảng công đã có sẵn wizard scope Tất cả/Trang/Đã chọn từ trước; (2) `payroll_period_config` (period_start_day=21/end_day=20) tồn tại trong DB nhưng chưa từng nối dây; (3) nhánh `scope="all"` của bảng lương đã viết sẵn trong `confirmModal()` nhưng chưa có nút bấm tới — nhờ đó bỏ hẳn phương án route BE bulk mới, chỉ cần thêm 1 nút. Nhánh `feature/Core System-period-schedule-gate` (từ `origin/develop`, cả 2 repo — không phải `sec_dev` vì đây là business logic, không phải RBAC). BE: migration `v58` thêm cột lịch chạy/khoá, `ComputeNextPeriodRange`/`IsWithinLockWindow`, `PayrollPeriodScheduler` mới (ticker 1 phút giờ VN, tự tạo kỳ + chốt công + tính lương), khoá 4 route (423), gate `Calculate` lọc `is_finalized` từ kỳ 09/2026 (đổi chữ ký trả thêm `excludedEmpCodes`, cập nhật 6 call site). FE: `usePayrollLockStatus` hook, disable nút khi khoá, nút "Duyệt tất cả" mới, trang cấu hình mới `/v1/settings/Core System-period` (FE trước đó 0 UI cho config này), banner cảnh báo NV bị loại. `go test ./internal/...`: 795/0 (từ 794/0, 0 hồi quy). `npm run test`: 106/106. **Xác minh thật trên DB dev** (không phải chỉ unit test): chạy `go run ./cmd/Core System` local, `curl` trực tiếp xác nhận migration áp dụng sạch, GET/PUT period-config đúng, và đặc biệt: đặt cấu hình ép khoá → cả 4 route trả đúng `423` kèm `unlockDate`; phát hiện phụ thú vị — cấu hình MẶC ĐỊNH (21→24) khiến hôm nay 24/08/2026 đang thật sự nằm trong khoảng khoá, xác nhận gate đúng trên lịch thật. Đã tắt server + trả cấu hình DB dev về mặc định trước khi kết thúc. Chưa kiểm bằng Playwright (dự án không có hạ tầng test component, và phiên này không khởi động được trình duyệt) — khuyến nghị kiểm tay trước khi merge. Chưa push, chưa merge. Xem `self-docs/Attendance-Core System-Approval-Gate-240826.md`.
- **220826** — Thi hành 3 ý tưởng tối ưu tạo template báo cáo nhiều cột (copy/paste kiểu Office), từ dễ tới khó, theo yêu cầu user (ca thật: report ~100 cột, bấm tay "+ Thêm dòng" quá chậm). (1) Chọn nhiều dòng Shift/Ctrl+click ở gutter + Sao chép/Xoá theo lô qua menu chuột phải. (2) Dialog "Thêm nhiều dòng từ cột lương" (checkbox, dùng lại component `Modal` chung Tailwind) — tick N cột lương có sẵn sinh N dòng cùng lúc; tự phát hiện + tự sửa 1 bug: rule CSS toàn cục `input{width:100%}` (app/globals.css) kéo giãn checkbox, sửa bằng ép width/height inline đúng tiền lệ đã có trong file. (3) Dán từ Excel (Ctrl+V, TSV) — viết hàm thuần `parseReportLinesFromClipboardText` (test được), tự suy Nguồn dữ liệu khi thiếu cột đó (khớp `[`→công thức, `,`→tổng nhiều cột, khớp tên/mã cột lương thật, khớp nhãn định danh), không throw, luôn trả `warnings` cho dòng cần tự kiểm tra. `tsc`/`eslint` sạch, `vitest` 38/38 pass (14 test mới), kiểm cả 3 ý bằng Playwright thật trên dev server local (phải khởi động lại tay be `:8080`/fe `:3000` vì đã tắt từ phiên trước). **User yêu cầu giữa phiên: đợt này chỉ dừng ở test kỹ tại local, KHÔNG tự commit/push** — chờ xác nhận. Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md` mục Phase 8.

- **260826** — Restore DB `payroll_adapter` local từ dump `payroll_adapter_dump_20260825 (1).sql` (drop+recreate, bỏ 2 dòng `\restrict`/`\unrestrict` không tương thích psql 14 local, 0 lỗi, 65 bảng/4.226 worker/536MB). Dựng `Core System-adapter serve` chạy `:8081` (sửa `.env`: `HTTP_ADDR`→`API_ADDR` đúng tên biến code đọc, thêm `SYNC_API_TOKEN` bắt buộc để service khởi động). Chạy `go run ./cmd/summary-sync --write` (CLI mới, thay route HTTP `generate-from-daily` đã bị tắt hôm nay) để sync `attendance_summary` (Core System-backend, nhánh `develop_v1`) từ adapter cho cả 8 kỳ 01-08/2026 — lộ ra 2 lớp bug schema mà commit `bf1f399` (16:12 cùng ngày, cùng tác giả) chưa bắt hết: thiếu 8 cột (`start_date`/`end_date` + 6 cột OT/cơm) và `department_code varchar(50)` quá hẹp cho tên phòng ban thật (58 ký tự). Vá bằng 3 migration mới cùng khuôn `bf1f399` đã đặt (`ADD COLUMN IF NOT EXISTS`, không sửa file đã tracked-applied): `v64_attendance_summary_start_end_date.sql`, `v65_attendance_summary_ot_meal_units.sql`, `v66_attendance_summary_department_code_width.sql`. Sync lại thành công cả 8 kỳ, 0 lỗi (dòng theo kỳ: 3265/3938/3846/4013/3971/3734/3945/3810). Xem `self-docs/Attendance-Summary-Adapter-Sync-Schema-Fix-260826.md`. `Core System-backend@develop_v1`: 3 migration mới, chưa commit (nhánh có người khác đang làm dở, để họ tự commit theo luồng rewrite của họ).
- **220826** — Sửa bug PRE-EXISTING (không do các đợt UI/UX Report Template gây ra — đã xác minh bằng `git worktree` tại commit trước khi làm việc đó): mở sheet "Kỳ lương" hoặc "Phân quyền" (ruy-băng Cấu hình) làm TOÀN BỘ nút khác trên ruy-băng đứng hình không bấm được. Đo bằng `MutationObserver`+patch `Element.prototype.innerHTML` xác nhận `TinhLuongExcel` tự render lại ~240 lần/giây trong lúc 2 sheet này mở, ghi lại đúng div `dangerouslySetInnerHTML` (ruy-băng tĩnh) hàng trăm lần/giây dù giá trị y hệt — đã đo loại trừ 15+ hook/effect riêng, toàn bộ state nội bộ `usePayrollData()`, `useAuth/useToast/useRouter`, RAF auto-scroll nhưng **không tìm ra được cơ chế chính xác** khiến component cha tự render lại (ghi rõ trong tài liệu, không giả vờ đã biết). Fix theo hướng chặn đúng điểm gây treo bất kể nguyên nhân sâu: tách file mới `StaticRibbonHtml.tsx` — 1 component lá `React.memo(forwardRef(...))` chỉ nhận prop `html` (string hằng số module) nên luôn bail-out, không bao giờ re-commit dù cha render lại bao nhiêu lần; áp cho cả `SETTINGS_STATIC` (`SettingsRibbon.tsx`) và `HOME_CLIPBOARD` (`commonControls` trong `TinhLuongExcel.tsx`). `tsc`/`eslint` sạch, `vitest` 97/4 (4 fail tiền tồn tại, xác nhận bằng `git stash`). Playwright thật: mutation 0 (trước đó hàng nghìn), chuyển sheet cấu hình khác phản hồi tức thì. Phát hiện phụ không sửa (ngoài phạm vi): tile "Kỳ lương" không bao giờ được tô nền "đang mở" do so khớp tuyệt đối text bị lệch bởi icon "₫" — bug có từ lúc tách `SettingsRibbon.tsx`, không liên quan đợt này. Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md` mục Phase 9. Chưa commit, chưa push (theo đúng yêu cầu "test local trước, chờ xác nhận mới push" đang áp dụng từ Phase 8 cùng file).
- **200826** — Cải thiện UI/UX màn "Cấu hình > Template báo cáo" (FE, `feature_v2`), **đợt 5**: dòng "Công thức tuỳ chỉnh" trước cao gấp ~5 lần dòng thường, cột "Mã cột" phải rộng bất thường — làm 5 việc: (1) rút gọn ô công thức về 1 dòng + icon mở popover chứa toàn bộ khối sửa cũ; (2) icon thay chữ cho "Kiểm tra"/"Xoá dòng"; (3) bôi đậm số dòng đang active (focus); (4) kéo giãn cột (tái dùng nguyên cơ chế resize của lưới chính, key riêng `rtpl_*`); (5) đóng băng cột "Nhãn hiển thị" khi cuộn ngang. **Tự phát hiện + tự sửa 2 bug khi kiểm bằng Playwright (đo `getBoundingClientRect`/`getComputedStyle` thật, không chỉ nhìn ảnh):** (a) `table-layout:fixed` thiếu `width` cho `<table>` khiến trình duyệt "shrink-to-fit" ép bảng co theo khung nhìn, đè lên độ rộng cột đã kéo — sửa bằng đặt thẳng width = tổng cột; (b) cột A bị cột D (1740px) đè hình ở 2 hàng tiêu đề vì cả 2 cùng `position:sticky` (cần để đứng lại khi cuộn dọc) nên z-index bằng nhau thì thứ tự DOM quyết — sửa bằng nâng z-index cột A cao hơn hẳn. `tsc`/`eslint`/`vitest` (24/24) sạch. Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md`. Đã commit `e3a3c88` (gộp cả 5 đợt) + push `origin/feature_v2` — MR sẵn có !44 tự cập nhật, không tạo MR mới.
- **210826** — Sửa tiếp nút "+ Thêm dòng" (màn Cấu hình > Template báo cáo) bị trôi lệch nằm ngang hàng bên phải bảng thay vì xuống dòng dưới — user báo bằng ảnh sau khi đã push đợt trước. Nguyên nhân: div bọc `<table>` là `display:inline-block` (cần cho việc co khít độ rộng cột đã làm ở đợt 200826), `<button>` mặc định CŨNG inline-block — 2 phần tử inline-level liền kề bị trình duyệt xếp cùng dòng. Sửa: ép `display:"block"` + `width:"fit-content"` cho chính nút. `tsc`/`eslint`/`vitest` sạch, kiểm bằng Playwright thật. Chỉ 1 file `TinhLuongExcel.tsx`. Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md` mục Phase 6. Đã commit `87e0db4` + push (mạng bị timeout 1 lần, thử lại thành công sau khi user bật VPN).
- **210826** — User hỏi so sánh style nút với sheet "Danh sách cột lương" (Cấu hình rule lương): bên đó "+ Thêm cột" là nút ruy-băng JSX thật (`FormulasRibbon.tsx`), không phải nút chữ rời dưới lưới. Khảo sát xác nhận đúng vậy, đồng thời xác nhận sửa công thức bên lương dùng inline-expand TẠM (double-click 1 dòng, không phải luôn-mở-mọi-dòng) — nên khuyến nghị giữ popover cho Report Config (đúng, tránh lại lỗi dòng cao gấp 5 lần đã sửa) nhưng đồng bộ vị trí nút "+ Thêm dòng" lên ruy-băng. User chốt làm: thêm prop `onAddReportLine` (chỉ hiện khi đang mở sheet Sửa template) vào `SettingsRibbon.tsx`, xoá hẳn nút rời dưới bảng. Kiểm bằng Playwright: nút ẩn/hiện đúng theo context, thêm dòng đúng số lượng. `tsc`/`eslint`/`vitest` sạch. Chưa commit.
- **200826** — Cải thiện UI/UX màn "Cấu hình > Template báo cáo" (FE, `feature_v2`), **đợt 4**: user vẫn thấy nút "+ Thêm template báo cáo" không giống style Excel dù đã dời khỏi formula bar — dời hẳn lên RUY-BĂNG "Cấu hình" làm 1 nút icon+nhãn thật (khảo sát trước: ribbon render từ HTML tĩnh `SETTINGS_STATIC`/`ribbon-static.ts`, click bubble qua `onSettingsClick` so khớp theo text nút). Thêm nút mới (icon trang giấy + dấu cộng xanh, khuôn "Chèn" ở `HOME_DATA`) + 1 nhánh action mở thẳng sheet Tạo (không qua danh sách). Xoá hẳn khối toolbar rời đợt 1. Kiểm bằng Playwright thật + grep xác nhận 0 dấu vết nút cũ. `tsc`/`eslint` sạch. Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md`.
- **200826** — Cải thiện UI/UX màn "Cấu hình > Template báo cáo" (FE, `feature_v2`), **đợt 3**: user tự dùng thật, báo 2 lỗi kèm ảnh. (1) Cột "Công thức tuỳ chỉnh" trong mockup không hiện giá trị demo — viết `evalReportExprDemo` (`ReportTemplateModal.tsx`, tokenizer + recursive-descent, không dùng `eval()`) khớp đúng ngữ pháp thật của `evalExpression` (đọc kỹ `engine.go` trước khi viết: `+ - * /`, so sánh, `MIN/MAX/ABS/ROUND/IF/AND/OR/SUM`, biến thiếu/lỗi runtime → 0), nối vào mockup bằng cách tính TOÀN BỘ dòng theo thứ tự gốc (kể cả dòng ẩn) để dựng map alias tăng dần đúng ngữ nghĩa "chỉ nhìn về trước" của backend — 24 test mới pass, kiểm ảnh thật ra đúng `8,00` cho `[STD_REPORT]*2` (STD_REPORT=4 từ dòng ẩn `5-1`). (2) Nhiều dòng cuộn mất header — thêm `position:sticky` cho hàng chữ cái + hàng nhãn cột (freeze panes); **tự phát hiện + tự sửa 1 bug CSS khi kiểm bằng Playwright:** sticky không ăn vì div bọc bảng có `overflowX:"auto"` riêng — theo CSS spec việc này tự ép `overflow-y` thành `auto`, biến div đó (cao bằng nội dung, không giới hạn) thành scroll container riêng khiến sticky bám nhầm vào nó; bỏ `overflowX` dư thừa đó, để div cha ngoài (đã tự cuộn cả 2 chiều) xử lý — sửa xong header đứng lại đúng khi cuộn qua 14 dòng. `tsc`/`eslint`/`vitest` sạch. Không đụng BE (chỉ đọc `engine.go` để lấy đúng ngữ pháp). Chưa commit (gộp cả 3 đợt cùng 1 file cũ + 2 file mới). Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md`.
- **200826** — Cải thiện UI/UX màn "Cấu hình > Template báo cáo" (FE, `feature_v2`), 2 đợt cùng ngày, gộp 1 file sửa. Đợt 1: (1) dời nút "+ Thêm template báo cáo" ra khỏi formula bar sang 1 hàng toolbar riêng; (2) mockup "Xem trước hình dạng báo cáo" trong sheet Sửa/Tạo dùng dữ liệu thật `data.emps[0]` cho identity/component/sum, `expr` hiện "…" vì không có evaluator ở FE. Đợt 2 (sau khi hỏi lại 3 điểm đánh đổi hành vi qua `AskUserQuestion`, user chọn phương án an toàn cho cả 3 — không dồn công thức vào fx-bar chung, không gộp nút Lưu vào ribbon, giữ nút "+ Thêm dòng"): Phase 1+3+4 dựng lại "Cột/dòng" thành 1 bảng `<table>` Excel thật liền mạch (chữ cái A-E, gutter số dòng xuyên suốt kể cả 2 dòng merge "Tên template"/"Mô tả", tô màu nền theo Vai trò); Phase 2 thêm right-click menu (Chèn dòng trên/dưới, Sao chép dòng, Xoá dòng) — **tự phát hiện + tự sửa 1 bug khi kiểm bằng Playwright:** bản đầu portal ra `document.body` khiến biến CSS `--tl-bg` (chỉ khai trong rule `.tl-root`) không resolve, nền menu trong suốt — sửa bằng bỏ portal, render `position:fixed` ngay trong cây DOM hiện tại; Phase 5 Enter ở ô "Nhãn hiển thị" nhảy dòng dưới, Enter ở dòng cuối tự thêm dòng mới (rút gọn, không làm Tab-order đầy đủ vì rủi ro cao hơn lợi ích). `tsc`/`eslint` sạch xuyên suốt 5 phase, kiểm từng phase bằng Playwright thật (script tạm, xoá sau mỗi lần). Chưa commit. Xem `self-docs/Report-Template-Config-UIUX-Excel-Style-200826.md`.
- **200826** — Quyết định business "toàn bộ NV dùng chung 1 bộ cột rule, không có payslip template riêng theo NV" — khảo sát ra 3 cơ chế riêng biệt hỗ trợ điều ngược lại, xử lý cả 3 trên `chore/salary-structure-shared-columns` (BE từ `release/uat-180826`, FE từ `feature_v2`): (1) xoá hẳn `salary_formula_configs` (mồ côi, 0 code đọc, backup 18 dòng trước khi xoá); (2) tắt đường tạo `salary_component_overrides` (`source='formula'`) ở cả 2 UI entry point phát hiện được (`SalaryComponentOverridesPanel.tsx` + `ComponentDrawer.tsx` route `/v1/Core System` — cái thứ 2 không có trong khảo sát ban đầu), giữ nguyên `source='cell_pin'` (Nhập Excel/pin ô lương không bị đụng); (3) chặn 403 backend + ẩn UI (`SALARY_STRUCTURE_FEATURE_VISIBLE=false`) cho `employee_payroll_templates`. `go build`/`vet` sạch, test đầy đủ (kể cả DB integration) chỉ còn 3 fail tiền tồn tại xác nhận bằng `git stash` — 0 hồi quy. Chưa push, chưa merge. Xem `self-docs/Salary-Structure-Shared-Columns-200826.md`.

- **200826** — Thi hành 5 hạng mục RBAC user chọn (`self-docs/RBAC-Enhancement-Batch-200826.md`) trên `release/uat-180826`: (1) đóng 4 route "unknown" vào allowlist — dọn sạch 6 fail baseline (`go test` 759/6→765/0); (2) test audit "quyền ma" mới (`dead_permission_audit_test.go`) chạy trên bản clone dev, bắt đúng lại case `cb_staff`/`Reports` + tìm thêm 35 case mới + 107 module.action chưa nối dây, chỉ audit không tự sửa; (3) fine-gate `Approvals`+`Pipeline` — phát hiện `RequirePermission` mặc định ALLOW khi chưa seed (opt-out), nên không cần migration, không đổi hành vi hiện tại; (4) rate-limit 30 req/phút/user (`golang.org/x/time/rate`, middleware mới) cho 4 route ghi/xuất hàng loạt; (5) xác nhận audit trail không còn ca nào khác cần vá ngoài `salary_component_history`/`employee_history`. `go build`/`go vet` sạch, `765 passed/0 failed`. Chưa commit.
- **200826** — Test RBAC + reports end-to-end thật trên `release/uat-180826` (bản clone dữ liệu dev, xoá sau khi xong): xác nhận `hr_admin` xem được catalog + tải file Excel thật có dữ liệu nhân viên thật (không phải file rỗng); `config_admin` bị chặn đúng như phát hiện tĩnh trước đó (0/6 quyền `Settings.SalaryComponents`); **phát hiện mới** — `cb_staff` có `Reports.export=true` trong DB nhưng KHÔNG BAO GIỜ vào được `/reports/*` vì bị `RequireRole("hr_admin")` chặn cứng ở tầng ngoài trước khi tới permission check — "quyền ma", cần hỏi HR có chủ ý không. Đối chiếu bộ test tự động sẵn có (241 route) xác nhận `salary-components`/`reports` đã có role-gate đầy đủ, 6 fail còn lại thuộc route khác không liên quan. Ghi nhận sự cố môi trường cục bộ: DSN Postgres với `password=` rỗng gây lệch kết nối trên máy này (chưa rõ nguyên nhân gốc, đã né được bằng mật khẩu thật). Xem `self-docs/Handoff-Salary-Rule-Security-Configurability-200826.md` mục 7.
- **200826** — Đối chiếu 7 mục "chưa kiểm" trong `Handoff-Salary-Rule-Security-Configurability-200826.md` (RBAC role thật `cb_staff`/`hr_admin` có quyền `Settings.SalaryComponents`, `config_admin` không có quyền gì dù tên gợi ý; không có rate limit; `salary_component_history` bị self-heal migration bỏ qua; endpoint `Validate` an toàn; `/reorder`/`/visible` không đụng formula; phát hiện thêm `ot_multiplier_configs` mồ côi tương tự `salary_formula_configs`; migration xoá cấu hình vô điều kiện đã bị vô hiệu hoá). Từ đó `/propose` SPEC `self-docs/Salary-Formula-Audit-Orphan-Config-Hardening-200826.md`, user chọn Việc 1-A (vá audit) + Việc 2-3 (giữ nguyên `ot_multiplier_configs`, chờ HR). Thi hành Việc 1-A trên `release/uat-180826`: thêm 1 câu INSERT tổng quát vào `MigrationSeedInitialComponentVersions` (`internal/database/database.go`) bắt audit trail cho toàn bộ 11 khối self-heal — verify bằng cách tạo drift thật trên bản clone dữ liệu dev (không đụng DB thật), xác nhận ghi đúng + không trùng lặp khi chạy lại. `go test ./...` giữ nguyên baseline 759 pass/6 fail. Ngoài ra xác nhận tính năng reports đã có download `.xlsx` thật, không chỉ view. Chưa commit.
- **190826** — Tạo dump DB adapter `payroll_adapter` (PG 14.18, 55 bảng, 511 MB, ~208.500 dòng): `payroll_adapter-full-190826.dump` **81 MB** (`pg_dump -Fc`, TOC 411 mục / 55 `TABLE DATA`, kiểm toàn vẹn bằng `pg_restore -l`) + `payroll_adapter-schema-190826.sql` 116 KB (0 dòng dữ liệu) + README. Quét an toàn khác loại PII: **không có credential nào trong DB** — 0 cột tên `password`/`secret`/`token`/`credential`/`api_key`/`auth`, 0 bảng config, 0 dòng `wd_change_log` chứa chuỗi kiểu `Bearer`; thông tin ISU Workday chỉ ở `.env`. **ĐÍNH CHÍNH quan trọng phát hiện khi làm việc này:** khẳng định của tôi *"`Business_Title` đã kiểm trên 8.429/8.429 payload worker thật"* là **SAI — đúng là 8.427/8.429**. Nguyên nhân: tôi đo `with_jobdata 8429/8429` (tức `Worker_Job_Data` khác NULL) rồi **suy ra** `Business_Title` cũng đủ, hai chuyện không tương đương. Hai payload thiếu là `Worker_ID` 100206 và EE26000162 — cả hai CÓ `Position_Data` với 12 khoá nhưng **không có khoá `Business_Title`**; đã loại giả thuyết `Worker_Job_Data` dạng mảng (cả 8429 đều kiểu `object`). Hệ quả thật: backend ghi `position_name` rỗng cho 2 người đó, hành vi đúng và đã có test bao, và `100206` **có** trong `employees` nên là ca thật sẽ xảy ra. Đã sửa con số ở 5 file bàn giao + bản `.html` + publish lại artifact, và thêm mục ĐÍNH CHÍNH vào `Review-DB-Cleanup-Workday-Bridge-180826.md`. **Bài học lặp lại đúng bài học lỗi dấu phẩy `schema.sql`: đo trực tiếp thứ mình khẳng định, đừng suy ra từ phép đo lân cận.**

- **180826** — Tạo dump **schema-only** của DB backend dev để gửi đồng nghiệp tham khảo: `self-docs/files/payroll_engine-schema-180826.sql` (161 KB, `payroll_engine` PG 14.18, **79 bảng / 242 index / 63 FK**) + README đi kèm. Chủ động chọn schema-only thay vì kèm dữ liệu và nói rõ lý do với user: DB gốc có **12.044 nhân viên thật** kèm họ tên/email/CCCD/lương/tài khoản ngân hàng — không phát hành mặc định. Quét trước khi phát hành: **0 email, 0 token/khoá thật** (`api_key` chỉ là định nghĩa cột default rỗng), 0 DEFAULT đáng ngờ; chỉ có 1 function + 1 trigger (`employee_roles_fill_email`) và 3 dòng RLS. **Smoke test cứu một lần bàn giao hỏng:** bản dump đầu dùng `--schema=public` khiến pg_dump phát ra `CREATE SCHEMA public;` → restore chết ngay dòng 23 (`schema "public" already exists`) và ra **0 bảng**; đổi sang `--exclude-schema=atlas_schema_revisions` thì restore với `ON_ERROR_STOP=1` cho **0 lỗi**, 79 bảng, `diff` danh sách bảng với nguồn ra rỗng, `employees` đủ 212 cột, index 242 và FK 63 khớp tuyệt đối. **Sau đó user yêu cầu bản đầy đủ kèm dữ liệu** (lý do: đồng nghiệp đã có dữ liệu tương đương) — đã tạo `payroll_engine-full-180826.dump` 42 MB định dạng custom (`pg_dump -Fc`, 2,8 giây, TOC 533 mục / 80 `TABLE DATA`, kiểm toàn vẹn bằng `pg_restore -l` chứ không restore thật theo đúng yêu cầu "không cần smoke"). Gộp README chung cho cả 2 file, đặt cảnh báo dữ-liệu-nhân-sự-thật lên đầu kèm bảng liệt kê chính xác bảng nào chứa gì (12.044 NV, 76.856 work history, 25.865 hợp đồng, 24.530 người phụ thuộc, 10.700 tài khoản NH, 6.438 dòng lương) và chỉ rõ ai không có quyền thì dùng bản schema-only. README cũng ghi: đây là trạng thái ĐÍCH (prod 17/08 còn 146 bảng, sau khi apply 11 migration sẽ về đúng 79 và trùng khít), và **nếu file dump lệch với `internal/db/schema.sql` của repo thì tin file dump** vì repo còn thiếu 10 bảng + 10 cột.

- **180826** — Thi hành **Mức 1** của C1+C2 trên nhánh mới `chore/atlas-schema-truth-notes` @ `52eddb0` (tách từ `release/uat-180826` theo yêu cầu, **chưa merge, sẽ rebase sau** — nhớ `--force-with-lease`). Hai khối cảnh báo tại chỗ: (1) đầu `internal/db/schema.sql` — thay 2 dòng đang nói SAI ("single source of truth", "DO NOT EDIT BY HAND", trong khi file đã bị sửa tay nhiều lần ở các đợt dọn) bằng liệt kê chính xác 10 bảng + 10 cột thiếu, thừa 0, vì sao 7/10 bảng do `database.go`/`v*.sql` tạo nên Atlas không quản được, và bắt buộc `pg_dump` **16** nếu sinh lại; (2) `atlas.hcl` — ghi rõ `atlas migrate apply` không dùng được ở mọi môi trường kèm output thật `Pending Files: 30 (21 out of order)` + chỗ lệch description trên prod, trỏ runbook §B2 cho cách apply tay, nêu rõ `diff`/`validate`/`lint`/`hash` vẫn dùng được. Cập nhật `tables_code.md` số dòng nguồn 4.327→4.362. Verify: `sqlc generate` chạy sạch **và không sinh diff nào** (chứng minh comment SQL không ảnh hưởng parser), `atlas migrate validate` rc=0, `atlas.hcl` vẫn parse (lỗi duy nhất là role giả trong URL test), `go build`/`vet` rc=0, **0 file `.go` bị đổi** nên không chạy lại test suite. Mức 1 **không sửa được lỗi nào** — đã ghi rõ điều đó trong cả commit message lẫn `00-START-HERE.md` mục 3.5; Mức 2/3 giữ nguyên trong PLAN làm hồ sơ.

- **180826** — Lên plan cho C1 (bảng revision Atlas không phản ánh thực tế) + C2 (`schema.sql` chậm hơn DB thật), user chọn **Mức 1 (chỉ ghi cảnh báo vào tài liệu)** thi hành trên nhánh mới tách từ `develop` **SAU** khi MR UAT merge. Đo đạc để lên plan: dev có **1** dòng revision / prod **2** dòng, trong khi thư mục có **33** file → `atlas migrate status` ra `Pending Files: 30 (21 out of order)` + ERROR non-linear ⇒ `atlas migrate apply` **không dùng được ở bất kỳ môi trường nào** (nhưng `diff`/`validate`/`lint` vẫn chạy vì không đọc DB); phát hiện thêm dòng `20260812000000` trên prod có description `add_employee_roles_email` **≠** tên file cùng version trong repo (`hr_admin_new_accounts_seed`) — file apply lúc đó không phải file đang có. C2: `schema.sql` thiếu **10 bảng + 10 cột, thừa 0** (chỉ đi sau, chưa bao giờ sai); 7/10 bảng do `database.go`/`v*.sql` tạo nên atlas không bao giờ quản được; rào cản kỹ thuật: file gốc từ **PG 16.14** mà máy local chỉ có `pg_dump` **14.18** → phải regen qua Docker `postgres:16`, docker đã cài nhưng chưa chạy. Plan giữ đủ Mức 2/Mức 3 làm hồ sơ (baseline + regen + migration bù) kèm **3 cổng chặn** (chứng minh 32 migration đã có hiệu lực trước khi baseline; migration bù không được chứa `DROP` nào; diễn tập baseline trên bản sao phải cho số bảng/cột không đổi). Nội dung 2 khối cảnh báo của Mức 1 đã soạn sẵn để dán. Tài liệu: `llmwiki/wiki/sources/draft/180826-atlas-baseline-schema-truth-PLAN.md`, `00-START-HERE.md` mục 3.5. **Chưa sửa file nào.**

- **180826** — Dọn nốt nhóm A (nợ code còn lại của review) trên `release/uat-180826`, 3 commit đã push: **A1** `ffdbaed` xoá 14 field chết khỏi struct `Employee` + 10 khối gán trong `ApplyESSOverrides` (kiểm dữ liệu thật trước: 14 dòng có `ess_overrides` khác rỗng, **0 dòng** chứa 10 key sắp gỡ; giữ `EmployeeEducation.RefEducationLevelID` vì là cột khác vẫn sống); **A3** `5e90522` sinh lại `tables_code.md` từ `schema.sql` (69 bảng, loại `atlas_schema_revisions` vì ở schema riêng, đếm lại 42 repo thay cho "~38"); **A2** `1065d2e` regen `sqlc`. **Phát hiện lớn khi làm A2 — lỗi bản review 180826 BỎ SÓT:** `internal/db/schema.sql` có **dấu phẩy thừa** cuối khối `CREATE TABLE employee_educations` → SQL không hợp lệ → `sqlc generate` chết (`schema.sql:541:2: syntax error at or near ")"`), và vì `schema.sql` là `src` của `atlas.hcl` nên luồng `atlas migrate diff` cũng hỏng; xuất hiện từ commit `1cba84e` (đợt 2, 170826), `origin/develop` sạch. Review không bắt được vì tôi parse `schema.sql` bằng regex tự viết thay vì chạy chính công cụ tiêu thụ nó — **bài học: kiểm `schema.sql` bằng `sqlc generate`/`atlas migrate diff`, không tự viết parser**. Sau khi sửa, sqlc chạy sạch và loại 22 cột chết khỏi `gen/models.go` (−456 dòng); package `db_gen` vẫn không ai import nên 0 ảnh hưởng runtime. Cả 3 bước: build/vet rc=0, gofmt sạch, test **975/20 fail list IDENTICAL**. Cài `sqlc` v1.31.1 vào `$(go env GOPATH)/bin`.

- **180826** — Đổi cách merge theo đề xuất user: thay 3 MR liên tiếp vào `develop` (mỗi lần là một lượt deploy UAT) bằng **một nhánh tích hợp `release/uat-180826`** = `feature_v2` + `chore/db-cleanup-legacy-tables` + `feature/workday-bridge-contracts-workhistories`, mở **đúng 1 MR** → UAT chỉ nhận 1 lần deploy và mọi xung đột được giải + kiểm xong trước khi `develop` bị chạm. Dựng thật: merge dọn DB **0 xung đột**, merge Workday **2 xung đột** (`atlas.sum` → giữ cả 2 khối + `atlas migrate hash` ra 33 file/33 dòng; `docs/routes-permissions.md` → cả 2 nhánh cùng chèn mục trước `## Cross-reference`, giữ cả hai). Kiểm nội dung: 15 bảng tự-tái-sinh = 0, endpoint `/ref/*` đã xoá = 0, `SAVEPOINT sp_contract`/`allEntitiesFailed`/`report_templates`/`bulk-set` đều có mặt. `go build`/`go vet` rc=0, test **975 pass / 20 fail — fail list IDENTICAL `feature_v2` thuần (953/20) ⇒ 0 hồi quy**. Push `origin/release/uat-180826` @ `6d612f2` (51 commit, 67 file so với develop). **Hai điểm phải nhớ:** (1) MR !29 và MR nhánh Workday nay thừa, đóng lại; (2) sau khi MR tích hợp merge, **bắt buộc** `feature_v2 ← origin/develop`, không thì lần merge `feature_v2` kế tiếp mang 15 khối `CREATE TABLE IF NOT EXISTS` quay lại. Cập nhật `00-START-HERE.md` mục 3.4 (đánh số lại B0-B3) + runbook §4b.

- **180826** — Đợt dọn DB thứ 5 + chốt số bảng đích. User đề xuất làm trống prod rồi đổ lại data cũ; **kiểm chứng cho thấy phương án đó khó và rủi ro hơn**, đã trình bằng chứng và user đổi hướng: (1) backend TỪ CHỐI boot trên DB trống (`main.go` fail fast `bootstrap from scratch chưa hỗ trợ`); (2) dump data-only cũ KHÔNG nạp được vào schema mới — câu `COPY` liệt kê cả 5 cột đã xoá → `column ... does not exist`; (3) không có nguồn schema sạch đáng tin (`schema.sql` drift, atlas chain out-of-order). Thay vào đó **đạt mục tiêu prod = dev = 79 bảng ngay tại chỗ, 0 dòng dữ liệu mất**: đo thấy **42/43 bảng prod-thừa RỖNG HOÀN TOÀN** (chỉ `goose_db_version` 31 dòng) và **0 FK từ bảng sống trỏ vào** → thêm migration `20260818010000_drop_prod_legacy_tables.sql` xoá cả 43 trong MỘT câu `DROP TABLE` (Postgres tự giải phụ thuộc lẫn nhau). Diễn tập trọn chuỗi trên bản sao dump prod: **146 → 150 (boot tạo 4 bảng feature_v2) → 79 sau khi apply 11 migration**, `diff` danh sách bảng với dev **ra rỗng**, 8 bảng nghiệp vụ chính đếm trước = sau (employees 12.304, employee_contracts 25.865, employee_work_histories 76.856…), restart 3 lần không bảng nào tái sinh, `/adapter-sync/state` trả 200 mảng 2 entity. **Phát hiện lỗi prod tiền tồn tại:** `origin/develop` đã có sẵn `approval_repo.go`/`approval_service.go` + 2 route `/approvals`, `/admin/approval-rules` nhưng 3 bảng `approval_*` chưa từng tạo trên prod → 2 route đó đang hỏng sẵn trên UAT; user đồng ý vá cùng đợt bằng `20260724000000_approval_engine.sql`. Runbook §B2 nay liệt kê đủ **11 file** theo thứ tự + 2 câu `to_regclass` kiểm điều kiện trước. Commit `Core System-backend@19829cd`.

- **180826** — Trả lời câu "sau khi xong hết thì DB backend còn bao nhiêu bảng" bằng cách **dựng lại trạng thái cuối thật** trên bản sao dump prod `payroll_engine_1708` (merge cả 3 nhánh vào 1 worktree → boot backend để `RunMigrations`/`RunFileMigrations` chạy → apply migration): **146 → 150 (code tự tạo 4 bảng của feature_v2) → 119 (−32 bảng, +1 `adapter_sync_state`)**; công thức chung `N_cuối = N_hiện_tại + 4 − 32 (+1)`. Mốc đối chiếu: dev `payroll_engine` đã ở trạng thái cuối = **79** bảng, chênh 40 là tiền tồn tại (prod còn 43 bảng `wd_*`/di sản dev không có, và thiếu 3 bảng `approval_*` vì `20260724000000_approval_engine.sql` chưa từng chạy trên prod). **Hai lỗi thật trong chính runbook tôi viết sáng nay, chỉ lộ khi chạy trên dump prod:** (1) `20260817020000` lỗi `relation "adapter_sync_state" does not exist` — migration `20260813000000` chưa từng apply trên prod, phải chạy trước; (2) **`atlas migrate apply` sẽ BỊ TỪ CHỐI trên prod** — bảng revision chỉ ghi 2 mục (`baseline` + một `20260812000000 add_employee_roles_email` không khớp file nào trong repo) trong khi thư mục có 32 file, `atlas migrate status` trả `Pending Files: 30 (21 out of order)` + `ERROR: ... added out of order`; prod thực tế không dùng Atlas, phải apply từng file bằng `psql` trong một transaction. Đã sửa `DB-Cleanup-Deploy-Runbook-180826.md` §B2 (thay lệnh atlas bằng khối `psql` có thứ tự file + điều kiện trước) và thêm §B3b số bảng đích; cập nhật `00-START-HERE.md` mục 3.4.

- **180826** — Viết `self-docs/Adapter-Workday-Implementation-And-Local-Test-180826.md` (hướng dẫn thi hành + kiểm thử local cho đội adapter, file thứ 3 trong gói bàn giao) sau khi user chốt thứ tự merge `feature_v2` → `chore/db-cleanup-legacy-tables` → `feature/workday-bridge-contracts-workhistories`. **Phát hiện khi khảo sát repo adapter để viết cho đúng: `develop` local của `Core System-adapter` đang cũ 196 commit so với `origin/develop`** — grep trên nhánh local cho 0 hit khiến suýt kết luận sai là vòng lặp transfer event không tồn tại; thực tế `origin/develop` có đủ (`internal/sync/sync_transfer_events.go` gọi `InsertWorkerTransferEvent` dòng 64, `RefreshWorkerTransferEvent` dòng 108; `cmd/adapter/main.go` chạy nó theo cron `TRANSACTION_LOG_SYNC_CRON` mặc định 03:00 hằng ngày). Nhờ đó tài liệu chỉ đích danh **việc thiếu duy nhất là 2 chỗ ghi `wd_change_log`**, không bắt họ viết lại thứ đã có. Ghi kèm 3 bẫy đo được: FK `wd_worker_transfer_events.log_wid` → `wd_worker_transaction_logs(wid)` nên fixture phải chèn dòng cha trước; lỗi múi giờ **chỉ lộ** với mốc 17:00–23:59 UTC nên phải chọn đúng mốc mà test; `applied=0` không lỗi phần lớn là do `Worker_ID` không khớp `employees.employee_code` nào chứ không phải code sai. Gói gửi đội adapter chốt **3 file** (handover thứ tự → thi hành/test → hợp đồng dữ liệu) + link artifact, **không gửi** `00-START-HERE.md` và runbook (nội bộ backend).

- **180826** — Chốt thứ tự merge sau khi user xác nhận **`feature_v2` là thứ lên UAT tiếp theo**: gộp hết vào `develop` theo `feature_v2` → dọn DB → Workday, rồi `atlas migrate apply` **MỘT lần cho cả 8 migration** (thay vì 2 lần) — trạng thái giữa chừng (code mới + DB chưa đổi) đã kiểm chứng an toàn vô thời hạn nên gộp được. Diễn tập đúng thứ tự này trên worktree tạm: B1/B2 **0 xung đột** (quan trọng: các dòng đã xoá KHÔNG bị `feature_v2` kéo ngược lại), B3 **2 xung đột** — `atlas.sum` như dự đoán **+ `docs/routes-permissions.md` chưa từng lường trước** (cả `feature_v2` lẫn nhánh Workday cùng chèn một mục mới ngay trước `## Cross-reference`; giải bằng giữ cả hai mục). Kết quả cuối: 15 bảng tự-tái-sinh = 0, 3 endpoint `/ref/*` = 0, code cả 3 nhánh có đủ, build/vet rc=0, **975 pass / 20 fail — fail list IDENTICAL `feature_v2` thuần (953/20) ⇒ 0 hồi quy**. Ghi runbook B0-B5 kèm lệnh giải từng xung đột vào `00-START-HERE.md` mục 3.4 (thay 2 phương án bằng 1 phương án đã chốt) + `DB-Cleanup-Deploy-Runbook-180826.md` mục 4b. Kiểm phụ: `feature_v2` không thêm `migrations/v*.sql` nào, không có atlas migration riêng, không chạm 32 bảng đã xoá; 4 bảng mới của nó tự tạo lúc boot nên UAT không cần thao tác DB thủ công.

- **180826** — Push nhánh `chore/db-cleanup-legacy-tables` lên GitLab `code.vsol.vn/Enterprise/Core System-resource/Core System-backend` (21 commit, chờ user tạo MR vào `develop`) + **diễn tập merge Phase 2b trên local** trong worktree tạm: `develop`←cleanup rồi `feature_v2`←`develop`, **cả 2 bước 0 file xung đột**; kiểm 15 bảng tự-tái-sinh × `CREATE TABLE IF NOT EXISTS` = **0**, 3 endpoint `/ref/*` = **0**, 32 bảng đã DROP không còn tham chiếu `.go` nào; test `feature_v2` **953/20 trước = 953/20 sau, fail list identical → 0 hồi quy**. Bẫy tự dính rồi tự sửa: `CREATE DATABASE ... TEMPLATE payroll_engine` **thất bại im lặng** khi DBeaver đang mở kết nối → lượt đo đầu cho 743/141 toàn `pq: database does not exist`, vô giá trị; đo lại bằng `pg_dump`/`pg_restore` mới ra số thật. Ghi hướng dẫn bắt buộc cho đội `feature_v2` vào `self-docs/00-START-HERE.md` mục **3.4** (nơi mọi người luôn đọc trước) + đổi dòng "Việc kế tiếp" ở mục 0 trỏ tới đó. Template mapping cho đội adapter đã publish thành trang chia sẻ được. Sau đó push nốt nhánh `feature/workday-bridge-contracts-workhistories` (7 commit) lên cùng remote — **cả 2 nhánh nay đã lên GitLab, SHA khớp local** (`d5a8c78` / `1a7d43b`), chờ user tạo 2 MR vào `develop`. Lưu ý cho lúc merge: MR nào merge SAU sẽ xung đột `atlas/migrations/atlas.sum` (đã thử thật) — giữ cả 2 khối entry rồi chạy `atlas migrate hash`. Sau khi user cho biết **`develop` đang chạy UAT và `feature_v2` chưa merge vào**, chốt lại thứ tự B0-B5 (backup → merge cleanup vào develop/UAT nhận CODE trước → apply migration lên DB UAT sau → merge develop vào feature_v2 song song → MR Workday → cuối cùng feature_v2 vào develop), ghi vào `00-START-HERE.md` mục 3.4; điểm mấu chốt: B1 và B2 tách rời được nên nếu UAT đang nghiệm thu dở thì hoãn riêng B2. Kiểm thêm: `feature_v2` không đụng `adapter_sync*`/`atlas/` nên không tham gia xung đột `atlas.sum`; migration nhánh Workday chỉ là 1 câu INSERT vô hại; scheduler chỉ chạy khi `WD_ADAPTER_URL` khác rỗng nên UAT để trống là an toàn. Viết `self-docs/Adapter-Workday-Handover-180826.md` — điểm vào cho đội adapter, chia V0 (đo quy mô backfill) → V1 (đường ghi cơ bản) → V2 (múi giờ) → V3 (đường refresh) → V4 (backfill) kèm lý do thứ tự + điều kiện xong từng bước; **phát hiện khi viết: xoá nhánh adapter ở T14 đồng nghĩa đường ghi CƠ BẢN cũng chưa tồn tại phía họ, không chỉ 3 việc A/B/C của review** — nên phải tách V1 riêng, nếu chỉ gửi template thì họ hiểu nhầm là chỉ còn 3 việc.

- **180826** — Thi hành sửa theo review 180826 (chỉ Backend, theo quyết định phạm vi mới: dữ liệu Workday đi Workday→Adapter→Backend, ta chỉ dựng phía Backend + phát hành template mapping cho đội Adapter). **Phase 1** `chore/db-cleanup-legacy-tables` 5 commit (`6bdd893`..`d5a8c78`): xoá 3 endpoint `/ref/*` chết (kiểm bằng server thật: 404 vs 200), sửa header `tables_db.md` + ghi thẳng drift tiền tồn tại vào đầu file, migration mới `20260818000000` xoá 3 cột FK mồ côi (đã apply lên DB dev sau khi `pg_dump -Fc` 43MB: 213→212 cột, 12.044 dòng không đổi), cảnh báo 2 lớp migration đá nhau, runbook triển khai + backup bù 4 bảng seed. Gate: 917/9 = baseline. **Phase 3** `feature/workday-bridge-contracts-workhistories` 3 commit (`d628271`/`257c676`/`1a7d43b`): G1 truncate-theo-rune + SAVEPOINT (hợp đồng hỏng không còn làm kẹt vĩnh viễn sync employees), G7 chặn nhân đôi 25.865 dòng `pipeline` bằng CTE target + `NOT EXISTS`, G8 khoá `DO UPDATE` theo source, G6 `applied` đếm theo `RowsAffected`, D3 trả 500 khi mọi entity lỗi. Gate: 939/9 = baseline + 15 test mới. **Hai lỗi tự phát hiện lúc thi hành, không có trong PLAN:** (1) `NOT EXISTS` đặt chung `WHERE` vẫn chèn trùng khi `employee_code` trùng — vì nó lọc từng dòng ứng viên TRƯỚC `LIMIT`, phải đổi sang CTE chọn-trước-kiểm-sau; (2) test tích hợp bắt ngay `pq: inconsistent types deduced for parameter $2` — `INSERT ... SELECT` không truyền kiểu cột đích xuống tham số trong SELECT-list, bản kiểm psql thủ công không lộ vì dùng literal. **Đính chính review gốc:** khẳng định "`employees (213 cols)` sai" của tôi là sai — 213 là số cột thật, `b0d4883` đặt cố ý. **Phase 4:** xoá nhánh `Core System-adapter@feature/workday-bridge-transfer-changelog` sau khi lưu patch vào `self-docs/files/archive-.../`, phát hành `self-docs/Adapter-Workday-ChangeLog-Mapping-Template-180826.md` (hợp đồng dữ liệu + 3 yêu cầu bắt buộc G2/G3/G9 + 5 câu SQL tự kiểm). Tài liệu: `Review-DB-Cleanup-Workday-Bridge-180826.md` (mục THI HÀNH), `DB-Cleanup-Deploy-Runbook-180826.md`, SPEC/PLAN `llmwiki/wiki/sources/draft/180826-fix-review-db-cleanup-workday(.md/-PLAN.md)`, `document-map.md`. **Chưa push, chưa merge** — Phase 2/5 (merge + `atlas migrate hash`) chờ người dùng.

- **180826** — Review độc lập trước merge cho 2 luồng việc của 170826 (dọn DB 4 đợt + cầu nối Workday 2 repo): tự đo lại toàn bộ, không tin số cũ — grep 3 repo cho 32 bảng/25 cột, replay 6 migration trên bản sao dump prod `payroll_engine_1708` (146→114 bảng, employees 228→212 cột, 0 dòng mất, đúng 4 NOTICE CASCADE đã ghi trong comment), dựng 2 DB sạch từ atlas migrations để chứng minh `schema.sql` không thêm drift, chạy test 4 nhánh (BE cleanup 917/9 = develop 917/9 fail IDENTICAL; BE workday 924/9; adapter 163/0 vs 162/0) → **0 hồi quy cả 2 luồng**. Tìm được 8 vấn đề luồng 1 + 10 vấn đề luồng 2, nặng nhất: **G1** lỗi upsert `employee_contracts` (cột `sign_status VARCHAR(50)` không truncate) làm kẹt vĩnh viễn con trỏ sync employees — chặn merge; **G2/G3** `RefreshWorkerTransferEvent` + dòng transfer event cũ không sinh `wd_change_log` nên mất dữ liệu âm thầm; **F1** 3 endpoint `/ref/{provinces,banks,relation-types}` còn trỏ bảng đã DROP → 500 thay vì 404 (đợt 4 quên tiền lệ của đợt 2); **F4** không runbook: backend KHÔNG chạy Atlas, phải apply tay và bắt buộc deploy code TRƯỚC nếu không 15 bảng tái sinh. Đã thử merge thật: `atlas.sum` xung đột chắc chắn giữa 2 luồng, giải bằng `atlas migrate hash` (đã kiểm chứng sạch). Xác minh Workday thật: `Business_Title` đã kiểm trên 8429/8429 payload thật; `Employee_Contracts_Data` **0/8429** — chưa có mẫu thật nào. Tài liệu: `self-docs/Review-DB-Cleanup-Workday-Bridge-180826.md`. Không sửa code, không merge, không push.

- **170826** — Dọn dẹp DB đợt 4 (cuối): sau khi phân tích toàn diện 33 bảng cô lập + 63 bảng có FK, đối chiếu bằng dump `payroll_dev` thật (user tự tải + yêu cầu restore `payroll_engine_1708`, 146 bảng/12.304 NV — khớp 100% số liệu với local dev, đồng thời hé lộ 41 bảng `wd_*`/`payroll_rows` rỗng hoàn toàn từ một nỗ lực nhúng Workday cũ đã bỏ dở, không liên quan phạm vi này), user xác nhận xoá 16 bảng chết (9 độc lập + 7 có FK phụ thuộc, đúng thứ tự: `attendance_items`/`overtime_records`→`shift_configs`, `leave_budget_details`→`leave_budgets`, `leave_requests`, `sync_errors`, `ref_provinces`, `job_rotation_types`, `payroll_table_configs`, `ref_banks`, `ref_relation_types`, `employee_insurance_salaries`, `salary_history`, `payroll_record_ot_details`, `payroll_record_allowances`) + 9 cột FK chết trên `employees`/`org_structures` (trỏ `hris_provinces`/`hris_districts`/`work_locations`) — loại trừ `salary_formula_configs` (user tự phân tích riêng) và `work_locations` (giữ bảng, nhưng vẫn xoá cột `employees.work_location_id` trỏ tới nó theo xác nhận `AskUserQuestion`). Phát hiện thêm lúc áp dụng thật (không có trong SPEC ban đầu): `ref_banks`/`ref_relation_types` có FK chết bị bỏ sót ở vòng quét đầu (trỏ vào từ `employee_bank_accounts`/`employee_dependents`, cả 2 đều 0% dữ liệu) — dừng lại, xác nhận, dùng `CASCADE` đúng phạm vi. Sự cố kỹ thuật tự phát hiện + tự sửa: script Python xoá khối `database.go` làm mất line ending CRLF gốc của file — phát hiện qua diff bất thường, sửa bằng 1 commit riêng, xác nhận diff cuối chỉ đúng phạm vi dự kiến. Ghi nhận (không sửa, ngoài phạm vi): `schema.sql`/`tables_db.md` đã lệch sẵn 6 cột `employees` với DB thật từ trước đợt này. `go test ./...`: 918 passed/8 failed cả trước lẫn sau (khớp baseline 4 test case đã biết) — 0 hồi quy. Tài liệu: `self-docs/DB-Cleanup-Standalone-Tables-FK-Columns-170826.md` (canonical), SPEC/PLAN `llmwiki/wiki/sources/draft/170826-db-cleanup-standalone-tables-fk-columns(.md/-PLAN.md)`, `document-map.md`. Commit: `Core System-backend@b0d4883` (6 commit mới) trên `chore/db-cleanup-legacy-tables` — chưa push, chưa merge. **Toàn bộ 4 đợt dọn DB trong phiên này (170826) nay đã hoàn tất trên nhánh này.**

- **170826** — Xoá 5 cột FK chết trên `employees` trỏ tới `ref_provinces` (`ref_birth_province_id`, `ref_home_province_id`, `ref_perm_province_id`, `ref_recruit_province_id`, `ref_temp_province_id`) — 0/12044 dòng có giá trị, không code nào (backend/frontend) từng ghi (chỉ khai báo struct field trong `models/employee.go`, xác nhận grep `feature_v2`). Xác nhận `ref_provinces` bản thân bảng (97 dòng seed) cũng không được đọc/ghi bởi bất kỳ code runtime nào — chỉ migration seed 1 lần, endpoint `/ref/provinces` không frontend nào gọi. Không đụng `birth_province_ref_id`/`recruit_province_ref_id` (cột khác tên gần giống, trỏ `hris_provinces`, đang sống thật). Tiếp tục dùng nhánh `chore/db-cleanup-legacy-tables` (đợt 3, không tạo nhánh mới). `go test ./...`: 918 passed/8 failed (khớp baseline không đổi) — 0 hồi quy. Commit `Core System-backend@bd75d97` (3 commit: `ca64082`/`150d6bb`/`bd75d97`) — chưa push, chưa merge.

- **170826** — Xây cầu nối Workday → `employee_contracts` + `employee_work_histories` (gộp 2 phát hiện điều tra liên tiếp thành 1 SPEC/PLAN theo yêu cầu user "gộp vào chung 1 plan sau đó thực hiện"). Phát hiện A (`employee_contracts`, 25.865 dòng snapshot đóng băng 14/05): payload `wd_change_log` entity `worker` đã mang sẵn field hợp đồng (`Include_Employee_Contract_Data`, adapter thêm 01/08) — chỉ cần backend parse thêm, không cần đổi adapter, dùng chung con trỏ `worker`. Phát hiện B (`employee_work_histories`, 3 nguồn `pipeline`/`hris`/`manual`): `wd_worker_transfer_events` (adapter, 05/08) chưa ghi vào `wd_change_log` — bắt buộc sửa `InsertWorkerTransferEvent` thêm 1 điểm ghi mới, entity type mới `worker_transfer_event`. Tổng quát hoá `AdapterSyncService` (trước chỉ hardcode entity `worker`) thành vòng lặp theo danh sách entity, mỗi loại 1 con trỏ riêng — 1 entity lỗi không chặn entity khác. `hris_id` (uuid) sinh từ Contract_ID/log_wid Workday (chuỗi) bằng `uuid.NewSHA1` namespace cố định, ổn định qua nhiều lần sync. Nhánh mới cả 2 repo từ `origin/develop` (`Core System-backend@feature/workday-bridge-contracts-workhistories`, `Core System-adapter@feature/workday-bridge-transfer-changelog`) vì nhánh đang checkout không liên quan (chore dọn DB cũ / feature_v1 cũ hơn develop 50 commit). **Giới hạn môi trường quan trọng đã ghi rõ:** không có kết nối Workday thật + DB `payroll_adapter` cục bộ đồng bộ trước ngày có tính năng hợp đồng/transfer-event — verify bằng unit test fixture (7 test) + dry-run thật trên 8429 dòng `wd_change_log` production-shape tại local (`total=8429 withContract=0 parseErr=0`, xác nhận không crash trên dữ liệu cũ) thay vì chạy sync thật end-to-end. `go test ./...`: backend 918/8 (khớp baseline, `comm` rỗng), adapter 163/0 (không đổi) — 0 hồi quy cả 2 repo. Tài liệu: `self-docs/Workday-Bridge-Contracts-WorkHistories-170826.md` (canonical), SPEC/PLAN `llmwiki/wiki/sources/draft/170826-workday-bridge-contracts-workhistories(.md/-PLAN.md)`, `document-map.md`. Commit: `Core System-backend@319793d` (4 commit: `8321f37`/`63fc5c0`/`3a69894`/`319793d`), `Core System-adapter@a152d98` (1 commit) — cả 2 chưa push, chưa merge.

- **170826** — Dọn dẹp DB đợt 2: rà soát cụm bảng học vấn (`employee_educations`/`employees` + 8 bảng `ref_*`/lookup) theo yêu cầu user sau khi xem sơ đồ ER. Xoá 6 bảng `ref_*` hoàn toàn không dùng (`ref_majors`, `ref_graduate_schools` — 0 dòng; `ref_academic_degrees`, `ref_graduation_ranks`, `ref_degree_types`, `ref_training_forms` — có seed nhỏ, đã backup) + 11 cột FK chết trên `employee_educations`/`employees` (0% dữ liệu, xác nhận bằng `psql` trước khi xoá). Giữ nguyên `employee_educations`, `employees`, `ref_education_levels`, `majors`, `graduate_schools` (đang dùng thật) — dùng cột (phía trỏ đi) trước rồi mới xoá bảng nên không cần `CASCADE`. Giữa chừng user báo dữ liệu HRIS sắp chuyển sang lấy từ Workday thay vì portal — kiểm `Core System-adapter` xác nhận Workday integration hiện tại chưa map field học vấn nào, hỏi qua `AskUserQuestion` xem có nên hoãn xoá không, **user chọn "Vẫn dọn ref_* dư thừa ngay"**. Sửa thêm `internal/transport/http/ref/handler.go` (xoá 6 endpoint `/ref/{table}` tương ứng, verify bằng `curl` thật trả `404`/"unknown ref table" thay vì lỗi SQL 500) và `internal/db/schema.sql`/`tables_db.md` (script Python xoá khối, tái dùng phương pháp đợt 1). `go test ./...`: baseline 918 passed/8 failed → sau vẫn 918/8, đối chiếu tên test fail bằng `comm` cho kết quả rỗng — 0 hồi quy. Cùng nhánh `chore/db-cleanup-legacy-tables` với đợt 1 (không tạo nhánh mới). Tài liệu: `self-docs/DB-Cleanup-Education-Ref-Tables-170826.md` (canonical), SPEC/PLAN `llmwiki/wiki/sources/draft/170826-db-cleanup-education-ref-tables(.md/-PLAN.md)`, `document-map.md`. Commit `Core System-backend@fe2d305` (4 commit mới: `ee13c7a`/`a73d05b`/`1cba84e`/`fe2d305`) — chưa push, chưa merge.
- **170826** — Dọn dẹp DB: rà soát toàn bộ 107 bảng (không chỉ `hris_*`) đối chiếu code cả 3 repo, xoá 10 bảng không còn code nào dùng (`hris_oauth_tokens`, `hris_payroll_reports`, `ref_hospitals`, `hris_payroll_snapshots`, `hris_payroll_detail_snapshots`, `hris_allowance_override`, `allowance_overrides_bak_20260624`, `allowance_items`, `allowance_types`, `employee_change_requests`) trên nhánh mới `Core System-backend@chore/db-cleanup-legacy-tables` (từ `origin/develop`, không đụng `feature_v2` đang mở merge) — theo đúng yêu cầu "nếu ảnh hưởng version hiện tại thì chuyển nhánh mới". `hris_districts` (758 dòng) chủ động **giữ lại** dù cũng không có code dùng, theo quyết định trực tiếp của user. Backup dữ liệu 7/10 bảng có dòng bằng `pg_dump --data-only` trước khi xoá; `DROP TABLE ... CASCADE` cho 2 bảng có FK trỏ vào từ bảng đang sống (`employees.ref_hospital_id`, `payroll_record_allowances.allowance_item_id` — cả 2 cột/bảng xác nhận rỗng trước khi xoá). Sửa `database.go` bỏ 2 khối tạo bảng tự-hồi mỗi boot (nếu không sẽ vô hiệu hoá migration ở lần restart kế). Đồng bộ `internal/db/schema.sql` (xoá 487 dòng, giữ nguyên định dạng gốc PG16 — không dùng `pg_dump --schema-only` tại máy local PG14 vì gây nhiễu diff không liên quan) + `tables_db.md` (87/97 mục còn lại). Phát hiện + tự sửa 1 lỗi khi rà soát: quét nhầm trên nhánh dựa `develop` làm dương tính giả 3 bảng report-template (code của chúng chỉ có trên `feature_v2` chưa merge) — đã dựng lại bằng `git worktree` từ `feature_v2`. Tài liệu: `self-docs/DB-Cleanup-Legacy-Tables-170826.md` (canonical), SPEC/PLAN `llmwiki/wiki/sources/draft/170826-db-cleanup-legacy-tables(.md/-PLAN.md)`, `document-map.md`. Commit `Core System-backend@84d0565` (4 commit, `032a7c2`/`672dc06`/`1c633b1`/`84d0565`) trên `chore/db-cleanup-legacy-tables` — chưa push, chưa merge.

- **120826** — Sửa lỗ bảo mật "email admin toàn quyền hardcode trong source code" (`EnsureSuperAdmins`, `Core System-backend`) — phát hiện khi user hỏi "trên prod thì ai là admin để setup". Khảo sát xác nhận: seed cứng `user@company.test` chạy mỗi lần boot ở mọi môi trường kể cả prod, không phân biệt dev/staging/prod, không audit lúc cấp, không cách thu hồi mà không sửa code (rủi ro kiểu TASK-REF). Đề xuất qua chat (không AskUserQuestion, xác nhận bằng lời) + `/propose`+`/plan` đầy đủ: bỏ hardcode, seed từ biến môi trường mới `INITIAL_SUPER_ADMIN_EMAIL` CHỈ KHI bảng `super_admins` đang rỗng (không phải mỗi boot vô điều kiện), giữ nguyên nhánh `DEV_USER_EMAIL`. **Nhánh làm việc: `fix/admin-bootstrap-security`, tạo mới từ `origin/develop`** (không phải `feature_v2`) theo đúng yêu cầu user — vì RBAC/permission liên quan đã merge vào `develop` từ nhánh khác (`feature/rbac-email-role-assignment`) từ hôm trước, tách nhánh mới để merge sạch không kéo Nhập Excel. Đổi chữ ký `EnsureSuperAdmins(db, devEmail, initialSuperAdminEmail)` — phải sửa kèm 4 call site test có sẵn (`pipeline_route_integration_test.go`/`admin_read_route_integration_test.go`, đã bị bỏ sót nếu không grep kỹ). 4 test tích hợp mới tự snapshot/khôi phục bảng `super_admins` toàn cục (dùng chung bởi nhiều test khác) — xác nhận bằng `psql` tay dữ liệu thật nguyên vẹn trước/sau (3 dòng thật không đổi). `go test ./...`: baseline nhánh mới 913/6 fail (khác baseline `feature_v2` cũ, đo lại đúng) → sau 917/6 fail, 0 hồi quy. Không tạo `.env.example` (chưa tồn tại trong repo). Viết runbook vận hành đầy đủ (bootstrap môi trường mới, SQL thu hồi, SQL thêm/xoá thủ công). Tài liệu: `self-docs/Super-Admin-Bootstrap-Runbook-120826.md`, `document-map.md`. Commit: `Core System-backend@f0dc6f3` trên `fix/admin-bootstrap-security` — chờ push.
- **110826** — Xây luồng "Nhập Excel" thật (menu Tệp, bảng lương) — trước đây chỉ đọc-xem-trước, `applyImportedRows` là stub luôn lỗi. Đọc code kỹ trước khi làm (`/orca-workflow` → `/propose` → `/plan` → thi hành), chốt 4 quyết định qua `AskUserQuestion`: khớp header cả mã kỹ thuật lẫn tên hiển thị; NV lạ trong Excel bỏ qua dòng + cảnh báo (không chặn cả file); ô trống giữ nguyên giá trị cũ; làm đầy đủ cả BE+FE cùng đợt. Backend thêm `POST /Core System/cells/bulk-set/{periodId}` (tách `writeOverrideCell` dùng chung từ `SetCell`, `BulkSetCells` ghi nhiều ô rồi `CalculateOne` đúng 1 lần/NV — không phải 1 lần/ô như lặp `cell-set` cũ; middleware mới `RequireBulkEmpCompanyScope` kiểm TỪNG mã NV trong batch, 403 toàn bộ nếu 1 mã ngoài phạm vi công ty, khác với NV "không có trong kỳ" chỉ lỗi cục bộ). Frontend: `matchImportToGrid` (hàm thuần, VLOOKUP theo mã NV + đối chiếu header, 3 test vitest) + UI xem trước đầy đủ trước khi ghi thật. Không migration DB (tái dùng nguyên bảng override/cell_edits/audit_logs). `go test ./...`: baseline tự đo 915 passed/7 failed → sau 922 passed/7 failed, khớp baseline, 0 hồi quy. **Kiểm chứng end-to-end bằng `curl` trên dữ liệu dev thật** (không có công cụ điều khiển trình duyệt trong phiên này): đổi `PHONE_ALLOW` 1 NV thật, xác nhận `GROSS` tính lại đúng + audit trail đúng reason, rồi khôi phục lại giá trị cũ — chưa click-through UI trong trình duyệt thật. Tài liệu: `self-docs/Core System-Excel-Import-VLookup-110826.md` (canonical), `document-map.md`, `routes-permissions.md` (BE). Commit: `Core System-backend@9d53e6a` + `Core System-frontend@4d34e6b`, cả hai trên `feature_v2` — chưa push.
- **110826** — Phân tích + triển khai xong yêu cầu **"gán quyền RBAC chỉ cần điền theo email, không bắt buộc trỏ tới bảng employees"** (`/orca-workflow`, cả `/propose`+`/plan` rồi thi hành theo PLAN). Phát hiện mâu thuẫn kỹ thuật với quyết định 160726 (đổi `scope_company_code`→`scope_company_id` FK vì lo ngại giá trị dễ đổi làm mất quyền âm thầm) trước khi hỏi, chốt qua `AskUserQuestion`: Phương án C — `employee_roles.email` là khoá match quyền (NOT NULL), `employee_id` chuyển nullable chỉ để enrich hiển thị + phát hiện lệch (không dùng để resolve quyền), có báo cáo `GET /roles/email-drift` khi NV đổi email HRIS sau khi được gán quyền — không mất quyền âm thầm không dấu vết. **CÓ migrate dữ liệu**: backfill `employee_roles.email` từ `employees.email` cho 26/26 dòng cũ (migration `atlas/migrations/20260811000000_employee_roles_email_key.sql`, trigger tự điền email từ `employee_id` khi caller không cung cấp — giữ nguyên hành vi ~15 file test insert trực tiếp mà không cần sửa từng file). 7 task: migration (GATE) → 3 điểm resolve quyền (`getAppRoles`/`ResolveCompanyScope`/`ResolveDepartmentScope`) đổi JOIN bắt buộc sang so khớp email trực tiếp → API `AssignRole`/`RemoveRole`/`ListEmployeeRoles`/`AccessReview` theo email (LEFT JOIN employees, `flagAccessReviewExceptions` đổi group-key từ `EmployeeID` sang `Email`; lưu ý kỹ thuật Go: dùng `uuid.NullUUID` chứ không phải `*uuid.UUID` cho tham số nullable — gọi `.Value()` qua pointer nil sẽ panic) → endpoint báo cáo lệch → FE cho gõ email tự do (giữ autocomplete cũ, không bắt buộc chọn) + banner cảnh báo lệch. `go test ./...`: baseline tự đo 904 passed/10 failed (đã biết trước, không liên quan), 0 lệch qua toàn bộ 4 task backend. FE `tsc`/`eslint` sạch (trừ `DevToolkit.tsx` tiền tồn tại). **Kiểm tay trình duyệt thật bằng Playwright** (script tạm, đã xoá sau khi kiểm) qua dev-bypass: gõ email lạ → dòng gợi ý đúng, gán → toast thành công + dòng mới hiện đúng cột, xoá → dòng biến mất; xác nhận DB sạch sau kiểm. Tài liệu: `self-docs/RBAC-Improvement-Analysis-110826.md` (canonical), `document-map.md`, `routes-permissions.md` (2 repo). Commit: `Core System-backend@feature_v2` 4 commit (`77f2c72`/`cbead84`/`7951680`/`5da3e3d`), `Core System-frontend@feature_v2` 1 commit (`846e987`) — **chưa push**. Lưu ý: có phiên khác đang sửa song song vài file FE khác (Excel import vlookup) — đã tách stage cẩn thận, không đụng.
